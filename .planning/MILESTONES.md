# Milestones

## v1.1 Runtime & UX Expansion (Shipped: 2026-07-31)

**Phases completed:** 7 phases, 32 plans, 63 tasks

**Closeout:** Override closeout. Known verification overrides: 10 open artifacts acknowledged (see STATE.md Deferred Items).

### Known Gaps

- **QLT-04:** Physical iOS/Android UAT was not run, and the two-page Chromium
  media flow remains blocked by `blob-unavailable`. Deterministic backend media
  transport and four non-media Chromium rows pass but do not substitute.
- Live relay/Blossom interoperability and target-deployment persistence checks
  remain not run under the accepted residual-risk scripts.

**Key accomplishments:**

- A generation-guarded WebSocket controller now preserves backend runtime continuity while an accessible constellation projects cold start, reconnect, offline, and actionable failure truth.
- A safe-area-aware three-target mobile shell now keeps the verified napplet mounted while account state and canonical sign-out remain truthful across signer and transport changes.
- A closed browser-local theme contract now applies System, Light, or Dark before first paint, updates browser chrome, and exposes accessible account controls without crossing the napplet boundary.
- One accessible semantic ink, bone, and electric-amber palette now themes every shell-owned route and runtime state while the verified napplet frame and global browser theme owner remain stable.
- A minimal sovereign constellation gate now carries one mechanically linked identity from the ready ritual through the emitted SVG favicon.
- Accepted catalog truth now streams before artifact resolution, while immutable install review and exact stale-checked launch keep executable bytes behind backend authority.
- Arbitrary kind-35129 napplets now traverse policy-bounded relay discovery, exact manifest identity checks, Blossom integrity verification, and generation-safe catalog synchronization.
- Strict catalog codecs, correlated verified launch, 32-command WebSocket backpressure, and last-good streamed catalog retention
- Accessible mobile install, partial-state search, and verified launch UX over the backend-authoritative catalog seam
- 1. [Rule 1 - Bug] Preserved canonical caller correlation IDs
- Every-hop DNS and redirect controls now feed one streamed 5 MiB reader with passive MIME sniffing, incremental SHA-256, and exact-loopback Blossom fallback.
- Pinned Blossom SDK uploads now use backend-only scoped authorization, strict descriptor verification, all-required remote settlement, and bounded canonical remote/local outcome tokens.
- Process-owned RESOURCE and Blossom upload services now cross the verified napplet runtime seam with bounded concurrency, complete authority ownership, binary fidelity, and deterministic lifecycle cancellation.
- Backend-verified catalog launches now mint immutable window capabilities for canonical public NIP-19 encoding and isolated string storage across reconnects.
- Canonical atomic storage now preserves isolated string values across restart while enforcing exact backend authority, deterministic ordering, and conservative UTF-8 quotas.
- Eight canonical COMMON operations now combine public NIP-19 codecs, cached-first Applesauce projections, and launch-bound backend signing with required-relay settlement.
- Strict signed-manifest archetype decoding feeds a deterministic, generation-bound intent registry without granting stale catalog authority.
- Exact verified intent dispatch now uses backend-owned navigation reservations and private expiring single-use payload tickets with idempotent settlement.
- Same-origin inert popup reservations, exact-identity surface reuse, and opaque-history sandboxed frame stacks now execute through shell-owned browser controls.
- Production WebSockets now carry exact intent discovery and invocation into opener-safe, single-use verified launch claims without weakening iframe sandboxing or reconnect ownership.
- Exact NAP-MEDIA codecs feed one immutable serialized coordinator that generation-fences account playback ownership and broadcasts every accepted transition.
- Snapshot-first WebSocket media coordination with exact portal controls, server-bound account authority, immediate owner revocation, and generation-safe reconnect grace.
- Snapshot-gated mobile shell media controls with truthful autoplay and hidden-tab state, backed by a passing two-client production WebSocket lifecycle smoke.
- A 100-row pinned NAP registry now makes ten-domain contract drift, invented actions, incomplete evidence, and silent dispatch release-blocking while auditing all 33 v1.1 requirement mappings.
- A zero-red-row hostile boundary matrix now blocks private URL effects, malformed transfer envelopes, and stale binary settlements while preserving sanitized ordered results.
- Exact registry grants and server-owned account, artifact, instance, and generation authority now deny forged or stale capability, catalog, identity, and signer requests with zero effects.
- Queued and in-flight STORAGE effects now revalidate exact authority through atomic rename, while COMMON keeps immediate partial truth and generation-bound cleanup.
- One immutable response policy and exact opaque-frame/current-recipient checks now deny hostile browser sources, origins, sandbox escalation, and stale runtime delivery.
- Intent navigation now requires an exact target claim plus caller commit, while reconnect generation fences and ordered media reducers make stale browser lifecycle work inert.
- A ten-domain lifecycle matrix and built Fresh multi-client transport now prove ordered streaming, ownership fencing, reconnect grace, expiry, and shutdown under the complete release gate.

---
