---
phase: 03-mobile-shell-resilience
plan: "05"
type: execute
wave: 5
depends_on: [03-04]
files_modified: [components/PortalMark.tsx, components/ConnectionConstellation.tsx, routes/_app.tsx, static/logo.svg, static/favicon.ico, tests/portal_brand_test.tsx, tests/shell_resilience_test.tsx]
autonomous: true
requirements: [SHL-02]
must_haves:
  truths:
    - "Fresh starter branding is replaced by one sovereign constellation-gate mark shared by the ready ritual, shell identity, logo, and favicon (D-07, D-38)."
    - "The mark remains recognizable at favicon scale and visible against both accessible ink/bone/amber themes (D-39)."
  artifacts: [components/PortalMark.tsx, static/logo.svg, tests/portal_brand_test.tsx]
  key_links:
    - "PortalMark canonical geometry -> ConnectionConstellation ready state -> logo.svg -> document SVG favicon link"
---
<objective>Establish the sovereign constellation gate as the canonical portal brand, implementing D-07, D-38, and D-39. Purpose: Replace starter identity only after theme and surface contracts are stable. Output: Canonical inline/static SVG geometry, favicon metadata, and scalable-asset verification.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/03-mobile-shell-resilience/03-CONTEXT.md
@.planning/phases/03-mobile-shell-resilience/03-RESEARCH.md
@.planning/phases/03-mobile-shell-resilience/03-04-SUMMARY.md
<interfaces>
`ConnectionConstellation` owns the phase's connection geometry and consumes `PortalMark` for its ready gate.
`components/PortalMark.tsx` is the research-resolved source authority; `static/logo.svg` carries the same geometry and is used directly by the document favicon link. The existing starter `static/favicon.ico` is removed rather than regenerated.
</interfaces></context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Use one canonical gate geometry in ritual and shell metadata</name>
  <read_first>components/ConnectionConstellation.tsx, routes/_app.tsx, assets/styles.css, static/logo.svg, tests/shell_resilience_test.tsx, .planning/phases/03-mobile-shell-resilience/03-RESEARCH.md</read_first>
  <files>components/PortalMark.tsx, components/ConnectionConstellation.tsx, routes/_app.tsx, static/logo.svg, tests/portal_brand_test.tsx</files>
  <behavior>- PortalMark is the source geometry for ready ritual, inline shell identity, and logo SVG.
- Stable geometry identifiers prove derived linkage rather than visual coincidence.
- Both themes preserve mark contrast and non-color connection meaning.
- Fresh starter logo references are absent from document metadata and shell markup.</behavior>
  <action>Start RED with canonical-geometry and metadata assertions, then implement `PortalMark` as the research-resolved minimal node/link sovereign gate per D-38. Consume it as the D-07 ready form of `ConnectionConstellation`, generate or verify `static/logo.svg` from the same stable geometry identifiers, and update `_app.tsx` with one link whose relationship is `icon`, URL is `/logo.svg`, and MIME type is `image/svg+xml`, making the canonical SVG the document favicon. Use the D-39 token contract from Plan 04 for both-theme visibility and retain geometry as the primary state discriminator.</action>
  <verify><automated>deno test -A tests/portal_brand_test.tsx tests/shell_resilience_test.tsx</automated></verify>
  <acceptance_criteria>Tests prove one canonical geometry source across component/ritual/logo, correct both-theme contrast, portal metadata, and absence of Fresh starter logo references.</acceptance_criteria>
  <done>The ready ritual and document shell share one canonical sovereign portal mark.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Retire starter favicon fallback and verify SVG favicon semantics</name>
  <read_first>components/PortalMark.tsx, routes/_app.tsx, static/logo.svg, static/favicon.ico, tests/portal_brand_test.tsx</read_first>
  <files>static/favicon.ico, tests/portal_brand_test.tsx</files>
  <behavior>- The document has one SVG favicon link pointing to `/logo.svg` with `image/svg+xml`.
- Canonical SVG viewBox and defining node/link identifiers remain suitable for browser favicon scaling without a separate raster claim.
- The old Fresh `static/favicon.ico` is absent and production output contains the referenced SVG.</behavior>
  <action>Delete the stale Fresh `static/favicon.ico`; do not generate an ICO or introduce an image-generation dependency. Extend `tests/portal_brand_test.tsx` to parse `_app.tsx` favicon metadata and `static/logo.svg`, assert one `rel="icon"` SVG reference, a valid square viewBox, the canonical stable node/link geometry identifiers, and absence of the old ICO and Fresh asset references. Verify the production build copies `/logo.svg` so the favicon URL resolves.</action>
  <verify><automated>deno test -A tests/portal_brand_test.tsx && deno task check && deno task build</automated></verify>
  <acceptance_criteria>Tests prove one standards-compatible SVG favicon reference to the canonical portal asset, valid scalable SVG structure, removal of the starter ICO, emitted `/logo.svg`, no Fresh references, and successful check/build.</acceptance_criteria>
  <done>The document uses the canonical scalable SVG directly as its favicon with no unsupported ICO-generation step.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| static/inline branding → user perception | Brand and status geometry identify trusted portal surfaces at small sizes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11 | Spoofing | shell branding/status | high | mitigate | Canonical geometry, stable component-to-asset linkage, both-theme contrast, and non-color state semantics identify trusted shell surfaces. |
| T-03-15 | Tampering | static SVG asset/metadata | medium | mitigate | Geometry/linkage tests reject an SVG that diverges from PortalMark or favicon metadata that points elsewhere. |
</threat_model>
## Artifacts this phase produces
- Canonical `PortalMark` and ready-state `ConnectionConstellation` integration
- Canonical `static/logo.svg` used directly as the SVG favicon; obsolete starter `static/favicon.ico` removed
- Geometry-linkage, scalable SVG metadata, and production-build tests
<verification>`deno test -A tests/portal_brand_test.tsx tests/shell_resilience_test.tsx`, `deno task check`, and `deno task build` pass.</verification>
<success_criteria>SHL-02 is complete with one verified portal identity from ritual scale through favicon scale.</success_criteria>
<output>Create `.planning/phases/03-mobile-shell-resilience/03-05-SUMMARY.md` when done.</output>
