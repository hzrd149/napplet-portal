---
phase: 05-resource-and-blossom-transfer
plan: "03"
type: execute
wave: 2
depends_on: [05-01]
files_modified: [deno.json, deno.lock, runtime/blossom_transfer.ts, runtime/accounts.ts, tests/blossom_transfer_test.ts]
autonomous: true
requirements: [UPL-02, UPL-03]
must_haves:
  truths:
    - "Backend-owned authorization uploads validated bytes to every configured non-loopback Blossom server and verifies every returned descriptor."
    - "Required remote partial settlement produces canonical ok=false/status=failed plus explicit sanitized per-server error detail; optional local copying runs only after all required remotes accept and never changes remote success."
    - "Canonical UploadResult and UploadStatus expose every required and optional-local outcome without invented fields: accepted destinations use url/fallbackUrls and bounded sanitized ordinal/class tokens in error cover all outcomes, including ok=true complete with optional-local failure."
  artifacts: [runtime/blossom_transfer.ts, tests/blossom_transfer_test.ts]
  key_links:
    - "active PortalAccounts signer -> scoped kind-24242 authorization -> required Blossom upload"
    - "per-server internal settlement -> closed canonical url/fallbackUrls/status/error projection"
---
<objective>Implement backend-owned Blossom upload and exact settlement semantics. Purpose: satisfy explicit multi-server behavior without overclaiming replication or extending the closed NAP 0.31 result contract. Output: reviewed transfer adapter, signer seam, canonical projection, and deterministic settlement tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/05-resource-and-blossom-transfer/05-CONTEXT.md
@.planning/phases/05-resource-and-blossom-transfer/05-RESEARCH.md
@.planning/phases/05-resource-and-blossom-transfer/05-01-SUMMARY.md
@runtime/accounts.ts
@runtime/settings.ts
<interfaces>`PortalAccounts.signEvent(template): Promise&lt;NostrEvent&gt;` remains the only signer authority. Exact canonical `UploadResult` requires `ok`, `uploadId`, `status`, and `rail`, and optionally permits `url`, `fallbackUrls`, `sha256`, `originalSha256`, `size`, `mimeType`, `dimensions`, `blurhash`, `nip94`, and `error`; `error` is permitted even when `ok: true` and `status: "complete"`. `UploadStatus` adds `updatedAt`, `bytesSent`, and `bytesTotal`. There is no partial state or per-server field, so encode settlement as at most eight required ordinal tokens followed by one local token: `required[N]=accepted|network-error|timeout|rejected|descriptor-mismatch|cancelled` and `local=accepted|unavailable|network-error|timeout|rejected|descriptor-mismatch|cancelled|not-attempted`. Join tokens with semicolons, preserve configured ordinal order, and cap the complete error string at 512 ASCII characters. Accepted required URLs occupy `url` then ordered `fallbackUrls`; an accepted optional-local URL is appended last to `fallbackUrls`.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Upload one Blob to one required server with backend authorization</name>
  <files>deno.json, deno.lock, runtime/blossom_transfer.ts, runtime/accounts.ts, tests/blossom_transfer_test.ts</files>
  <behavior>- Input is sniffed, sized, and SHA-256 hashed before network work.
- Authorization is a short-lived kind-24242 event signed by the active backend account and scoped to server and hash.
- A returned descriptor is accepted only when hash, size, and URL origin/path agree with the request and server.</behavior>
  <action>Add exact `npm:blossom-client-sdk@5.0.0` to `deno.json` and `deno.lock`, then implement a thin `BlossomTransferAdapter` over its upload, auth, authorization-header, and BUD-10 parsing primitives plus `PortalAccounts.signEvent`. The package audit remains SUS because of low downloads and absent npm repository metadata, but the user explicitly authorizes this exact pin non-interactively based on reviewed npm tarball/source/API evidence and no install scripts; do not float the version or add transitive direct imports. Keep SDK objects inside the adapter, inject SDK/fetch behavior for deterministic tests, validate every SDK result against portal policy, pass AbortSignal throughout, and never log raw events, auth headers, destinations, or exception text.</action>
  <verify><automated>deno test -A tests/blossom_transfer_test.ts tests/accounts_test.ts</automated></verify>
  <done>One required Blossom server accepts a verified upload authorized only by the active process-owned signer.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Settle all required remotes before optional local copying</name>
  <files>runtime/blossom_transfer.ts, tests/blossom_transfer_test.ts</files>
  <behavior>- Every configured non-loopback server is required, matching existing all-required OUTBOX semantics.
- Complete means every required descriptor validates; any required rejection is partial/failed even if another server accepts.
- The exact discovered loopback cache is attempted only after complete required success; cache absent/failure is an independent optional outcome.
- Canonical results use primary url plus ordered accepted fallbackUrls and deterministic bounded settlement tokens in error; no extension field is added.</behavior>
  <action>Execute at most eight configured required servers with bounded concurrency and one shared cancellation/deadline signal while recording each sanitized outcome, including failures omitted by SDK success maps. Choose the first accepted required remote as `url`, subsequent accepted required remotes as ordered `fallbackUrls`, and emit `ok: true`/`complete` only after all required servers accept matching descriptors. Then attempt the exact optional local endpoint; if accepted, append its canonical URL last to `fallbackUrls`. Serialize every required ordinal token and the local token into the canonical optional `error` field for every terminal settlement, so complete plus local accepted, complete plus local failed, partial required, and full required failed are all explicit; `local=not-attempted` follows required failure. Never include hostnames, paths, response text, or exception text, never add a field, preserve ordinal order, and enforce the 512-ASCII-character ceiling. Keep richer structured records only for backend diagnostics, not as a substitute for canonical exposure. Add exact pinned-codec round-trip tests for those four matrices; each test asserts `ok`, `status`, URL ordering, exact tokens, length bound, and absence of extension keys. Phase 9 may audit parity but Phase 5 delivers the canonical exposure. Retain terminal status for 10 minutes or the latest 64 uploads per owner, whichever is smaller.</action>
  <verify><automated>deno test -A tests/blossom_transfer_test.ts</automated></verify>
  <done>UPL-02 and UPL-03 expose every required and optional-local outcome through exact canonical URLs and bounded error tokens for all four settlement matrices without changing the closed contract.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| upload bytes -> backend signer/network | Untrusted bytes cause signed authorization and multi-server writes. |
| Blossom response -> upload status | Untrusted descriptors influence URLs and success claims returned to a napplet. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-08 | Elevation of Privilege | upload authorization | high | mitigate | Active backend account only, short expiry, exact server/hash scope, and no signer material in browser messages. |
| T-05-09 | Tampering / Spoofing | returned descriptor | high | mitigate | Validate hash, size, origin, and hash path before accepting each server outcome. |
| T-05-10 | Repudiation | multi-server settlement | medium | mitigate | Immutable internal outcome records and deterministic canonical complete/partial/failed projection. |
| T-05-11 | Denial of Service | fanout | medium | mitigate | Fixed concurrency, 30-second deadline, 5 MiB cap, cancellation, and bounded terminal retention. |
| T-05-SC | Tampering | SDK supply chain | high | mitigate | Use only exact authorized 5.0.0, record lock resolution, expose it through a thin adapter, and test the reviewed API surface; residual low-adoption/metadata risk is explicitly accepted by the user. |
</threat_model>
<verification>`deno test -A tests/blossom_transfer_test.ts tests/accounts_test.ts` and `deno task check` pass.</verification>
<success_criteria>Uploads have backend-only authorization, verified descriptors, all-required remote settlement, independent optional caching, and canonical closed results.</success_criteria>
<output>Create `.planning/phases/05-resource-and-blossom-transfer/05-03-SUMMARY.md` when done.</output>
