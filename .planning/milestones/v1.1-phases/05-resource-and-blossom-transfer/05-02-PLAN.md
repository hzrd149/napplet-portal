---
phase: 05-resource-and-blossom-transfer
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified: [runtime/resource_policy.ts, runtime/resource_service.ts, runtime/blossom_cache.ts, tests/resource_policy_test.ts, tests/resource_service_test.ts]
autonomous: true
requirements: [RES-02, RES-03]
must_haves:
  truths:
    - "Every initial destination and redirect hop is re-parsed, re-resolved, and rejected if any address is private, link-local, ambiguous, or otherwise forbidden."
    - "HTTP(S), Blossom hash URLs, and BUD-10 reads settle independently under closed size, MIME, timeout, redirect, and integrity policy."
    - "An optional exact-loopback Blossom cache is tried first and a miss or outage falls through without weakening verification."
  artifacts: [runtime/resource_policy.ts, runtime/resource_service.ts, runtime/blossom_cache.ts, tests/resource_policy_test.ts, tests/resource_service_test.ts]
  key_links:
    - "resource URL -> every-hop DNS/IP policy -> manual redirect fetch -> incremental limiter/sniffer/hash"
    - "Blossom/BUD-10 source -> exact local cache candidate -> policy-approved upstream candidates -> hash verification"
---
<objective>Complete bounded RESOURCE resolution and local-first Blossom reads. Purpose: give napplets useful resource access without an unrestricted backend proxy or a trusted-cache bypass. Output: shared destination policy, resource service, refactored bounded cache reads, and adversarial tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/05-resource-and-blossom-transfer/05-CONTEXT.md
@.planning/phases/05-resource-and-blossom-transfer/05-RESEARCH.md
@.planning/phases/05-resource-and-blossom-transfer/05-01-SUMMARY.md
@runtime/blossom_cache.ts
@runtime/artifacts.ts
@runtime/settings.ts
<interfaces>Consume the immutable policy constants and binary result seam from Plan 01. Preserve `BlossomCache.fetch(hash, servers, authorPubkey?)` for artifact callers while routing it through the new bounded reader. `RuntimeSettings.settings.blossomServers` is the sole configured upstream source; `LOCAL_BLOSSOM_URL` is the sole loopback exception.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Reject an SSRF redirect before any forbidden-hop body is read</name>
  <files>runtime/resource_policy.ts, runtime/resource_service.ts, tests/resource_policy_test.ts, tests/resource_service_test.ts</files>
  <behavior>- HTTPS public origin redirecting to loopback, RFC1918, link-local, unique-local, multicast, unspecified, documentation, or metadata destinations is blocked before the next body read.
- Credentials, fragments, ambiguous numeric/IPv6 hosts, non-80/443 public ports, unsupported schemes, overlong URLs, and any hostname with one forbidden A/AAAA answer are blocked.
- A public-to-public chain revalidates all answers per hop and stops after three redirects.</behavior>
  <action>Build `ResourceDestinationPolicy` around URL parsing plus injected `Deno.resolveDns` and address classification. Fetch with `redirect: "manual"`; resolve and classify every A and AAAA answer before each request and validate every resolved Location against the same rules. Treat the exact configured/discovered loopback cache origin as a separate equality-based destination class, never a hostname or subnet exception. Because ordinary Deno fetch cannot pin a validated address, document this residual DNS rebinding exposure in code-level threat comments and ensure all application-visible redirect/DNS failures collapse to `blocked-by-policy`.</action>
  <verify><automated>deno test -A tests/resource_policy_test.ts tests/resource_service_test.ts</automated></verify>
  <done>Adversarial DNS and redirect fixtures prove every-hop controls and the narrow loopback-cache exception.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Resolve HTTP(S), Blossom, BUD-10, and ordered batches under one bounded reader</name>
  <files>runtime/resource_service.ts, runtime/blossom_cache.ts, tests/resource_service_test.ts</files>
  <behavior>- Actual streamed bytes, not Content-Length, stop at 5 MiB; timeout and cancellation cancel the reader.
- Observed signature bytes determine allowed MIME; conflicting caller/header hints cannot permit HTML, SVG, or script.
- Blossom/BUD-10 bytes match the declared SHA-256 before release; cache miss/failure proceeds upstream, and corrupt cache bytes are rejected or safely retried upstream.
- `bytesMany` preserves input order and length, retains successful siblings, and rejects envelopes over eight URLs.</behavior>
  <action>Implement one incremental reader that checks advisory headers early, reads chunks with the combined AbortSignal, cancels immediately above the limit, retains a bounded sniff prefix, hashes while consuming, and constructs a Blob only after policy succeeds. Add narrow first-party magic-byte/text validation for the approved passive MIME set instead of introducing an unreviewed MIME package. Parse only strict HTTPS URLs plus Blossom hash/BUD-10 forms evidenced by the pinned contracts; reuse the existing BUD-10 xs/as ordering behavior without accepting caller-supplied loopback exceptions. Refactor `BlossomCache` away from `arrayBuffer()` and implicit redirects while preserving optional health reporting and artifact signature/aggregate/hash verification downstream.</action>
  <verify><automated>deno test -A tests/resource_service_test.ts tests/blossom_cache_test.ts tests/artifact_resolver_test.ts</automated></verify>
  <done>RES-02 and RES-03 pass deterministic success, partial, timeout, oversize, MIME, redirect, cache miss, corrupt cache, BUD-10, and integrity cases.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| resource identifier -> outbound network | Attacker-controlled URLs select process network destinations. |
| remote/cache response -> canonical Blob | Untrusted metadata and bytes cross into a napplet-visible result. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04 | Information Disclosure / Elevation of Privilege | SSRF redirect and DNS resolution | high | mitigate | Manual redirects, all-answer classification at every hop, exact cache-origin exception, and adversarial fixtures. |
| T-05-05 | Tampering | MIME/hash validation | high | mitigate | Byte sniffing plus incremental SHA-256 before Blob release; caller/header hints are non-authoritative. |
| T-05-06 | Denial of Service | response stream | high | mitigate | Content-Length precheck, actual-byte ceiling, deadline, redirect cap, URL cap, batch cap, and reader cancellation. |
| T-05-07 | Spoofing | local cache | medium | mitigate | Exact configured loopback origin and unchanged artifact/content integrity checks; cache is transport only. |
</threat_model>
<verification>`deno test -A tests/resource_policy_test.ts tests/resource_service_test.ts tests/blossom_cache_test.ts tests/artifact_resolver_test.ts` and `deno task check` pass.</verification>
<success_criteria>All resource forms resolve only through every-hop policy, bounded byte handling, passive MIME validation, and required integrity checks.</success_criteria>
<output>Create `.planning/phases/05-resource-and-blossom-transfer/05-02-SUMMARY.md` when done.</output>

