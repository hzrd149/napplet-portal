---
phase: 04-installed-napplet-discovery
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified: [main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, runtime/artifacts.ts, tests/catalog_runtime_test.ts]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "Production preview resolves any valid kind-35129 coordinate through policy-selected hints plus configured reads, not the startup fixture (D-01, D-02)."
    - "Exact manifest identity and artifact integrity are verified before review or launch material is exposed (D-03, D-11 through D-13)."
    - "Account, settings, relay event, EOSE, error, and reconnect signals update one process-owned CatalogService without erasing last-good state (D-05 through D-07)."
  artifacts: [main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, tests/catalog_runtime_test.ts]
  key_links:
    - "main.ts account/settings streams -> RelayPolicy/EventRuntime production sources -> CatalogService load/status/account-change signals"
    - "coordinate plus exact event ID -> EventRuntime relay load -> PortalArtifactResolver integrity boundary"
    - "NIP-78 relay subscription lifecycle -> CatalogService raw truth/status projection"
---
<objective>Construct the production arbitrary-coordinate catalog runtime from the Plan 01 domain seams, implementing D-01 through D-07 and D-11 through D-13. Purpose: replace fixture-only catalog resolution and connect real account, settings, relay, and artifact lifecycles without broadening catalog-domain ownership. Output: narrowly composed production services and a non-fixture integration test.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-installed-napplet-discovery/04-01-SUMMARY.md
@.planning/phases/04-installed-napplet-discovery/04-CONTEXT.md
@.planning/phases/04-installed-napplet-discovery/04-RESEARCH.md
@main.ts
@runtime/event_runtime.ts
@runtime/portal_runtime.ts
@runtime/artifacts.ts
<interfaces>Consume Plan 01 `CatalogService`, `RelayPolicy.previewReads`, generation/status methods, and exact install/launch selectors. Preserve one process-owned EventStore/RelayPool, reactive runtime settings, active signer identity, and the existing `PortalArtifactResolver` verification boundary.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Preview and launch one non-fixture napplet through production composition</name>
  <read_first>main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, runtime/artifacts.ts, runtime/catalog.ts, runtime/relay_policy.ts, tests/end_to_end_test.ts, .planning/phases/04-installed-napplet-discovery/04-RESEARCH.md</read_first>
  <files>main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, runtime/artifacts.ts, tests/catalog_runtime_test.ts</files>
  <behavior>- Production preview resolves an arbitrary valid kind-35129 coordinate from Plan 01 policy-selected hint/configured relays rather than the configured fixture.
- Exact manifest event ID is loaded and verified against coordinate publisher/identifier before artifact resolution.
- A non-fixture production-path test previews, approves, and launches a second coordinate through injected relay/Blossom sources.
- Wrong manifest ID, publisher, identifier, signature, aggregate, or blob fails closed without executable bytes.</behavior>
  <action>Replace the fixture comparison in production catalog resolution with a resolver accepting coordinate, exact manifest event ID, and Plan 01 selected relays. Load the exact event through `EventRuntime`, require kind/pubkey/d-tag and ID to match the normalized coordinate/selectors, then pass declared Blossom data through `PortalArtifactResolver` before returning review or executable output. In `main.ts`, construct the production resolver and inject current configured reads plus `RelayPolicy.previewReads` into `CatalogService`; arbitrary naddr hints are request-scoped and never mutate settings. Add `tests/catalog_runtime_test.ts` with injected relay and fetch sources proving a non-fixture coordinate completes preview, approval, and launch while each identity/integrity mismatch fails closed per D-01 through D-03 and D-11 through D-13.</action>
  <verify><automated>deno test -A tests/catalog_runtime_test.ts tests/artifact_resolver_test.ts && deno task check</automated></verify>
  <acceptance_criteria>The production path has no fixture-coordinate gate; tests prove policy-selected relay use, arbitrary coordinate and exact-event loading, manifest identity checks, Blossom integrity verification, complete non-fixture preview-to-launch behavior, and no executable output on mismatch.</acceptance_criteria>
  <done>Production can resolve and launch an arbitrary approved napplet through the same verified catalog authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Drive catalog synchronization from account, settings, and relay lifecycle</name>
  <read_first>main.ts, runtime/event_runtime.ts, runtime/catalog.ts, runtime/relay_policy.ts, tests/catalog_runtime_test.ts, .planning/phases/04-installed-napplet-discovery/04-CONTEXT.md</read_first>
  <files>main.ts, runtime/event_runtime.ts, tests/catalog_runtime_test.ts</files>
  <behavior>- Active-account change cancels old NIP-78 work, resets old authority, begins policy-approved loading for the new account, and cannot accept late old events.
- Settings relay changes replace the subscription through current permitted reads without restart.
- Relay next/EOSE/error and reconnect produce explicit loading/ready/stale/error signals while last-good data remains available.
- Teardown owns exactly one active catalog subscription and releases it deterministically.</behavior>
  <action>In `main.ts`, compose active identity and reactive settings into one serialized/generation-guarded catalog synchronization owner. Before each account or relay-set replacement, unsubscribe old work and advance generation; subscribe the current account's NIP-78 filter through the bounded policy-approved configured read set. Extend `EventRuntime` only as needed to expose observable next/EOSE/error/reconnect lifecycle without duplicating EventStore truth, and map those signals to Plan 01 CatalogService loading/ready/stale/error/account-reset methods. Preserve last-good data on transient errors, clear authority only for an actual account change, and prove late prior-generation events cannot load per D-05 through D-07.</action>
  <verify><automated>deno test -A tests/catalog_runtime_test.ts tests/catalog_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Tests prove account and settings replacement, policy-approved relay selection, event/EOSE/error/reconnect status mapping, last-good preservation, generation rejection, and exactly-one-subscription teardown.</acceptance_criteria>
  <done>One production owner continuously synchronizes the active account's catalog without hiding useful partial state.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| relay/Blossom network -> production resolver | Untrusted events and bytes can become review facts or executable content. |
| account/settings streams -> catalog synchronization owner | Changing local authority and endpoints replaces active network work. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-14 | Spoofing / Tampering | exact manifest production resolver | high | mitigate | Verify event ID, signature, kind, publisher, identifier, aggregate, and blobs before output; cover each mismatch in non-fixture tests. |
| T-04-15 | Server-Side Request Forgery | preview relay construction | high | mitigate | Consume only Plan 01 bounded policy-selected relays; keep hints request-scoped and test configured fallback. |
| T-04-16 | Tampering | account/settings subscription replacement | high | mitigate | Unsubscribe-before-replace plus generation checks prevent old-account or old-relay events from loading. |
| T-04-17 | Denial of Service | catalog synchronization lifecycle | medium | mitigate | Own one active subscription, bound relay count, and deterministically tear down on replacement/process cleanup. |
</threat_model>
## Artifacts this plan produces
- Arbitrary-coordinate production catalog resolver and composition root wiring
- Account/settings/relay lifecycle synchronization owner
- Non-fixture production-path integration tests
<verification>`deno test -A tests/catalog_runtime_test.ts tests/catalog_test.ts tests/artifact_resolver_test.ts && deno task check` passes.</verification>
<success_criteria>CAT-01 through CAT-03 operate through real production construction, and all high-severity resolver and lifecycle threats fail closed under automated tests.</success_criteria>
<output>Create `.planning/phases/04-installed-napplet-discovery/04-02-SUMMARY.md` when done.</output>
