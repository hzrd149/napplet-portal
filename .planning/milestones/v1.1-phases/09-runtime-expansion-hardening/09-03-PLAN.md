---
phase: 09-runtime-expansion-hardening
plan: "03"
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - runtime/artifacts.ts
  - runtime/catalog.ts
  - runtime/signer_service.ts
  - runtime/nap_contract_registry.ts
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - tests/adversarial_authority_test.ts
autonomous: true
requirements: [QLT-02]
must_haves:
  truths:
    - "Capabilities, catalog launches, identity, and signing use exact server-derived account, manifest, instance, and generation authority."
    - "Foreign, stale, malformed, or ungranted requests produce zero effects and sanitized errors."
    - "Every mandatory capability/catalog/signer authority row is green before state isolation work begins."
  artifacts:
    - path: tests/adversarial_authority_test.ts
      provides: "Focused capability, catalog, identity, and signer authority evidence"
  key_links:
    - from: runtime/nap_dispatcher.ts
      to: runtime/portal_runtime.ts
      via: "server-derived exact window capability context"
---

<objective>
Close capability, exact catalog, identity, and signer adversarial rows per D-05 through D-08.

Purpose: Prove a napplet cannot invent launch, account, identity, generation, or signer authority.
Output: Focused hostile-input suite and bounded remediation in seven declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@runtime/nap_contract_registry.ts
@runtime/nap_dispatcher.ts
@runtime/portal_runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace forged capability and identity to zero signer effect</name>
  <files>runtime/signer_service.ts, runtime/nap_contract_registry.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_authority_test.ts</files>
  <behavior>
    - A declared action without the exact verified grant is rejected before signer invocation.
    - Foreign account, identity, instance, and stale generation variants remain effect-free and sanitized.
  </behavior>
  <action>Trace one canonical request from decoder through registry grant, current window authority, dispatcher, and signer owner. Derive authority out of band, re-check generation immediately before asynchronous effects, and assert no signer call for every hostile mutation per D-05 through D-08.</action>
  <verify><automated>deno test -A tests/adversarial_authority_test.ts --filter "authority tracer"</automated></verify>
  <done>The tracer proves forged or stale client fields cannot create signer effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust capability, catalog, identity, and signer authority rows</name>
  <files>runtime/artifacts.ts, runtime/catalog.ts, runtime/signer_service.ts, runtime/nap_contract_registry.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_authority_test.ts</files>
  <behavior>
    - Only current exact accepted catalog artifacts confer launch authority; display staleness never preserves authority.
    - Signer operations cannot use nsec/client event authority or cross active accounts.
    - Malformed grants, invented actions, foreign artifacts/accounts, and stale manifests/generations deny before effects.
  </behavior>
  <action>Use closed property/table cases for capability grants, catalog authority, account/identity ownership, signer separation, and generation replacement per D-05 through D-08. Remediate every row only in the declared authority owners. Require zero mandatory red rows; report an externally owned failure with exact evidence.</action>
  <verify><automated>deno test -A tests/adversarial_authority_test.ts tests/catalog_test.ts tests/catalog_runtime_test.ts tests/signer_service_test.ts</automated></verify>
  <done>All mandatory capability, catalog, identity, and signer rows pass with exact authority and zero denied effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| napplet claims→process authority | Client-declared identity and grants are untrusted. |
| account/artifact→launch or signing effects | Effects require exact current server-owned authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-04 | Elevation of privilege | capability/dispatcher | critical | mitigate | Server-derived exact grants and current generation recheck before effects. |
| T-09-05 | Spoofing | catalog/signer | critical | mitigate | Exact accepted artifact and active-account signer binding. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused authority and existing catalog/signer suites pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 capability, catalog, identity, and signer boundaries are fail-closed under hostile inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-03-SUMMARY.md` when done.
</output>
