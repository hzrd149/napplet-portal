---
phase: 09-runtime-expansion-hardening
plan: "06"
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - routes/api/runtime.ts
  - islands/NappletShell.tsx
  - runtime/connections.ts
  - runtime/intent.ts
  - runtime/media_reducer.ts
  - runtime/media_sessions.ts
  - runtime/portal_runtime.ts
  - runtime/transport.ts
  - tests/adversarial_browser_lifecycle_test.ts
autonomous: true
requirements: [QLT-02, QLT-03]
must_haves:
  truths:
    - "Intent tickets and media ownership remain exact, generation-bound, and inert after expiry, replacement, or closure."
    - "Reconnect, late completion, duplicate correlation, and shutdown cannot revive stale browser authority."
    - "Every mandatory browser lifecycle row is green before the exhaustive lifecycle and browser acceptance waves begin."
  artifacts:
    - path: tests/adversarial_browser_lifecycle_test.ts
      provides: "Focused intent, media, reconnect, replacement, and shutdown evidence"
  key_links:
    - from: islands/NappletShell.tsx
      to: routes/api/runtime.ts
      via: "generation-bound intent/media commands over authenticated transport"
---

<objective>
Close intent, media, reconnect, replacement, and shutdown adversarial rows per D-05 through D-08.

Purpose: Prevent stale tabs, late work, and forged lifecycle input from bypassing process-owned authority.
Output: Focused browser-lifecycle tests and remediation in nine declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-05-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@routes/api/runtime.ts
@runtime/intent.ts
@runtime/media_sessions.ts
@runtime/connections.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a stale intent completion through replacement and denial</name>
  <files>routes/api/runtime.ts, islands/NappletShell.tsx, runtime/intent.ts, runtime/portal_runtime.ts, runtime/transport.ts, tests/adversarial_browser_lifecycle_test.ts</files>
  <behavior>
    - Intent tickets are bounded, exact-target, single-use, and generation-bound.
    - Popup closure, expiry, account change, and replacement make late completion inert.
    - Denials expose stable sanitized errors and leave no live correlation.
  </behavior>
  <action>Trace one intent ticket from shell request through authenticated transport, process-owned validation, replacement, and late completion. Recheck current account, identity, window, and generation immediately before effects; terminalize the correlation exactly once and assert denial plus cleanup per D-05 through D-08.</action>
  <verify><automated>deno test -A tests/adversarial_browser_lifecycle_test.ts --filter "intent lifecycle tracer" tests/intent_runtime_test.ts tests/intent_navigation_test.tsx</automated></verify>
  <done>The tracer proves stale or replayed intent work cannot navigate, open, deliver, or retain authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust media, reconnect, replacement, and shutdown rows</name>
  <files>routes/api/runtime.ts, islands/NappletShell.tsx, runtime/connections.ts, runtime/media_reducer.ts, runtime/media_sessions.ts, runtime/portal_runtime.ts, runtime/transport.ts, tests/adversarial_browser_lifecycle_test.ts</files>
  <behavior>
    - Media revoke or stop precedes grant; stale owners, tabs, results, and generations cannot mutate or resume playback.
    - Duplicate correlations, grace expiry, account change, replacement, late completion, and shutdown terminalize deterministically.
    - Stream-oriented partial truth remains observable without waiting for global completion.
  </behavior>
  <action>Expand the closed hostile table across media ownership, reconnect grace, duplicate correlations, replacement, late completion, and shutdown. Assert process authority and ordered effects before client presentation, then remediate all rows only in declared owners per D-05 through D-08. Completion requires zero mandatory red rows; an externally owned failure is an execution blocker with exact evidence.</action>
  <verify><automated>deno test -A tests/adversarial_browser_lifecycle_test.ts tests/media_lifecycle_test.ts tests/media_transport_test.ts tests/connection_controller_test.ts tests/runtime_transport_test.ts</automated></verify>
  <done>All mandatory intent, media, reconnect, replacement, and shutdown rows pass with no stale authority or sensitive output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tab/shell→process runtime | Browser lifecycle observations and commands are untrusted and reorderable. |
| stale async completion→current owner | Late work must not cross account, identity, window, or generation transitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-08 | Elevation of privilege | intent/media ownership | high | mitigate | Single-use tickets and revoke-before-grant generation reducers. |
| T-09-09 | Tampering | reconnect/replacement | high | mitigate | Current-generation rechecks and deterministic terminal cleanup. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused intent/media/reconnect adversarial suites pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 and relevant QLT-03 browser lifecycle boundaries are fail-closed under hostile and stale inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-06-SUMMARY.md` when done.
</output>
