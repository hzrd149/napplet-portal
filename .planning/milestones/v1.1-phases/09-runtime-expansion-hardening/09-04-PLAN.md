---
phase: 09-runtime-expansion-hardening
plan: "04"
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - runtime/common.ts
  - runtime/storage.ts
  - runtime/storage_store.ts
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - tests/adversarial_state_isolation_test.ts
autonomous: true
requirements: [QLT-02, QLT-03]
must_haves:
  truths:
    - "COMMON projections and STORAGE mutations remain exact-account, exact-instance, exact-scope, and generation isolated."
    - "Foreign, stale, malformed, over-quota, concurrent, or failed-persistence requests produce no cross-namespace effect or disclosure."
    - "Stream-oriented COMMON partial truth remains observable without duplicating state or waiting for completeness."
  artifacts:
    - path: tests/adversarial_state_isolation_test.ts
      provides: "Focused COMMON stream and STORAGE namespace isolation evidence"
  key_links:
    - from: runtime/nap_dispatcher.ts
      to: runtime/common.ts
      via: "server-owned account and generation context"
    - from: runtime/nap_dispatcher.ts
      to: runtime/storage.ts
      via: "server-owned account, instance, and scope namespace"
---

<objective>
Close COMMON and STORAGE state-isolation adversarial rows per D-05 through D-08 while preserving D-12 stream semantics.

Purpose: Prove napplets cannot cross account, instance, scope, quota, persistence, or reactive-state boundaries.
Output: Focused isolation suite and bounded remediation in six declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-03-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@runtime/common.ts
@runtime/storage.ts
@runtime/storage_store.ts
@runtime/nap_dispatcher.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a foreign namespace request to zero state effect</name>
  <files>runtime/common.ts, runtime/storage.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_state_isolation_test.ts</files>
  <behavior>
    - A storage request bearing foreign account, instance, scope, or stale generation is rejected before reads or mutations.
    - A COMMON subscription derives current partial truth from the server-owned stream and retires on generation replacement.
  </behavior>
  <action>Trace one canonical STORAGE request and one COMMON subscription through decoder, current authority, service owner, projection, and terminal cleanup. Derive namespace and stream ownership out of band; assert sanitized zero-effect denial and immediate partial truth per D-05 through D-08 and D-12.</action>
  <verify><automated>deno test -A tests/adversarial_state_isolation_test.ts --filter "state isolation tracer"</automated></verify>
  <done>The tracer proves foreign namespace input is inert and COMMON preserves generation-bound partial truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust COMMON lifecycle and STORAGE isolation rows</name>
  <files>runtime/common.ts, runtime/storage.ts, runtime/storage_store.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_state_isolation_test.ts</files>
  <behavior>
    - Shared and instance storage are account-isolated, scope-isolated, quota-bounded, deterministic, and unchanged after rejected or failed mutations.
    - Empty, partial, stale, update, replacement, and shutdown COMMON states preserve stream ordering and retire stale subscribers.
    - Concurrent mutations and persistence failures cannot expose or corrupt a neighboring namespace.
  </behavior>
  <action>Expand closed tables across invalid scopes, foreign namespaces, quota boundaries, concurrent mutations, persistence failures, partial COMMON updates, stale generation emissions, replacement, and shutdown. Remediate only the declared state owners, derive portal state from existing reactive sources, and avoid nested subscriptions or wait-for-complete flows per D-05 through D-08 and D-12.</action>
  <verify><automated>deno test -A tests/adversarial_state_isolation_test.ts tests/storage_test.ts tests/common_test.ts</automated></verify>
  <done>All mandatory COMMON and STORAGE rows pass with exact isolation, deterministic cleanup, and stream-first partial truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| napplet namespace claims→state services | Account, instance, scope, and generation fields are untrusted. |
| reactive source→napplet projection | Stale or cross-account emissions must not cross the current owner boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-06 | Information disclosure | storage/common | high | mitigate | Exact namespace derivation, current-generation filtering, and sanitized projections. |
| T-09-07 | Tampering | storage persistence | high | mitigate | Quota checks, serialized mutations, atomic failure behavior, and namespace tests. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused isolation and existing COMMON/STORAGE suites pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 and QLT-03 COMMON/STORAGE boundaries remain isolated and stream-correct under hostile and stale inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-04-SUMMARY.md` when done.
</output>
