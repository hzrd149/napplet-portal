---
phase: 09-runtime-expansion-hardening
plan: "02"
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - runtime/resource_policy.ts
  - runtime/resource_service.ts
  - runtime/binary_transport.ts
  - runtime/transport.ts
  - routes/api/runtime.ts
  - tests/adversarial_transport_transfer_test.ts
autonomous: true
requirements: [QLT-02, QLT-03]
must_haves:
  truths:
    - "Untrusted URLs, redirects, transfer metadata, correlations, and late binary frames fail closed before network or delivery effects."
    - "Errors expose no private destination, local path, token, secret, or cross-owner data."
    - "Every mandatory transport, URL, and transfer adversarial row is green before downstream work begins."
  artifacts:
    - path: tests/adversarial_transport_transfer_test.ts
      provides: "Focused hostile URL, redirect, RESOURCE/UPLOAD, correlation, and binary-transfer evidence"
  key_links:
    - from: routes/api/runtime.ts
      to: runtime/resource_policy.ts
      via: "authenticated request context before policy and service effects"
---

<objective>
Close transport, URL-policy, and RESOURCE/UPLOAD adversarial rows per D-05 through D-08.

Purpose: Prevent hostile napplet traffic from reaching private destinations, crossing transfer ownership, or leaking sensitive failures.
Output: Focused boundary tests and complete remediation in six declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@runtime/resource_policy.ts
@runtime/resource_service.ts
@runtime/binary_transport.ts
@runtime/transport.ts
@routes/api/runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one hostile resource request to a sanitized zero-effect denial</name>
  <files>runtime/resource_policy.ts, runtime/resource_service.ts, runtime/binary_transport.ts, routes/api/runtime.ts, tests/adversarial_transport_transfer_test.ts</files>
  <behavior>
    - A private, ambiguous, or disallowed URL is rejected before fetch/upload and returns a stable sanitized error.
    - The same request with foreign ownership or a stale generation cannot create or complete a transfer.
  </behavior>
  <action>Build the first deny-by-default table row through strict decoding, authenticated outer context, URL policy, redirect revalidation, transfer ownership, and response projection. Assert effects first and message shape second. Bind every decision to server-owned account, verified identity, correlation, and generation per D-05/D-06; sanitize errors and logs per D-07/D-08.</action>
  <verify><automated>deno test -A tests/adversarial_transport_transfer_test.ts --filter "resource boundary tracer"</automated></verify>
  <done>The hostile tracer cannot fetch, upload, settle, or disclose sensitive destination/error data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust URL, redirect, binary, and transfer adversarial rows</name>
  <files>runtime/resource_policy.ts, runtime/resource_service.ts, runtime/binary_transport.ts, runtime/transport.ts, routes/api/runtime.ts, tests/adversarial_transport_transfer_test.ts</files>
  <behavior>
    - Missing, extra, wrong-type, oversized, duplicate, cancelled, expired, late, and post-shutdown transfer inputs fail closed.
    - URL normalization and every redirect hop reject private/local destinations, invalid schemes, credentials, ambiguity, and policy drift.
    - Mixed server settlement preserves ordered per-server results without leaking internal errors.
  </behavior>
  <action>Expand the closed table and remediate every failing row in only the declared transport/transfer owners. Reuse the Phase 5 policy and bounded registry patterns; do not weaken validation or wait for all stream data. The task completes only when the mandatory table contains zero red rows. A failure owned elsewhere is an execution blocker to report with exact evidence, not a completion condition.</action>
  <verify><automated>deno test -A tests/adversarial_transport_transfer_test.ts tests/resource_policy_test.ts tests/resource_service_test.ts tests/binary_transport_test.ts</automated></verify>
  <done>All mandatory URL, redirect, transport, and transfer rows pass with zero effect on denial and no sensitive output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| napplet→runtime transport | Untrusted envelopes and binary frames request backend effects. |
| runtime→network | URL and redirect decisions can reach private or local services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Server-side request forgery | resource policy | critical | mitigate | Normalize and validate initial URL and every redirect before I/O. |
| T-09-02 | Tampering | transfer correlations | high | mitigate | Exact owner/generation binding, bounded registries, and terminal cleanup. |
| T-09-03 | Information disclosure | projected errors | high | mitigate | Stable public error codes and explicit secret/path/private-address assertions. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused adversarial and existing resource/transport suites pass with no mandatory red rows.
</verification>

<success_criteria>
QLT-02 and relevant QLT-03 transport boundaries are fail-closed under hostile inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-02-SUMMARY.md` when done.
</output>
