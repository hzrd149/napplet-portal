---
phase: 07-intent-navigation
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - components/NappletFrame.tsx
  - islands/NappletShell.tsx
  - tests/intent_navigation_test.tsx
  - tests/shell_architecture_test.ts
autonomous: true
requirements: [INT-02, INT-03]
must_haves:
  truths:
    - "INT-03: Existing exact handler surfaces focus/reuse only within the same account and verified identity; otherwise policy selects an allowed new tab or stack."
    - "New-tab reservation synchronously opens a same-origin inert page from the trusted iframe message event, retains its WindowProxy until commit/close, and the target page severs opener before ticket consumption."
    - "Every stacked frame retains sandbox=allow-scripts, exact source ownership, and shell back/close control without remounting unrelated frames."
    - "INT-02: blocked/closed popups and navigation failures acknowledge failed exactly once."
  artifacts:
    - path: components/NappletFrame.tsx
      provides: "Reusable per-surface source-bound iframe bridge with exact sandbox parity"
    - path: islands/NappletShell.tsx
      provides: "Surface registry, synchronous popup reservation, reuse/stack execution, and history ownership"
    - path: tests/intent_navigation_test.tsx
      provides: "Deterministic adapter tests for reuse, popup, stack, history, sandbox, and source binding"
  key_links:
    - from: islands/NappletShell.tsx
      to: components/NappletFrame.tsx
      via: "one bridge/ref/verified identity/runtime window owner per surface"
    - from: islands/NappletShell.tsx
      to: runtime intent command seam
      via: "reservationId carried by validated runtime.forward, authorized ticket command, and exact terminal commit/close acknowledgement"
    - from: browser history
      to: shell stack registry
      via: "opaque same-document surface IDs only"
---

<objective>
Implement shell-owned reuse, popup, and stacked-frame navigation with sandbox and history preservation.

Purpose: INT-03 requires browser operations to remain in the island while backend policy and artifact authority remain server-owned.
Output: Multi-surface frame bridge, compact stack controls, synchronous popup reservation, and deterministic navigation tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-intent-navigation/07-02-SUMMARY.md
@.planning/phases/07-intent-navigation/07-CONTEXT.md
@.planning/phases/07-intent-navigation/07-RESEARCH.md
@components/NappletFrame.tsx
@islands/NappletShell.tsx
@tests/iframe_bridge_test.ts
@tests/shell_architecture_test.ts
@tests/catalog_ui_test.tsx

<interfaces>
Consume the exact internal reserve/authorized/ack/claim codecs from 07-02; public iframe messages remain pinned NAP-INTENT envelopes. New-tab uses a generated bounded `_napplet_intent_<opaque>` name and same-origin `/intent/reserved#<reservationId>` inert route. It deliberately does not request opener isolation because the shell must retain the WindowProxy for commit/close; `/intent/reserved` executes `window.opener = null` before runtime connection or server-held ticket consumption.
Current `NappletFrame` uses exact `sandbox="allow-scripts"`; current shell preserves mounted Home/Profile/Settings/Napplet views and already owns browser history/popstate.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one verified stack launch through a per-surface bridge and browser history</name>
  <files>components/NappletFrame.tsx, islands/NappletShell.tsx, tests/intent_navigation_test.tsx</files>
  <behavior>
    - One backend-authorized stack command creates one distinct frame surface with exact sandbox, bridge, source, owner, and verified identity.
    - pushState stores only opaque shell surface IDs; Back hides/removes the top surface and reveals the prior mounted frame without rewriting unrelated srcdoc.
    - Compact Back and Close controls are keyboard accessible, shell-owned, and settle surface closure once.
  </behavior>
  <action>Refactor `NappletFrame` from the single-global-frame assumption into a reusable per-surface component whose bridge callback, iframe ref, verified identity, and runtime window owner remain source-bound. Add a pure/injectable shell surface-stack controller and wire one production stack path into `NappletShell`. Preserve the exact `sandbox="allow-scripts"` attribute for every frame, allocate unique opaque surface IDs, keep unrelated active frames mounted, use same-document history state rather than route reload, and add compact accessible Back/Close controls. Route each message by exact `event.source`; hidden or older frames must never impersonate the active surface.</action>
  <verify>
    <automated>deno test -A tests/intent_navigation_test.tsx --filter "stack"</automated>
  </verify>
  <done>A verified handler stacks in a separately authenticated sandboxed frame; Back/Close restore the prior mounted surface and correlated lifecycle acknowledgement is exact-once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add synchronous new-tab reservation and identity-safe reuse</name>
  <files>islands/NappletShell.tsx, tests/intent_navigation_test.tsx, tests/shell_architecture_test.ts</files>
  <behavior>
    - New-tab mode validates exact event.source, synchronously creates reservationId, calls window.open on `/intent/reserved#<reservationId>` with a generated bounded name and no opener-isolation feature, then forwards the reservation through runtime.forward; null immediately produces blocked.
    - Backend authorization supplies the single-use ticket/launch path for that reservationId; shell commits only the matching still-open handle or closes it on denial, verification failure, stale generation, timeout, or closure.
    - The reservation page is same-origin and inert, validates its fixed route and reservationId shape, severs opener before any runtime connection/ticket claim, and cannot accept caller content or destinations.
    - Reuse/focus selects only an open surface with matching account and exact verified handler identity; cross-account/version matches are forbidden.
    - Reusing a surface does not add a duplicate frame or corrupt stack history.
  </behavior>
  <action>Handle reservation synchronously inside the validated iframe `message` event: require the registered WindowProxy and the expected opaque sandbox origin, generate reservationId and a bounded shell-owned name, open only the fixed same-origin inert reservation route, retain the returned WindowProxy, and immediately send reserve through the existing validated `runtime.forward` path. Do not request opener isolation at open time because that conflicts with later opener-driven navigation/close. On same-origin runtime authorization, require matching reservationId, owner, source, and generation before navigating the still-open reserved handle to the backend-issued same-origin launch path; otherwise close it. The target page sets `window.opener = null` before transport or claim. Model `reserved -> authorized -> committed` and `reserved|authorized -> blocked|closed|failed` as exact-once terminal transitions, with timeout/unload/account/generation cleanup and replay rejection. Implement identity-safe focus/reuse and test null popup, manually closed handle, wrong origin/source/reservation/generation, duplicate ack, cleanup, fixed route/name constraints, opener-sever ordering, CSP compatibility, sandbox parity, and existing history behavior.</action>
  <verify>
    <automated>deno test -A tests/intent_navigation_test.tsx tests/shell_architecture_test.ts tests/iframe_bridge_test.ts</automated>
  </verify>
  <done>Popup reservation is activation-safe and constrained, reuse is identity-safe, failures clean up, and all modes preserve existing mounted shell/runtime state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe message → shell controller | Untrusted frame source requests browser-affecting navigation. |
| shell → browser popup/history | Authorized commands invoke ambient browser capabilities. |
| surface registry → frame bridge | Multiple opaque-origin frames must retain distinct source and identity ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Spoofing | multi-frame bridge | high | mitigate | Per-surface exact event.source/ref/owner/verified-identity registration and tests. |
| T-07-09 | Tampering | new-tab reservation | high | mitigate | Fixed same-origin inert route, bounded generated name/reservationId, exact source/origin/generation checks, and backend-only launch path. |
| T-07-09A | Elevation of privilege | opener lifetime | high | mitigate | Retain opener only for shell commit/close; reservation page severs opener before transport, claim, or napplet execution; CSP excludes injected inline/cross-origin content. |
| T-07-10 | Elevation of privilege | reuse registry | high | mitigate | Require same account and exact verified target identity before focus/reuse. |
| T-07-11 | Denial of service | surfaces/popups/history | medium | mitigate | Bounded lifecycle registries, exact-once cleanup, closed-handle detection, and opaque history state. |
| T-07-SC | Tampering | package supply chain | high | mitigate | No package installs. |
</threat_model>

<verification>
Run `deno test -A tests/intent_navigation_test.tsx tests/shell_architecture_test.ts tests/iframe_bridge_test.ts tests/shell_resilience_test.tsx` and `deno task check`.
</verification>

<success_criteria>
- INT-03 reuse, new-tab, and stack modes all have deterministic tested browser behavior.
- Every frame remains opaque-origin sandboxed and exact-source authenticated.
- Browser history and controls operate on shell IDs without remounting unrelated active napplets.
</success_criteria>

## Source Coverage Audit

| Source | Items covered here | Status |
|--------|--------------------|--------|
| GOAL | Shell-controlled trusted handler navigation | COVERED |
| REQ | INT-02 browser failure outcomes; INT-03 all navigation modes | COVERED |
| RESEARCH | synchronous popup reservation; per-surface bridge; stack/history model; sandbox parity; ack timing | COVERED |
| CONTEXT | all Shell navigation decisions and browser portions of Failure/lifecycle | COVERED |

<output>
Create `.planning/phases/07-intent-navigation/07-03-SUMMARY.md` when done.
</output>
