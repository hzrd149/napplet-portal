---
phase: 06-common-data-and-durable-storage
plan: "03"
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - main.ts
  - routes/api/runtime.ts
  - runtime/common.ts
  - runtime/event_runtime.ts
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - tests/common_test.ts
  - tests/common_runtime_integration_test.ts
autonomous: true
requirements: [COM-01, COM-02]
must_haves:
  truths:
    - "A napplet can invoke exactly the eight pinned COMMON operations and receives one canonical correlated result for each request."
    - "NIP-19 helpers accept only the six public canonical forms and never decode or emit nsec material."
    - "Profile and follows calls return current partial/stale truth immediately while bounded relay work updates the shared process-owned Applesauce EventStore for later calls."
    - "Follow, unfollow, react, and report use backend signer/outbox authority and return stable denial or publication failures without leaking event authority."
    - "Common-data work is generation-bound and cancelled on expiry/account/manifest replacement without destroying process-owned cached truth."
  artifacts:
    - path: runtime/common.ts
      provides: "Eight-operation exact COMMON decoder/service and sanitized projections/actions"
      exports: [CommonService]
    - path: runtime/event_runtime.ts
      provides: "Policy-aware kind-0/kind-3 loads feeding the one shared EventStore"
    - path: tests/common_test.ts
      provides: "Contract, codec, stream, mutation, generation, and teardown coverage"
    - path: tests/common_runtime_integration_test.ts
      provides: "Production composition coverage for launch authority, real signer/outbox success, denial, and required-relay failure"
  key_links:
    - from: runtime/common.ts
      to: runtime/event_runtime.ts
      via: "bounded policy-aware relay observations feed the existing EventStore"
      pattern: "eventStore"
    - from: runtime/nap_dispatcher.ts
      to: runtime/common.ts
      via: "exact eight-request switch emits one matching result envelope"
      pattern: "common\.(encodeNip19|decodeNip19|getProfile|follows|follow|unfollow|react|report)"
    - from: routes/api/runtime.ts
      to: runtime/portal_runtime.ts
      via: "successful backend catalog.launch registers its verified launch value on the existing window before COMMON dispatch"
      pattern: "catalogCommand|registerLaunchAuthority"
    - from: main.ts
      to: runtime/accounts.ts
      via: "the process CommonService/OutboxAdapter uses signerAccounts.identity and signerAccounts.signEvent"
      pattern: "signerAccounts\.(identity|signEvent)"
    - from: main.ts
      to: runtime/outbox.ts
      via: "production OutboxAdapter publishes to the RelayPolicy-filtered required relay set through processRuntime.eventRuntime.relayPool"
      pattern: "OutboxAdapter|relayPool"
---

<objective>
Complete the pinned NAP-COMMON surface with canonical codecs, live Applesauce-backed partial projections, and backend-authorized social actions.

Purpose: Let napplets use common Nostr capabilities without browser-owned relay, signer, or duplicated application state.
Output: CommonService, shared EventRuntime extensions, exact dispatcher coverage, lifecycle cleanup, and comprehensive COMMON tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-common-data-and-durable-storage/06-CONTEXT.md
@.planning/phases/06-common-data-and-durable-storage/06-RESEARCH.md
@.planning/phases/06-common-data-and-durable-storage/06-01-SUMMARY.md
@.planning/phases/06-common-data-and-durable-storage/06-02-SUMMARY.md
@runtime/event_runtime.ts
@runtime/outbox.ts
@runtime/accounts.ts
@runtime/nap_dispatcher.ts
@runtime/catalog.ts
@main.ts
@routes/api/runtime.ts

<interfaces>
The exact pinned request set is common.encodeNip19, common.decodeNip19, common.getProfile, common.follows, common.follow, common.unfollow, common.react, and common.report. Each result echoes id and uses its matching 0.31.0 result type. There is no profile update/push envelope: keep backend truth live and answer later correlated calls from newer state. EventRuntime owns the single EventStore, RelayPool, loader, and RelayPolicy-aware request seam.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete public NIP-19 codecs and live partial profile/follows projections</name>
  <files>runtime/common.ts, runtime/event_runtime.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/common_test.ts</files>
  <behavior>
    - encode/decode round-trip npub, note, nprofile, nevent, naddr, and nrelay with normalized type-specific fields; nsec, malformed TLV, ambiguous, unsupported, extraneous-key, and oversized requests return correlated errors.
    - getProfile accepts hex pubkey, npub, or nprofile and returns the current sanitized EventStore view without waiting for relay completion; empty, cached/stale, and later replacement truth are observable on successive calls.
    - follows returns current active-account kind-3 contact truth in deterministic hex-pubkey order and schedules bounded policy-aware refresh through the existing EventRuntime.
    - Relay observations feed the one process-owned EventStore; no per-frame store, pool, loader, nested subscription, mirrored profile state machine, or wait-for-all flow is created.
    - Expiry/account/manifest generation change cancels window-owned loads/correlations and drops late results while cached EventStore truth remains process-owned (D-01 through D-04, D-15).
  </behavior>
  <action>Implement exact codecs for the first four COMMON request types using the installed pinned contracts plus nostr-tools NIP-19 and Applesauce profile/contact helpers. Extend EventRuntime with policy-aware kind-0 and kind-3 Observable load seams that feed its existing EventStore. Compose ownership with RxJS subscriptions/finalize and generation guards; avoid nested subscriptions and do not await an open-ended source to completion before returning cached partial truth. Sanitize profiles and contacts to pinned public result fields. Emit exactly one result per request ID; subsequent calls observe updated truth. Do not invent a profile push message or any ninth COMMON operation. This implements D-01 through D-04 and D-15.</action>
  <verify>
    <automated>deno test -A tests/common_test.ts --filter "NIP-19|profile|follows|generation|expiry"</automated>
  </verify>
  <done>Canonical public NIP-19 helpers and partial/stale/updating profile/follows behavior use one shared Applesauce lifecycle and never expose secret forms or non-contract envelopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete follow, unfollow, react, report, and eight-operation parity</name>
  <files>runtime/common.ts, tests/common_test.ts</files>
  <behavior>
    - follow/unfollow validate documented npub targets, preserve current contact-list metadata/tags while deterministically changing p tags, sign backend-side, and report required publish settlement.
    - react validates a 64-hex event id, bounds reaction text, validates optional emoji URL, builds the canonical reaction event, signs, and publishes through existing backend authority.
    - report validates event/pubkey target, one of seven pinned NIP-56 reasons, and bounded text before backend signing/publication.
    - Missing active identity, signer denial, invalid inputs, and partial/failed required publication return ok:false with stable redacted errors and no signer/relay/internal-event leakage.
    - A table-driven parity test proves exactly eight request types and their eight matching response types; no profile push or extra COMMON request exists (D-01 through D-04).
  </behavior>
  <action>Implement the remaining four action operations against injected signing/publication ports. Follow/unfollow must re-read the active account's latest kind-3 event from the shared EventStore, preserve content and every unrelated tag, and deterministically add/remove only the requested `p` tags. Build kind-7 reactions with validated target context and kind-1984 reports with the validated NIP-56 reason; keep unsigned construction in CommonService and every signer/relay object backend-only. Apply the resolved bounded validation, recheck current authority immediately before publication, add a successfully published replacement/action event to the shared EventStore, and map signer denial/signing failure/zero required relays/any required-relay rejection to canonical stable action failures. Finish with table-driven parity over exactly encodeNip19, decodeNip19, getProfile, follows, follow, unfollow, react, and report and their matching 0.31.0 responses. Do not add a profile push envelope. This completes D-01 through D-04 and COM-01/COM-02.</action>
  <verify>
    <automated>deno test -A tests/common_test.ts tests/common_storage_runtime_test.ts</automated>
  </verify>
  <done>All and only the eight pinned COMMON operations work through current backend authority, projections remain stream-backed, actions settle explicitly, and every response is correlated and contract-shaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire launch-bound COMMON authority to the production signer and required-relay outbox</name>
  <files>main.ts, routes/api/runtime.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/common_runtime_integration_test.ts</files>
  <behavior>
    - A successful backend catalog.launch registers the exact verified account, coordinate, accepted manifest event id, dTag, aggregate hash, granted COMMON domain, and backend window instance before that window can dispatch COMMON; stale/failed launch and iframe-declared identity register nothing.
    - Production CommonService receives active identity and signing only from the existing PortalAccounts instance in main.ts, and publishes only through an OutboxAdapter backed by the process EventRuntime relay pool and RelayPolicy-filtered configured plus NIP-65 write relays.
    - An authorized launched window can execute a representative action through the real composition seam and receives one correlated success only after every non-empty required relay accepts.
    - A window without a successful exact launch or COMMON grant receives a stable correlated denial and neither signs nor publishes.
    - Signer denial/signing failure, an empty required-relay set, or any required relay rejection produces a stable correlated publication failure, records no successful mutation, and leaks no signer or relay internals.
  </behavior>
  <action>Extend the existing process composition rather than creating a second runtime. In `main.ts`, construct the production `OutboxAdapter` with `identity: () => signerAccounts.identity`, `signEvent: (template) => signerAccounts.signEvent(template)`, the existing process EventRuntime relay pool adapted to the adapter's boolean publish port, configured relays plus EventStore-derived NIP-65 outboxes, and the current RelayPolicy; construct/configure CommonService on `processRuntime` beside `configureCatalog`. In `RuntimeServiceHub`, make successful `catalog.launch` output the sole registration input for immutable per-window authority and expose one COMMON dispatch method that rechecks live account plus exact accepted catalog/manifest authority before every action. In `/api/runtime`, route exact pinned COMMON messages through that bridge method and wrap its one matching result in the existing `runtime.event` frame; ensure the legacy `runtime.start` verified artifact uses the same registration function rather than a browser-supplied tuple. Add production-seam integration tests using injected deterministic relay/signer boundaries while exercising the actual `main.ts`/route composition API for authorized success, missing/denied authority, and required publication failure. Preserve the exact pinned contract, string-only STORAGE behavior, and the absence of any unsolicited COMMON push envelope. Do not modify Blossom, relay, or artifact caches.</action>
  <verify>
    <automated>deno test -A tests/common_runtime_integration_test.ts --filter "authorized success|denied|publication failure"</automated>
  </verify>
  <done>The shipped Fresh runtime—not only a unit-constructed service—binds verified launch authority to the actual account signer and required-relay publisher, with authorized success, denial, and publication failure proven at the production seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| COMMON decoder to Nostr services | Napplet-controlled public identifiers, relay hints, reactions, reports, and target lists reach backend logic. |
| EventStore/relay projection to iframe | Partial cached and live Nostr data is sanitized into the pinned public result surface. |
| common actions to signer/outbox | Validated intent crosses into backend signing and publication authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Information Disclosure | NIP-19 decode | high | mitigate | Explicitly allow only npub, note, nprofile, nevent, naddr, nrelay and reject nsec before projection. |
| T-06-11 | Denial of Service | profile/action inputs | medium | mitigate | Bound ids, arrays, text, relay hints, and correlated window-owned work. |
| T-06-12 | Spoofing | late projection results | high | mitigate | Check account/manifest/window generation before emitting and cancel work on expiry. |
| T-06-13 | Elevation of Privilege | signer/outbox actions | high | mitigate | Recheck current backend authority and capability immediately before signing and publishing. |
| T-06-14 | Information Disclosure | profile/action errors | medium | mitigate | Sanitize projected fields and return stable errors without signer, relay, or exception detail. |
| T-06-SC | Tampering | dependency supply chain | low | accept | No package install or upgrade occurs; existing lockfile-pinned packages remain unchanged. |
</threat_model>

<verification>
Run `deno test -A tests/common_test.ts tests/storage_test.ts tests/common_storage_runtime_test.ts`, then `deno task test`, `deno task check`, and `deno task build`.
</verification>

<success_criteria>
Every one of the exact eight COMMON request/response operations is contract-tested; public codecs exclude secrets, projections remain partial and update through the shared Applesauce store, actions remain backend-authorized, and teardown prevents stale authority from emitting.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Common Nostr helpers plus durable isolated key-value state | 06-01..06-03 | COVERED | Authority tracer, durable storage, and full COMMON expansion.
REQ | COM-01 | Canonical NIP-19 plus all common actions | 06-01, 06-03 | COVERED | Exact eight-operation parity.
REQ | COM-02 | Applesauce-backed partial/updating common data | 06-03 | COVERED | Shared EventStore and bounded relay projection.
REQ | STO-01 | set/get/list/remove through pinned STORAGE | 06-01, 06-02 | COVERED | String-only four-operation union.
REQ | STO-02 | account/manifest/scope isolation, quotas, deterministic serialization | 06-01, 06-02 | COVERED | Backend authority plus quota/codec policy.
REQ | STO-03 | reconnect/restart durability outside shell/browser state | 06-02 | COVERED | Process-owned atomic snapshot.
RESEARCH | — | Verified manifest/window authority prerequisite | 06-01 | COVERED | First tracer slice.
RESEARCH | — | Exact COMMON eight and STORAGE four contract surfaces | 06-01..06-03 | COVERED | No invented envelope or operation.
RESEARCH | — | Global snapshot serialization and malformed/write-failure safety | 06-02 | COVERED | One document-wide commit tail.
RESEARCH | — | Shared EventStore projections and generation cleanup | 06-03 | COVERED | RxJS composition, no duplicate state.
CONTEXT | D-01..D-04 | Common codec, sync helper, partial projection, shared runtime | 06-01, 06-03 | COVERED | Exact helper/projection/action work.
CONTEXT | D-05..D-09 | Account/manifest/scope/instance namespace and revocation | 06-01, 06-02 | COVERED | Server-derived authority only.
CONTEXT | D-10..D-14 | Durable store, validation/quotas, serialization, failed-write integrity | 06-02 | COVERED | Closed atomic snapshot.
CONTEXT | D-15..D-16 | Lifecycle cleanup/privacy and adversarial recovery coverage | 06-01..06-03 | COVERED | Redaction, teardown, restart/concurrency tests.
</source_audit>

<output>
Create `.planning/phases/06-common-data-and-durable-storage/06-03-SUMMARY.md` when done.
</output>
