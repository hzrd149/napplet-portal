---
phase: 06-common-data-and-durable-storage
plan: "02"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - runtime/storage_store.ts
  - runtime/storage.ts
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - tests/storage_test.ts
autonomous: true
requirements: [STO-01, STO-02, STO-03]
must_haves:
  truths:
    - "A napplet can set, get, list, and remove string values in omitted/shared and instance scopes only."
    - "Stored values survive reconnect and process restart while remaining isolated by active account and exact accepted manifest identity."
    - "All mutations across the single snapshot serialize globally, deterministic output is stable across restart, and failed/quota-exceeding writes preserve the prior committed state."
    - "Sign-out and manifest replacement revoke access without deleting durable namespaces."
  artifacts:
    - path: runtime/storage_store.ts
      provides: "Closed version-1 canonical snapshot codec and atomic filesystem replacement"
      exports: [NappletStorageStore, NappletStorageSnapshot]
    - path: runtime/storage.ts
      provides: "String-only isolated storage service, quota policy, and deterministic operations"
      exports: [StorageService]
    - path: tests/storage_test.ts
      provides: "Codec, persistence, isolation, quota, concurrency, and recovery tests"
  key_links:
    - from: runtime/nap_dispatcher.ts
      to: runtime/storage.ts
      via: "all four exact storage requests use backend-resolved WindowCapabilityContext"
      pattern: "storage\.(get|set|remove|keys)"
    - from: runtime/storage.ts
      to: runtime/storage_store.ts
      via: "global copy-validate-persist-commit mutation tail"
      pattern: "write"
---

<objective>
Complete durable, deterministic, quota-bounded NAP-STORAGE behind the verified window authority seam.

Purpose: Give napplets restart-safe state without browser ownership, filesystem exposure, or cross-account/manifest leakage.
Output: Atomic snapshot store, isolated storage service, four-operation dispatcher coverage, and adversarial persistence tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-common-data-and-durable-storage/06-CONTEXT.md
@.planning/phases/06-common-data-and-durable-storage/06-RESEARCH.md
@.planning/phases/06-common-data-and-durable-storage/06-01-SUMMARY.md
@runtime/settings_store.ts
@runtime/account_store.ts
@runtime/nap_dispatcher.ts
@runtime/portal_runtime.ts

<interfaces>
Consume WindowCapabilityContext from Plan 01. Namespace identity is the collision-free tuple of active account pubkey, coordinate, manifestEventId, dTag, aggregateHash, canonical scope, and backend instanceId only for instance scope. The pinned wire surface is exactly storage.get, storage.set, storage.remove, storage.keys; value is string, missing get is null, scope is omitted/shared/instance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist one closed globally serialized storage snapshot</name>
  <files>runtime/storage_store.ts, runtime/storage.ts, tests/storage_test.ts</files>
  <behavior>
    - Missing file initializes an empty version-1 snapshot; a valid snapshot round-trips in canonical namespace/key order.
    - Unknown versions, extra fields, arrays, prototype-bearing maps, or non-string values fail closed and are not overwritten by a later mutation.
    - Concurrent mutations in the same or different namespaces commit through one global tail without lost updates.
    - An injected write/rename failure leaves both in-memory and on-disk committed truth unchanged and the queue accepts later valid work.
    - Restart reloads shared and instance values exactly; storage.keys ordering is deterministic before and after reload (D-10, D-12, D-14, D-16).
  </behavior>
  <action>Implement a process-owned version-1 snapshot with a closed parser, canonical serialization, fixed configured path, random sibling temp file, directory mode 0700, file mode 0600 where supported, atomic rename, and best-effort redacted cleanup. Use one global mutation tail for the entire document: copy the latest committed immutable state, validate, persist, then publish it in memory. Never reset or overwrite malformed durable state. Keep opaque namespace tuple keys inside the document and never derive filesystem paths from napplet input. This implements D-10, D-12, D-14, and D-16.</action>
  <verify>
    <automated>deno test -A tests/storage_test.ts --filter "snapshot|concurrent|restart|write failure|malformed"</automated>
  </verify>
  <done>Canonical snapshots survive restart, concurrent cross-namespace writes cannot erase each other, and malformed or failed writes preserve all prior durable evidence and state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete all four isolated string-only STORAGE operations and quotas</name>
  <files>runtime/storage.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/storage_test.ts</files>
  <behavior>
    - storage.get/set/remove/keys accept exact request keys, echo bounded correlation ids, default omitted scope to shared, accept only shared/instance, and reject every other scope.
    - Values are strings only; get returns null for a missing key; remove of a missing key is deterministic; keys are lexically sorted.
    - Shared namespaces are isolated by active account plus exact accepted manifest identity; instance adds only the backend-issued instanceId.
    - Quotas use UTF-8 bytes: 512 KiB aggregate across shared and all instances for one account/exact manifest, 256 keys per namespace, 1 KiB per key, and 64 KiB per value, with minus-one/exact/plus-one Unicode tests.
    - Sign-out/replacement returns not-authorized but retains values; reconnect restores access to the same bound window; a new verified matching launch after restart can read prior shared truth (D-05 through D-16).
  </behavior>
  <action>Replace the tracer port with StorageService and implement precisely the pinned four-operation request/result union. Accept string values only, canonicalize omitted scope to shared, and reject invented scope names. Derive every namespace component from the current backend context; never accept account, coordinate, manifest, aggregate, or instance selectors from the message. Enforce the research-approved conservative quota constants on prospective UTF-8 state before persistence and return only stable redacted codes such as invalid-request, not-authorized, quota-exceeded, storage-unavailable, or unsupported. Do not delete namespaces during sign-out or replacement. This implements D-05 through D-16 and completes STO-01 through STO-03.</action>
  <verify>
    <automated>deno test -A tests/storage_test.ts tests/common_storage_runtime_test.ts</automated>
  </verify>
  <done>All four exact STORAGE operations are durable, deterministic, isolated across account/manifest/instance boundaries, safely quota-bounded, and correct across reconnect, restart, revocation, and recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher to storage service | Untrusted keys, scopes, and values meet server-derived namespace authority. |
| storage service to filesystem | Validated immutable state crosses into one host-only durable document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | namespace resolver | high | mitigate | Derive account, manifest, and instance solely from current backend WindowCapabilityContext. |
| T-06-06 | Denial of Service | storage.set | high | mitigate | Enforce aggregate, key-count, key-byte, and value-byte limits before disk mutation. |
| T-06-07 | Tampering | concurrent snapshot writes | high | mitigate | Serialize the whole document globally and commit memory only after atomic persistence succeeds. |
| T-06-08 | Information Disclosure | keys/errors/filesystem | high | mitigate | List only the resolved namespace and map all failures to stable redacted public codes. |
| T-06-09 | Tampering | malformed snapshot recovery | medium | mitigate | Reject closed-codec failures and refuse automatic reset/overwrite. |
| T-06-SC | Tampering | dependency supply chain | low | accept | No package install or upgrade occurs; existing lockfile-pinned packages remain unchanged. |
</threat_model>

<verification>
Run `deno test -A tests/storage_test.ts tests/common_storage_runtime_test.ts`, then `deno task test` and `deno task check`.
</verification>

<success_criteria>
The exact four-operation string-only STORAGE contract persists across restart and reconnect, cannot cross account/manifest/instance boundaries, and remains atomic and deterministic under malformed input, quota pressure, concurrency, and write failure.
</success_criteria>

<output>
Create `.planning/phases/06-common-data-and-durable-storage/06-02-SUMMARY.md` when done.
</output>
