---
phase: 02-backend-runtime-expansion
plan: "03"
type: execute
wave: 2
depends_on: [02-01]
files_modified: [runtime/blossom_cache.ts, runtime/artifacts.ts, runtime/portal_runtime.ts, tests/blossom_cache_test.ts, tests/artifact_resolver_test.ts]
autonomous: true
requirements: [V2-05, V2-10]
must_haves:
  truths:
    - "The fixed loopback Blossom endpoint is discovered with a bounded HEAD and receives BUD-10 xs/as hints (D-06, D-08)."
    - "Unavailable, timeout, miss, or proxy failure falls through to upstream without blocking launch (D-07)."
    - "Executable manifests and blobs still pass Phase 1 signature, aggregate, and hash checks regardless of cache source (D-09, V2-10)."
  artifacts: [runtime/blossom_cache.ts, tests/blossom_cache_test.ts]
  key_links: ["PortalArtifactResolver -> BlossomCache -> local proxy/upstream -> existing integrity verification"]
  prohibitions:
    - statement: "A cache hit must not silently become authority to execute unverified bytes."
      status: unverified
      flagged: true
---
<objective>Add transparent local Blossom read-through while preserving the executable integrity boundary. Purpose: Cache artifacts/media locally without coupling availability to correctness. Output: BlossomCache integration and deterministic fallback tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-RESEARCH.md
@.planning/phases/01-one-day-napplet-runtime-mvp/01-03-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resolve one verified manifest through local Blossom with upstream fallback</name>
  <read_first>runtime/artifacts.ts, runtime/portal_runtime.ts, tests/artifact_resolver_test.ts, .planning/phases/02-backend-runtime-expansion/02-CONTEXT.md</read_first>
  <files>runtime/blossom_cache.ts, runtime/artifacts.ts, runtime/portal_runtime.ts, tests/blossom_cache_test.ts, tests/artifact_resolver_test.ts</files>
  <behavior>- Probe `http://127.0.0.1:24242/` by bounded HEAD.
- Build local fetch URLs with repeated `xs` and optional `as` parameters.
- Fall through on unhealthy probe, timeout, 404, or fetch failure.
- Reject bad signatures, aggregate hashes, or blob hashes from every source.</behavior>
  <action>Implement `BlossomCache`, `discoverLocalBlossom`, and `fetchWithBlossomCache` with injected fetch/timer dependencies. Wrap `PortalArtifactResolver` source retrieval so ordinary media can trust a healthy loopback response but `resolveNapplet` always performs existing verification per D-06 through D-09. Only fixed loopback discovery and validated configured upstream HTTP(S) servers are eligible; browser-supplied arbitrary hints are not forwarded. Resolve V2-05 and V2-10 unclassified edge assumptions with explicit no-cache, cache-hit, miss, failure, malformed-byte, and attested-identity cases.</action>
  <verify><automated>deno test -A tests/blossom_cache_test.ts tests/artifact_resolver_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Recorded fetch calls prove fixed HEAD discovery and correct xs/as encoding; every failure class reaches upstream; existing invalid-signature/aggregate/blob tests remain fail-closed; one tracer resolves valid executable HTML through the local cache path.</acceptance_criteria>
  <done>Loaded napplets can use the standard local Blossom cache without weakening integrity or availability.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| configured/hinted URL → server fetch | URLs influence backend outbound HTTP requests. |
| cache bytes → executable resolver | Untrusted bytes approach the iframe execution boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-06 | Spoofing | BlossomCache URL routing | high | mitigate | Fixed loopback discovery, allowed schemes, canonical upstream configuration, and no arbitrary browser hints. |
| T-02-07 | Tampering | PortalArtifactResolver | high | mitigate | Signature, aggregate, and content hash verification after every fetch source. |
| T-02-08 | Denial of Service | local probe/proxy | medium | mitigate | Abort timeout and immediate upstream fallback. |
</threat_model>
## Probe assumptions
- V2-05 and V2-10 were unclassified; the plan makes absent/degraded cache and unverifiable identity explicit test predicates.
## Artifacts this phase produces
- `BlossomCache`, `BlossomCacheHealth`, `discoverLocalBlossom`, `fetchWithBlossomCache`
- `runtime/blossom_cache.ts`, `tests/blossom_cache_test.ts`
<verification>`deno test -A tests/blossom_cache_test.ts tests/artifact_resolver_test.ts` and `deno task check` pass.</verification>
<success_criteria>Local proxy hits and all fallback paths work; no executable byte bypasses Phase 1 verification.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-03-SUMMARY.md` when done.</output>
