---
phase: 02-backend-runtime-expansion
plan: "06"
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: [components/HomeView.tsx, components/NappletFrame.tsx, islands/NappletShell.tsx, assets/styles.css, tests/catalog_ui_test.tsx, tests/shell_architecture_test.ts, tests/end_to_end_test.ts]
autonomous: true
requirements: [V2-09, V2-10]
must_haves:
  truths:
    - "An empty catalog shows `No napplets installed`; initial loading shows `Syncing installed napplets…`; stale/error states retain synchronized cards and use the exact approved status copy."
    - "Zero, one, and many verified catalog entries render as start-aligned responsive cards without page-level horizontal overflow; missing optional metadata has defined fallbacks and unresolved manifests cannot launch."
    - "Each card shows verified icon/name/accepted version, uses accepted manifest ID for launch, and exposes an accessible named overflow action."
    - "Update review shows publisher/coordinate, old/new manifest IDs, display/version changes, aggregate hash, and line-by-line Added/Removed capability changes (D-20)."
    - "Update approval keeps the accepted version launchable until command acceptance; failure keeps the dialog open and preserves accepted state."
    - "Uninstall confirmation names the napplet, explains cache retention, disables both actions while publishing, and failure remains retryable."
    - "When the visible catalog changes during a dialog, the stale dialog closes and announces `The installed catalog changed. Review the latest version before continuing.`"
    - "Without an active signer, catalog mutations are disabled with `Connect a signer to change installed napplets.` while public catalog content remains readable."
    - "Update identifiers middle-truncate, expose full accessible/copy values, and never overflow; dialogs trap focus, Escape closes before mutation, and focus returns to the invoking card."
    - "Inline statuses coalesce non-critical stream updates while operation failures remain associated alerts; status is never color-only and reduced motion avoids reorder/pulse/comparison animation."
    - statement: "At 320px, napplet names clamp to two lines and coordinate/version metadata middle-truncates without hiding actions."
      verification: backstop
  artifacts: [components/HomeView.tsx, tests/catalog_ui_test.tsx]
  key_links:
    - "CatalogProjection stream -> NappletShell -> HomeView stable keyed cards"
    - "card actions -> correlated catalog commands -> synchronized projection update"
  prohibitions:
    - statement: "Update availability must not silently auto-accept a newer manifest or its capabilities."
      status: unverified
      flagged: true
    - statement: "Uninstall must not silently claim or perform immediate content-addressed cache deletion."
      status: unverified
      flagged: true
---
<objective>Turn the synchronized catalog into the approved mobile installed-napplet experience. Purpose: Make account-owned installs, updates, and uninstall observable and safe. Output: Responsive cards, dialogs, stream states, and end-to-end UI tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-UI-SPEC.md
@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-04-SUMMARY.md
@.planning/phases/02-backend-runtime-expansion/02-05-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Launch one synchronized accepted catalog card</name>
  <read_first>components/HomeView.tsx, components/NappletFrame.tsx, islands/NappletShell.tsx, assets/styles.css, tests/end_to_end_test.ts, .planning/phases/02-backend-runtime-expansion/02-UI-SPEC.md</read_first>
  <files>components/HomeView.tsx, components/NappletFrame.tsx, islands/NappletShell.tsx, assets/styles.css, tests/catalog_ui_test.tsx, tests/end_to_end_test.ts</files>
  <behavior>- Last projection renders immediately and updates in place without replacing populated content with a page spinner.
- One card launches only its accepted verified manifest ID.
- Missing optional metadata uses fallback; unresolved required manifest data disables launch.</behavior>
  <action>Replace the configured tile projection with stable-keyed `InstalledCatalogGrid` and `NappletCard` data from the backend catalog stream. Implement exact empty/loading/stale/error copy, 48px anatomy, accepted version/source metadata, fallback icon/name, one-column mobile and auto-fit 280px grid from 640px, 16px gaps, and no page-level horizontal overflow. Keep iframe persistent and derive all card identity from the runtime projection per D-17 and D-19.</action>
  <verify><automated>deno test -A tests/catalog_ui_test.tsx tests/end_to_end_test.ts</automated></verify>
  <acceptance_criteria>Tests cover empty/loading/stale/error/populated/partial and zero-one-many states; a valid card launches accepted event ID; unresolved details show `Manifest details unavailable` and cannot launch; stream update retains card identity.</acceptance_criteria>
  <done>Home renders and launches synchronized accepted catalog entries across all data states.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Approve updates and uninstall through accessible stale-safe dialogs</name>
  <read_first>components/HomeView.tsx, islands/NappletShell.tsx, runtime/catalog.ts, tests/catalog_ui_test.tsx, .planning/phases/02-backend-runtime-expansion/02-UI-SPEC.md</read_first>
  <files>components/HomeView.tsx, islands/NappletShell.tsx, assets/styles.css, tests/catalog_ui_test.tsx, tests/shell_architecture_test.ts</files>
  <behavior>- Native update dialog presents complete attested comparison and capability diff.
- Native uninstall dialog publishes replacement only after explicit destructive action.
- In-flight, error, signer-absent, stale projection, Escape, Back, and focus-return paths follow UI-SPEC.</behavior>
  <action>Add card overflow actions, native `UpdateReviewDialog` and `UninstallDialog`, exact UI-SPEC copy, maximum 36rem width and viewport-safe height, scrollable comparison with visible heading/actions, old/new stacking below 640px, Added/Removed text diff, accessible identifiers/copy actions, and command pending/error handling. Close stale dialogs on catalog identity change; Back closes dialog first; do not autofocus the destructive action. Use the approved colors, typography, focus ring, 44px targets, aria-live/status/alert roles, and reduced-motion constraints.</action>
  <verify><automated>deno test -A tests/catalog_ui_test.tsx tests/shell_architecture_test.ts tests/end_to_end_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Tests assert exact comparison fields/copy, no-capability-change copy, accepted-version launch before approval, disabled mutations without signer, in-flight labels, retryable failures, stale close announcement, Back/Escape ordering, uninstall cache-retention copy, and focus return.</acceptance_criteria>
  <done>Operators can review and approve verified updates or uninstall entries without stale or iframe-controlled identity.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| catalog projection → browser UI | Public network state becomes actionable controls. |
| iframe/card interaction → catalog command | Browser requests signed account mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14 | Spoofing | NappletCard/update dialog | high | mitigate | Render runtime-attested manifest projection only; command keys bind coordinate and current accepted ID. |
| T-02-15 | Tampering | stale dialog command | high | mitigate | Close on projection identity change and backend re-reads latest catalog before signing. |
| T-02-16 | Repudiation | destructive action | medium | mitigate | Named confirmation, explicit command response, and associated failure status. |
</threat_model>
## Probe assumptions
- V2-09/V2-10 unclassified edges are resolved through all catalog cardinalities, missing metadata, stale dialogs, signer absence, concurrent replacement, and accepted identity tests.
## Artifacts this phase produces
- `InstalledCatalogGrid`, `NappletCard`, `UpdateReviewDialog`, `UninstallDialog`, `InlineStatusNotice`
- `tests/catalog_ui_test.tsx`
<verification>`deno test -A tests/catalog_ui_test.tsx tests/shell_architecture_test.ts tests/end_to_end_test.ts` and `deno task check` pass; 320px backstop remains `human_needed` absent explicit browser evidence.</verification>
<success_criteria>All 18 installed-catalog/AUTH-independent UI considerations and their responsive/accessibility states have executable evidence or an explicit backstop.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-06-SUMMARY.md` when done.</output>

