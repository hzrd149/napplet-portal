---
phase: 01-one-day-napplet-runtime-mvp
plan: 05
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified: [runtime/relay_adapter.ts, runtime/outbox.ts, runtime/portal_runtime.ts, tests/relay_stream_test.ts, tests/identity_service_test.ts]
autonomous: true
requirements: [STREAM-01, STREAM-02, STREAM-03, STREAM-04, STREAM-05, STREAM-06, STREAM-07, NAP-02, NAP-03, NAP-04, QUAL-02]
must_haves:
  truths:
    - "D-24/D-28/D-45: one backend pool/store composes store-first and continuing live streams with centralized dedupe and no nested subscriptions"
    - "D-25/D-26/D-32/D-33/D-34: logical subscriptions remain connection/window/subId-owned and close/reconnect/expiry clean up exactly"
    - "D-27/D-29/D-30: RELAY uses supplied URL, exact observed hints, one EOSE and live tail; OUTBOX uses preset+NIP-65 and no EOSE"
    - "D-36–D-40: relay/outbox/encrypted publishing follows exact signing ownership and waits for relay acceptance"
  artifacts:
    - { path: runtime/relay_adapter.ts, provides: provenance-preserving store/live relay streams }
    - { path: runtime/outbox.ts, provides: NIP-65 routing, no-EOSE streams, settled publish }
    - { path: runtime/portal_runtime.ts, provides: process-wide runtime/service composition }
  key_links:
    - { from: runtime/portal_runtime.ts, to: runtime/connections.ts, via: window delivery and cleanup }
    - { from: runtime/outbox.ts, to: runtime/accounts.ts, via: active backend signer }
---
<objective>Expand Plan 01's initial-plus-live RELAY tracer into the complete backend continuing-stream and canonical publishing seam through the singleton runtime.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@runtime/accounts.ts
@runtime/connections.ts
@runtime/transport.ts
@../kehto/packages/runtime/src/relay-handler.ts
@../kehto/packages/runtime/src/relay-result.ts
@../hyprgate-gui/apps/shell/src/lib/relay/relay-req-stream.ts
@../hyprgate-gui/apps/shell/src/lib/kehto/outbox-router.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Stream store-first RELAY data with provenance, EOSE, live tail, and cleanup</name>
  <files>runtime/relay_adapter.ts, tests/relay_stream_test.ts</files>
  <read_first>runtime/connections.ts; tests/fixtures/supplied_napplet_contract.json; ../kehto/packages/runtime/src/relay-handler.ts; ../kehto/packages/runtime/src/relay-result.ts; ../hyprgate-gui/apps/shell/src/lib/relay/relay-event-store.ts; ../hyprgate-gui/apps/shell/src/lib/relay/relay-req-stream.ts; .planning/phases/01-one-day-napplet-runtime-mvp/COVERAGE.md</read_first>
  <behavior>- Matching store values merge with live `pool.req` messages and dedupe centrally. - Only raw `message.from` becomes relayHints; cache provenance is omitted when unknown. - Exactly one relay.eose occurs without completing live delivery. - Close removes ownership before unsubscribe and emits canonical closed. - Two windows sharing subId remain independent.</behavior>
  <action>Implement D-24–D-30, D-32–D-34, D-43/D-45 with a single backend-wide `RelayPool`/`EventStore`, RxJS composition and `finalize`; never nest subscriptions or await stream completeness. RELAY uses the napplet relay URL. Preserve exact provenance before dedupe, reconstruct one EOSE from raw relay state, and keep the tail live. Treat partial relay availability as native relay-pool behavior.</action>
  <verify><automated>deno test -A tests/relay_stream_test.ts</automated></verify>
  <acceptance_criteria>Tests prove store-first then live updates, dedupe, exact/omitted hints, one nonterminal EOSE, independent ownership, immediate close, and replay-safe reconnect.</acceptance_criteria>
  <done>NAP-RELAY streams real backend-owned Nostr data continuously and canonically.</done>
</task>
<task type="auto" tdd="true">
  <name>Compose identity, OUTBOX, publishing, and singleton runtime delivery</name>
  <files>runtime/outbox.ts, runtime/portal_runtime.ts, tests/identity_service_test.ts, tests/relay_stream_test.ts</files>
  <read_first>runtime/accounts.ts; runtime/relay_adapter.ts; runtime/artifacts.ts; runtime/connections.ts; tests/fixtures/supplied_napplet_contract.json; ../hyprgate-gui/apps/shell/src/lib/kehto/outbox-router.ts; ../kehto/packages/runtime/src/relay-handler.ts; .planning/phases/01-one-day-napplet-runtime-mvp/COVERAGE.md</read_first>
  <behavior>- One runtime/pool/store/account/cache serves all connections. - IDENTITY reads/broadcasts active/offline/unavailable state. - OUTBOX combines preset relays with NIP-65, emits no EOSE, and stays live. - RELAY publish forwards signed event unchanged; OUTBOX signs unsigned template; encrypted relay publish signs/encrypts server-side. - Success follows required acknowledgements and canonical per-relay outcomes.</behavior>
  <action>Implement D-07/D-08, D-22, D-27, D-31/D-33, D-36, D-37, D-38, D-39, and D-40 against the actual supplied-napplet fixture captured in Plan 01. Register only identity, relay, and outbox services in the Kehto runtime. Use stock Kehto outbox routing with Applesauce/NIP-65 inputs. RELAY forwards an already-signed event unchanged per D-37; OUTBOX signs its unsigned template. For D-38 log only napplet identity, event ID/kind and request ID. For D-39 keep encryption/signing backend-owned. Per D-40 report success only after target/required relay acknowledgements. Reject signer-dependent paths when unavailable and keep read subscriptions alive. Teardown windows/subscriptions, then services, then runtime/pool.</action>
  <verify><automated>deno test -A tests/identity_service_test.ts tests/relay_stream_test.ts</automated></verify>
  <acceptance_criteria>Identity and events traverse runtime→connection→napplet; RELAY/OUTBOX distinctions match COVERAGE.md; publish results arrive only after canonical settlement.</acceptance_criteria>
  <done>The four integrated NAP capability domains run through one backend authority.</done>
</task>
</tasks>

## Artifacts this phase produces
- `runtime/relay_adapter.ts`: relay subscription/publish adapter.
- `runtime/outbox.ts`: NIP-65 outbox router adapter.
- `runtime/portal_runtime.ts`: singleton composition and teardown.
- Stream and identity service tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| napplet→relay/outbox services | Untrusted URLs, filters, events, templates and correlation IDs reach network/signing authority |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Elevation of privilege | publish services | high | mitigate | Domain-specific signed-vs-template validation; backend signer only for OUTBOX/encrypted publish |
| T-01-16 | Repudiation | publish settlement | high | mitigate | Await relay acknowledgement/required fanout and return canonical per-relay outcomes |
| T-01-17 | Information disclosure | relay logging | high | mitigate | Allowlist identity/event ID/kind/request ID; omit content/signature/secrets |
| T-01-18 | Denial of service | subscriptions | medium | mitigate | Ownership registry, immediate close/finalize, grace expiry cleanup |
| T-01-SC | Tampering | npm installs | high | mitigate | Dependency install already gated by Plan 01 |
</threat_model>
<verification>`deno task check && deno test -A tests/relay_stream_test.ts tests/identity_service_test.ts`.</verification>
<success_criteria>The supplied napplet can read identity, receive updating relay/outbox streams, close them, and publish with exact canonical semantics.</success_criteria>
<output>Create `.planning/phases/01-one-day-napplet-runtime-mvp/01-05-SUMMARY.md`.</output>
