---
phase: 06-common-data-and-durable-storage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - routes/api/runtime.ts
  - components/NappletFrame.tsx
  - tests/common_storage_runtime_test.ts
autonomous: true
requirements: [COM-01, STO-01, STO-02]
must_haves:
  truths:
    - "A verified launched frame can complete one canonical common.encodeNip19 request and one string-only storage.set/storage.get round trip."
    - "No COMMON or STORAGE request is dispatched until the backend has bound the window to the current account and exact verified manifest/launch identity."
    - "Reconnect preserves the backend-issued window/instance authority, while expiry, sign-out, or manifest replacement revokes access."
  artifacts:
    - path: runtime/nap_dispatcher.ts
      provides: "Exact COMMON/STORAGE envelope decoding and authority-first dispatch seam"
      exports: [NapDispatcher, WindowCapabilityContext]
    - path: runtime/portal_runtime.ts
      provides: "Process-owned authority-bound window lifecycle"
    - path: tests/common_storage_runtime_test.ts
      provides: "End-to-end verified-window tracer coverage"
  key_links:
    - from: routes/api/runtime.ts
      to: runtime/portal_runtime.ts
      via: "backend-resolved catalog launch result binds the accepted manifest identity before runtime.forward is accepted"
      pattern: "bind.*(launch|authority|capability)"
    - from: runtime/portal_runtime.ts
      to: runtime/nap_dispatcher.ts
      via: "current WindowCapabilityContext is resolved and rechecked for every forwarded request"
      pattern: "dispatch"
---

<objective>
Prove the Phase 6 authority and contract architecture end to end with one canonical NIP-19 helper and one durable-storage-shaped string round trip.

Purpose: Close the current security gap between browser-held verified launch facts and backend runtime dispatch before expanding either domain.
Output: A production authority registry/dispatcher seam, verified launch binding, canonical result envelopes, and an end-to-end tracer test.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-common-data-and-durable-storage/06-CONTEXT.md
@.planning/phases/06-common-data-and-durable-storage/06-RESEARCH.md
@runtime/portal_runtime.ts
@runtime/transport.ts
@runtime/connections.ts
@runtime/catalog.ts
@routes/api/runtime.ts
@components/NappletFrame.tsx

<interfaces>
Existing catalog launch returns VerifiedCatalogArtifact with manifestEventId, capabilities, and launch {dTag, aggregateHash, srcdoc}. RuntimeServiceHub.openWindow(windowId, send, connectionId) owns cleanup through close(). Runtime transport carries runtime.forward {connectionId, windowId, message}; identity components are not part of pinned COMMON/STORAGE messages. Preserve those boundaries and add authority only from backend-verified launch output.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one verified launch to canonical COMMON and STORAGE round trips</name>
  <files>runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, routes/api/runtime.ts, components/NappletFrame.tsx, tests/common_storage_runtime_test.ts</files>
  <behavior>
    - A backend-resolved launch registers immutable account pubkey, coordinate, manifestEventId, dTag, aggregateHash, granted domains, and a backend-issued instanceId against the owned connection/window before iframe forwarding is enabled (D-07, D-08, D-09).
    - One valid common.encodeNip19 request receives exactly one correlated common.encodeNip19.result using the pinned 0.31.0 shape; unsupported/ambiguous input receives a correlated stable error (D-01, D-02).
    - One storage.set followed by storage.get accepts only string value data and omitted/shared scope, returning canonical correlated result envelopes (D-05, D-11).
    - Foreign, unbound, signed-out, capability-denied, or superseded-manifest windows receive stable redacted authorization results without reaching a service.
    - Socket reconnection within the existing grace period retains the same window and instance context; actual expiry closes correlated work and revokes the context without deleting storage state (D-13, D-15).
  </behavior>
  <action>Start with the failing end-to-end test, then add an exact-key NapDispatcher decoder and backend WindowCapabilityContext registry. Bind authority only from the exact catalog.launch result already reverified by CatalogService; the iframe may trigger launch but must never declare account, manifest, aggregate, domain grants, or instance identity. Extend the frame allowlist only after verified binding and advertise COMMON/STORAGE according to the verified manifest capabilities. Implement the tracer's encode path with the existing pinned nostr-tools codec and its storage path through an injected in-memory test port whose interface is durable-service-ready. Keep storage values strictly strings and scope closed to omitted/shared or instance; do not introduce structured values, arbitrary scopes, or a profile push envelope. Recheck current identity and accepted manifest at every dispatch and settle one response per request. This implements D-01, D-02, D-05, D-07 through D-09, D-11, D-13, and D-15.</action>
  <verify>
    <automated>deno test -A tests/common_storage_runtime_test.ts</automated>
  </verify>
  <done>A verified frame completes both tracer paths end to end, while every unverified, stale, foreign, revoked, or ungranted path fails closed and cleanup follows connection expiry rather than transient detach.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iframe to shell | Untrusted napplet messages may originate only from the registered verified contentWindow. |
| shell/WebSocket to backend dispatcher | Caller-controlled envelopes cross into backend-owned account, launch, and capability authority. |
| catalog launch to window authority | Verified executable identity becomes an immutable server-side namespace input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Spoofing | runtime.forward / window context | high | mitigate | Resolve connection/window ownership and immutable verified launch context server-side for every dispatch. |
| T-06-02 | Elevation of Privilege | COMMON/STORAGE capability grant | high | mitigate | Check the current exact manifest grant before decoding and invoking each domain. |
| T-06-03 | Information Disclosure | authorization errors | medium | mitigate | Return stable redacted result errors without account, manifest, signer, relay, path, or stored-value details. |
| T-06-04 | Tampering | stale launch context | high | mitigate | Invalidate generation on account/accepted-manifest replacement and reject late or superseded work. |
| T-06-SC | Tampering | dependency supply chain | low | accept | No package install or upgrade occurs; existing lockfile-pinned packages remain unchanged. |
</threat_model>

<verification>
Run `deno test -A tests/common_storage_runtime_test.ts`, then `deno task check`.
</verification>

<success_criteria>
The backend—not iframe input—owns the complete verified namespace context, one real COMMON request and one real string-only STORAGE round trip cross the full proxy, and revoked contexts cannot dispatch.
</success_criteria>

<output>
Create `.planning/phases/06-common-data-and-durable-storage/06-01-SUMMARY.md` when done.
</output>
