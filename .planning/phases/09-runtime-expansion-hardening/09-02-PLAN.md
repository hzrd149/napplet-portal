---
phase: 09-runtime-expansion-hardening
plan: "02"
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - runtime/security_headers.ts
  - main.ts
  - routes/_app.tsx
  - routes/api/runtime.ts
  - components/NappletFrame.tsx
  - tests/adversarial_boundaries_test.ts
autonomous: true
requirements: [QLT-02]
must_haves:
  truths:
    - "Hostile input cannot cross capability, account, verified artifact, source-window, generation, signer, storage, URL, intent, media, or reconnect-token boundaries."
    - "Production responses enforce a closed CSP and Permissions-Policy compatible with the exact sandboxed runtime flow."
    - "Public errors and logs contain stable codes but no secrets, private destinations, paths, tokens, or cross-account data."
  artifacts:
    - path: tests/adversarial_boundaries_test.ts
      provides: "Deny-by-default boundary matrix"
    - path: runtime/security_headers.ts
      provides: "Single closed production header policy"
  key_links:
    - from: components/NappletFrame.tsx
      to: runtime/nap_contract_registry.ts
      via: "exact current WindowProxy plus granted action routing"
    - from: runtime/security_headers.ts
      to: main.ts
      via: "production response middleware"
---

<objective>
Turn the parity ledger into adversarial proof and repair every red boundary row, per D-05 through D-08.

Purpose: Demonstrate that napplet-controlled input cannot escape backend authority or leak sensitive state.
Output: Boundary matrix, targeted implementation fixes, and enforced/tested browser security headers.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/CONTRACT-PARITY.md
@runtime/nap_contract_registry.ts
@runtime/resource_policy.ts
@runtime/connections.ts
@runtime/catalog.ts
@components/NappletFrame.tsx
@routes/api/runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one forged iframe action to a sanitized denial with zero effect</name>
  <files>components/NappletFrame.tsx, routes/api/runtime.ts, runtime/nap_contract_registry.ts, tests/adversarial_boundaries_test.ts</files>
  <behavior>
    - A wrong source window, wrong direction, extra key, stale generation, or missing capability never invokes a handler.
    - The caller receives only a stable protocol-safe denial where the contract permits a response.
  </behavior>
  <action>Build a table-driven tracer through browser message validation, WebSocket decoding, authenticated outer context, capability registry, and one domain dispatcher. Derive authority server-side and assert zero effects before checking the sanitized denial. Re-check identity/generation immediately before effects after asynchronous work. Preserve the exact iframe sandbox without allow-same-origin and use exact event.source ownership, per D-05 and D-06.</action>
  <verify><automated>deno test -A tests/adversarial_boundaries_test.ts --filter "iframe tracer"</automated></verify>
  <done>Every hostile tracer mutation fails closed with no handler call, cross-owner delivery, or sensitive output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust the adversarial matrix and enforce production browser policy</name>
  <files>runtime/security_headers.ts, main.ts, routes/_app.tsx, routes/api/runtime.ts, components/NappletFrame.tsx, tests/adversarial_boundaries_test.ts</files>
  <behavior>
    - Capability/codec, iframe, catalog/artifact, resource/upload, signer/common, storage, intent, media, reconnect, error, and log hostile tables deny safely.
    - Built responses contain exact CSP, frame-ancestor, and Permissions-Policy values without wildcard or unsafe-eval expansion.
    - Forbidden marker assertions cover secrets, reconnect tokens, local paths, private URLs, account data, and raw thrown messages.
  </behavior>
  <action>Expand the table by changing one dimension per case: missing/extra/wrong/oversized values; foreign account/window/source; stale ID/generation; invalid storage scope; ambiguous URL/DNS/redirect; duplicate correlation; late completion; replacement; and post-shutdown arrival. Reuse existing closed validators and Phase 5 policy code rather than creating alternative parsers. Add one security-header owner and assert the actual production response policy still permits Fresh assets, same-origin WebSocket, and verified sandboxed srcdoc while denying unrequired capabilities. Fix each discovered implementation gap in its owning file, and sanitize errors/logs per D-07 and D-08.</action>
  <verify><automated>deno test -A tests/adversarial_boundaries_test.ts && deno task build</automated></verify>
  <done>Every mandatory hostile row has executable zero-effect evidence and the built shell runs under its closed policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| iframe→shell | Opaque-origin untrusted messages enter the shell. |
| socket→backend | Untrusted JSON/binary frames meet authenticated connection authority. |
| backend→network/storage/signer | Authorized requests can cause durable or external effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-03 | Spoofing | iframe/socket ownership | high | mitigate | Exact source, connection, window, account, verified identity, and generation checks before effects and sends. |
| T-09-04 | Information disclosure | errors/logs | high | mitigate | Stable-code projection and forbidden-marker assertions. |
| T-09-05 | Elevation of privilege | CSP/sandbox/capability | high | mitigate | Exact sandbox, closed policy headers, registry-derived grants, and deny-by-default dispatch. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; slopcheck remains applicable to locked dependencies. |
</threat_model>

<verification>
Focused hostile suite passes and production build serves the exact security policy without runtime regressions.
</verification>

<success_criteria>
QLT-02 is proven at every primary trust boundary with repairs applied where tests expose gaps.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-02-SUMMARY.md` when done.
</output>
