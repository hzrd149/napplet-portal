---
phase: 09-runtime-expansion-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/nap_contract_registry.ts
  - tests/fixtures/v1_1_contract_matrix.json
  - tests/contract_parity_test.ts
  - tests/requirement_traceability_test.ts
  - .planning/phases/09-runtime-expansion-hardening/CONTRACT-PARITY.md
autonomous: true
requirements: [QLT-01]
must_haves:
  truths:
    - "Every exact request, result, and event discriminant in the ten in-scope pinned 0.31.0 NAP domains has one explicit disposition and direction."
    - "Any missing, invented, advertised-but-unsupported, or SILENT_IGNORE action blocks the release."
    - "All 33 v1.1 requirements map exactly once and completion claims require implementation plus verification evidence."
  artifacts:
    - path: runtime/nap_contract_registry.ts
      provides: "Single first-party dispatch/capability/evidence registry"
    - path: tests/fixtures/v1_1_contract_matrix.json
      provides: "Codec-derived machine-readable ten-domain matrix"
    - path: .planning/phases/09-runtime-expansion-hardening/CONTRACT-PARITY.md
      provides: "Generated readable parity report"
  key_links:
    - from: "@napplet/core@0.31.0 and @napplet/nap@0.31.0 declarations/exports"
      to: tests/fixtures/v1_1_contract_matrix.json
      via: "literal discriminant extraction"
    - from: tests/fixtures/v1_1_contract_matrix.json
      to: runtime/nap_contract_registry.ts
      via: "exact canonical-to-registered set join"
---

<objective>
Create the release-blocking tracer from pinned codecs through the production registry and evidence ledger, per locked contract decisions D-01 through D-04.

Purpose: Make contract drift and silent dispatch mechanically impossible before broad hardening begins.
Output: Canonical registry, generated JSON/Markdown parity evidence, and deterministic requirement-ledger checks.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-runtime-expansion-hardening/09-CONTEXT.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@deno.json
@components/NappletFrame.tsx
@runtime/nap_dispatcher.ts
@runtime/portal_runtime.ts
@tests/runtime_contract_test.ts
@tests/contract_drift_test.ts

<interfaces>
Treat the pinned npm packages as the executable contract authority. The ten in-scope domains are SHELL, IDENTITY, RELAY, OUTBOX, RESOURCE, UPLOAD, COMMON, STORAGE, INTENT, and MEDIA. Registry dispositions are exactly SUPPORTED, EXPLICIT_DENY, or OUT_OF_SCOPE_NOT_ADVERTISED; SILENT_IGNORE is never legal. Re-inventory the working tree at task start so completed Phase 6-8 execution and late Phase 5 review fixes are included rather than overwritten.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one codec-derived action through grant, decoder, dispatcher, result, and test</name>
  <precondition>For each of Phases 06, 07, and 08, the canonical VERIFICATION frontmatter has exactly `status: passed`, every PLAN has its matching canonical SUMMARY with `status: complete`, and every structured `coverage[].verification[]` entry in those summaries has `status: pass`; halt without freezing the matrix if any per-phase gate fails.</precondition>
  <files>runtime/nap_contract_registry.ts, tests/fixtures/v1_1_contract_matrix.json, tests/contract_parity_test.ts, .planning/phases/09-runtime-expansion-hardening/CONTRACT-PARITY.md</files>
  <behavior>
    - One real canonical request literal is extracted from the pinned package, registered with exact direction/capability/handler/terminal semantics, dispatched, and matched to a named passing test.
    - An absent row, invented row, wrong direction, missing evidence field, advertised unsupported row, or silent fallthrough fails.
  </behavior>
  <action>Before contract work, run one deterministic gate independently for each phase directory 06-common-data-and-durable-storage, 07-intent-navigation, and 08-cross-tab-media-sessions. For each phase: parse only the canonical VERIFICATION YAML frontmatter and require its status scalar to equal `passed`; derive expected SUMMARY names from that phase's PLAN filenames and require every corresponding file; parse each SUMMARY frontmatter and require its completion status to equal `complete`; require a non-empty canonical `coverage` array and require every nested `coverage[].verification[]` record to have `status: pass`. The passing typed phase VERIFICATION is the phase-level check/test gate; the canonical summary coverage records the per-plan executable evidence. Implement this as a fail-fast Deno evaluation so a missing artifact, malformed frontmatter, absent coverage evidence, or one failed phase identifies the exact phase and plan. D-19 makes these three independent checks mandatory despite `workflow.verifier=false`. Then inventory the post-Phase-8 tree and exact pinned package exports/declarations. Implement one production-quality vertical row across every seam, generate JSON and readable Markdown from the same source, and record package source, discriminant, direction, domain, grant, decoder, owner, terminal semantics, requirement IDs, test, and disposition. Use pinned packages directly, never sibling path imports or parallel codecs, per D-01 and D-03. Preserve Phase 5 review-fix changes visible at execution time.</action>
  <verify><automated>deno eval --allow-read 'import { extract } from "jsr:@std/front-matter/yaml"; for (const [phase, count] of [["06-common-data-and-durable-storage",3],["07-intent-navigation",4],["08-cross-tab-media-sessions",3]] as const) { const dir=`.planning/phases/${phase}`; const prefix=phase.slice(0,2); const verification=extract(await Deno.readTextFile(`${dir}/${prefix}-VERIFICATION.md`)).attrs as Record&lt;string,unknown&gt;; if (verification.status !== "passed") throw new Error(`${prefix}: canonical verification status is not passed`); for (let n=1;n&lt;=count;n++) { const id=String(n).padStart(2,"0"); await Deno.stat(`${dir}/${prefix}-${id}-PLAN.md`); const summary=extract(await Deno.readTextFile(`${dir}/${prefix}-${id}-SUMMARY.md`)).attrs as Record&lt;string,unknown&gt;; if (summary.status !== "complete") throw new Error(`${prefix}-${id}: summary status is not complete`); const coverage=summary.coverage as Array&lt;{verification?: Array&lt;{status?: unknown}&gt;}&gt; | undefined; if (!coverage?.length || coverage.some((entry) =&gt; !entry.verification?.length || entry.verification.some((item) =&gt; item.status !== "pass"))) throw new Error(`${prefix}-${id}: canonical coverage verification evidence is absent or not passing`); } }' && deno test -A tests/contract_parity_test.ts</automated></verify>
  <done>The tracer proves one canonical action end to end and every intentionally malformed counterpart blocks before effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust all ten domains and mechanically audit requirement traceability</name>
  <files>runtime/nap_contract_registry.ts, tests/fixtures/v1_1_contract_matrix.json, tests/contract_parity_test.ts, tests/requirement_traceability_test.ts, .planning/phases/09-runtime-expansion-hardening/CONTRACT-PARITY.md</files>
  <behavior>
    - Canonical-minus-registry and registry-minus-canonical are empty for all ten domains.
    - Every advertised row is supported or explicitly denied and has executable evidence; non-advertised rows are provably absent from grants.
    - REQUIREMENTS contains 33 unique IDs, each mapped to one phase with a legal evidence-backed status.
  </behavior>
  <action>Expand the tracer across every request/result/event family and direction in the ten domains. Join registry, manifest grants, bridge routing, specialized RESOURCE/UPLOAD binary routing, service ownership, and named tests. Make D-02's missing/invented/silent cases hard failures. Add a parser test for REQUIREMENTS/ROADMAP ledgers; report current contradictions without prematurely editing completion state, because final reconciliation belongs after all Phase 9 evidence exists. Keep unrelated NAP domains explicitly out of scope and unadvertised.</action>
  <verify><automated>deno test -A tests/contract_parity_test.ts tests/requirement_traceability_test.ts</automated></verify>
  <done>All canonical rows and all 33 requirements are mechanically accounted for; the readable report has zero blocker rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| pinned package→registry | External declarations are authoritative input to generated evidence. |
| manifest grant→dispatcher | Untrusted napplet capability requests enter backend-owned routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering | contract registry | high | mitigate | Exact bidirectional set comparison and generated evidence reject missing/invented rows. |
| T-09-02 | Elevation of privilege | grants/dispatch | high | mitigate | Advertised actions require exact supported handler evidence; all other actions deny or remain unadvertised. |
| T-09-SC | Tampering | pinned dependencies | high | mitigate | No installs occur in this plan; assert exact 0.31.0 locks and preserve package legitimacy evidence. |
</threat_model>

<verification>
Run the focused parity and traceability tests; inspect CONTRACT-PARITY.md for ten domains and zero blockers.
</verification>

<success_criteria>
QLT-01 has exhaustive codec/action/direction evidence and silent handling is release-blocking.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md` when done.
</output>
