---
phase: 09-runtime-expansion-hardening
plan: "06"
type: execute
wave: 6
depends_on: [09-05]
files_modified:
  - deno.json
  - deno.lock
  - playwright.config.ts
  - tests/browser/portal_acceptance_test.ts
autonomous: true
requirements: [QLT-04]
must_haves:
  truths:
    - "A real local Chromium engine automates phone viewports, accessibility/focus, reduced motion, themes, history, reconnect, intent modes, and two-page media ownership."
    - "Browser automation is never represented as physical iOS or Android evidence."
    - "The user-authorized @playwright/test 1.62.1 SUS residual risk is recorded and the dependency is pinned exactly."
  artifacts:
    - path: playwright.config.ts
      provides: "Pinned local-Chromium browser harness"
    - path: tests/browser/portal_acceptance_test.ts
      provides: "Integrated browser acceptance suite"
  key_links:
    - from: tests/browser/portal_acceptance_test.ts
      to: "built Fresh server"
      via: "Playwright webServer/local executable configuration"
---

<objective>
Automate all browser-observable Phase 9 acceptance with a real engine, per D-13 through D-16 and the user's explicit dependency authorization.

Purpose: Verify UI/runtime behavior that string-render tests cannot prove while preserving honest real-device boundaries.
Output: Exact Playwright pin, local Chromium configuration, and browser acceptance suite.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-05-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@deno.json
@islands/NappletShell.tsx
@shell/connection.ts
@shell/theme.ts
@assets/styles.css
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pin Playwright and trace one phone viewport through the built portal</name>
  <reversibility rating="costly">Replacing the browser runner changes configuration and every browser acceptance call site, but not a public product contract.</reversibility>
  <files>deno.json, deno.lock, playwright.config.ts, tests/browser/portal_acceptance_test.ts</files>
  <behavior>
    - The exact @playwright/test 1.62.1 dependency launches the installed local Chromium against a built/start Fresh server.
    - One phone portrait flow loads without horizontal overflow, console errors, CSP violations, or inaccessible primary navigation.
  </behavior>
  <action>Per D-17/D-18, record `USER-AUTHORIZED residual SUS risk` beside the exact dependency decision in execution evidence, then add only `npm:@playwright/test@1.62.1` as a dev dependency and preserve the exact registry tarball/version lock evidence. The user's original autonomous overnight instruction explicitly authorizes this non-interactive evidence-based decision and says never to pause; this is direct authorization, not an inferred workflow default, so no human checkpoint is permitted for this exact pin. Configure the known local Chromium executable with isolated loopback port/startup/cleanup and implement one production-quality phone tracer. Do not download an alternate browser or weaken CSP to make the test pass.</action>
  <verify><automated>deno task build && deno run -A npm:@playwright/test@1.62.1 test tests/browser/portal_acceptance_test.ts --grep "phone tracer"</automated></verify>
  <done>The exact pinned runner drives local Chromium against the built portal and the phone tracer passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Automate the full browser acceptance matrix</name>
  <files>playwright.config.ts, tests/browser/portal_acceptance_test.ts</files>
  <behavior>
    - Portrait and landscape phone viewports cover responsive/safe-area overflow and keyboard-only focus order.
    - System/light/dark and reduced-motion emulation update document presentation without crossing the iframe boundary.
    - Back/Forward, stacked and new-tab intent, blocked/closed popup, reconnect/offline/online/visibility, and two-page media revoke-before-grant are asserted.
    - Accessible names, roles, statuses, dialogs, focus return, and console/CSP cleanliness are checked.
  </behavior>
  <action>Expand the tracer into the locked browser-observable matrix per D-13. Use browser contexts/pages, media emulation, keyboard actions, popup capture, and two pages in one context. Exercise real shell-visible runtime behavior through deterministic backend fixtures or seeded test controls that do not bypass production authorization. Keep physical-device/live-service claims excluded from pass results per D-14 through D-16.</action>
  <verify><automated>deno run -A npm:@playwright/test@1.62.1 test tests/browser/portal_acceptance_test.ts</automated></verify>
  <done>Every locally automatable QLT-04 row passes in Chromium with accessibility, lifecycle, and security assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser harness→built portal | Test controls must not create a production authorization bypass. |
| npm dependency→workspace | Newly pinned SUS package executes in the development environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-09 | Elevation of privilege | test fixtures | high | mitigate | Exercise production authority paths; isolate deterministic setup without shipping bypass routes. |
| T-09-10 | Repudiation | device claims | medium | mitigate | Label Chromium evidence automated and keep physical-device rows separately NOT RUN. |
| T-09-SC | Tampering | @playwright/test@1.62.1 | high | accept | USER-AUTHORIZED residual SUS risk after research verified official registry/repository, high adoption, exact tarball pin, and no reported postinstall; lock exact version and add no other package. |
</threat_model>

<verification>
Build plus the full local Chromium suite passes using the exact lockfile version.
</verification>

<success_criteria>
All browser-observable QLT-04 behavior is automated without overclaiming physical-device evidence.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-06-SUMMARY.md` when done.
</output>
