---
phase: 09-runtime-expansion-hardening
plan: "05"
type: execute
wave: 5
depends_on: [09-02, 09-03, 09-04]
files_modified:
  - tests/lifecycle_matrix_test.ts
  - tests/production_multiclient_smoke_test.ts
  - tests/runtime_reconnect_smoke_test.ts
  - deno.json
autonomous: true
requirements: [QLT-03]
must_haves:
  truths:
    - "Every expanded runtime seam covers normal, empty, partial, stale, denied, timeout, reconnect, replacement, shutdown, and mixed-settlement behavior."
    - "Two or more built-server clients preserve exact ownership, ordering, streaming, reconnect grace, and teardown invariants."
    - "Check, full tests, coverage, build, start, and bounded production smoke all pass without sleep-driven unit races."
  artifacts:
    - path: tests/lifecycle_matrix_test.ts
      provides: "Deterministic domain/lifecycle cross-product"
    - path: tests/production_multiclient_smoke_test.ts
      provides: "Real built Fresh multi-client WebSocket evidence"
  key_links:
    - from: tests/lifecycle_matrix_test.ts
      to: runtime/nap_contract_registry.ts
      via: "every supported row has lifecycle evidence"
    - from: tests/production_multiclient_smoke_test.ts
      to: routes/api/runtime.ts
      via: "built-server WebSocket clients"
---

<objective>
Prove deterministic failure/lifecycle behavior and the real production multi-client transport, per D-09 through D-12.

Purpose: Close the gap between isolated correctness and reconnecting concurrent browser sessions.
Output: Exhaustive lifecycle matrix, built-server multi-client smoke, and release quality commands.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-02-SUMMARY.md
@runtime/nap_contract_registry.ts
@runtime/connections.ts
@runtime/portal_runtime.ts
@routes/api/runtime.ts
@tests/runtime_reconnect_smoke_test.ts
@deno.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one partial stream through reconnect, replacement, and shutdown deterministically</name>
  <files>tests/lifecycle_matrix_test.ts, tests/runtime_reconnect_smoke_test.ts</files>
  <behavior>
    - Initial partial truth is observable before later updates and never waits for global completion.
    - Reconnect within grace preserves logical ownership; replacement and expiry reject stale completion; shutdown leaves no pending work.
  </behavior>
  <action>Choose one representative stream seam and drive it with injected clock, IDs, transport, persistence, and service doubles. Assert ordered partial/stale/updating truth plus exact terminal cleanup without wall-clock sleeps or nested subscription patterns. Retain bounded eventual polling only in the existing real-process smoke, per D-09 through D-12.</action>
  <verify><automated>deno test -A tests/lifecycle_matrix_test.ts --filter "lifecycle tracer" tests/runtime_reconnect_smoke_test.ts</automated></verify>
  <done>The tracer proves stream-first reconnect and teardown semantics with deterministic ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust lifecycle rows and run a two-plus-client built-server scenario</name>
  <files>tests/lifecycle_matrix_test.ts, tests/production_multiclient_smoke_test.ts, tests/runtime_reconnect_smoke_test.ts, deno.json</files>
  <behavior>
    - Every supported registry row links to normal plus applicable empty/partial/stale/denied/timeout/reconnect/replacement/shutdown/mixed-settlement evidence.
    - Two clients prove snapshot/order, stream updates, reconnect token/grace, stale-frame rejection, intent correlation, media revoke-before-grant, detach/expiry, and shutdown.
    - Coverage reports uncovered branches without substituting a percentage for behavioral gates.
  </behavior>
  <action>Complete the registry-to-lifecycle join and add missing deterministic cases. Extend the established isolated production harness: build, bind a validated loopback PORTAL_PORT, spawn start, poll readiness with a deadline, connect at least two WebSocket clients, exercise cross-client ordering and ownership, then terminate and clean up in finally. Add a dedicated task command if useful, while keeping full `deno task test` inclusive. Repair only failures revealed by the matrix in their owning modules and include late Phase 5 fixes in the regression run.</action>
  <verify><automated>deno task check && deno task test && deno test -A --coverage=.coverage && deno coverage .coverage && deno task build && deno test -A tests/production_multiclient_smoke_test.ts</automated></verify>
  <done>All deterministic and real-transport gates pass; zero supported contract/lifecycle rows lack evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reconnecting clients→process runtime | Multiple untrusted sessions contend for process-owned authority. |
| async completion→current owner | Late work may cross replacement or shutdown boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-06 | Spoofing | reconnect ownership | high | mitigate | Multi-client token/grace/replacement/stale-frame assertions. |
| T-09-07 | Denial of service | correlations/transfers | medium | mitigate | Bounded registries, deterministic expiry, cancel, and shutdown cleanup. |
| T-09-08 | Tampering | mixed settlements | medium | mitigate | Preserve ordered per-item/server outcomes and independently terminalize correlations. |
| T-09-SC | Tampering | dependencies | high | mitigate | No installs in this plan; lockfile remains unchanged. |
</threat_model>

<verification>
The complete release command chain passes from formatting through built-server multi-client teardown.
</verification>

<success_criteria>
QLT-03 has behavior-based evidence across every new runtime seam and the production transport.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-05-SUMMARY.md` when done.
</output>
