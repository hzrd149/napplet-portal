---
phase: 09-runtime-expansion-hardening
plan: "04"
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - runtime/security_headers.ts
  - main.ts
  - routes/_app.tsx
  - routes/api/runtime.ts
  - components/NappletFrame.tsx
  - runtime/portal_runtime.ts
  - tests/adversarial_browser_boundary_test.ts
autonomous: true
requirements: [QLT-02]
must_haves:
  truths:
    - "Iframe source/origin, sandbox, CSP, and Permissions-Policy fail closed under hostile browser input."
    - "Foreign and stale frame messages cannot dispatch or receive runtime data."
    - "Every mandatory sandbox and response-policy adversarial row is green before browser lifecycle work begins."
  artifacts:
    - path: tests/adversarial_browser_boundary_test.ts
      provides: "Focused iframe sandbox, source/origin, and response-header evidence"
  key_links:
    - from: components/NappletFrame.tsx
      to: routes/api/runtime.ts
      via: "exact WindowProxy/source binding followed by authenticated generation-bound transport"
---

<objective>
Close browser sandbox, source/origin, and response-header adversarial rows per D-05 through D-08.

Purpose: Prevent hostile frames from bypassing the browser boundary or weakening the shell's response policy.
Output: Focused browser-boundary tests, closed response policy, and remediation in seven declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@components/NappletFrame.tsx
@routes/api/runtime.ts
@runtime/portal_runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a forged iframe source through sandbox and runtime denial</name>
  <files>runtime/security_headers.ts, main.ts, routes/_app.tsx, routes/api/runtime.ts, components/NappletFrame.tsx, runtime/portal_runtime.ts, tests/adversarial_browser_boundary_test.ts</files>
  <behavior>
    - Foreign origin/source and stale window/generation messages cannot dispatch or receive data.
    - Built responses enforce closed CSP and Permissions-Policy while the iframe retains sandbox=allow-scripts without same-origin authority.
  </behavior>
  <action>Trace one forged browser message through exact event.source ownership, origin rules, authenticated socket/window binding, and current generation checks. Add one response-header owner, retain the locked iframe sandbox, and assert zero dispatch/delivery plus sanitized errors per D-05 through D-08.</action>
  <verify><automated>deno test -A tests/adversarial_browser_boundary_test.ts --filter "browser boundary tracer" && deno task build</automated></verify>
  <done>The built shell and tracer prove forged frames cannot gain runtime authority or weaken browser policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust iframe sandbox, source/origin, and response-header rows</name>
  <files>runtime/security_headers.ts, main.ts, routes/_app.tsx, routes/api/runtime.ts, components/NappletFrame.tsx, runtime/portal_runtime.ts, tests/adversarial_browser_boundary_test.ts</files>
  <behavior>
    - Sandbox flags retain script execution without same-origin authority and cannot be weakened by napplet input.
    - CSP and Permissions-Policy remain present and restrictive on the built shell and runtime responses.
    - Origin, WindowProxy source, account, identity, and generation mismatches deny before dispatch and expose only sanitized errors.
  </behavior>
  <action>Expand the closed hostile table across sandbox tokens, response headers, origin/source identity, and stale frame generations per D-05 through D-08. Remediate only the declared browser-boundary owners and preserve the canonical sandbox contract. Completion requires zero mandatory red rows; an externally owned failure is an execution blocker with exact evidence.</action>
  <verify><automated>deno test -A tests/adversarial_browser_boundary_test.ts tests/napplet_frame_test.tsx tests/runtime_transport_test.ts && deno task build</automated></verify>
  <done>All mandatory iframe and response-policy rows pass with no unauthorized dispatch, data delivery, or sensitive output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| iframe→shell/runtime | Browser messages and frame identity are untrusted. |
| server response→browser enforcement | Missing or weakened headers can expand iframe authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-07 | Spoofing | iframe source/runtime window | critical | mitigate | Exact source, origin, account, identity, window, and generation binding. |
| T-09-08 | Elevation of privilege | sandbox/response policy | high | mitigate | Locked sandbox tokens plus restrictive CSP and Permissions-Policy on built responses. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused iframe/header adversarial suites and production build pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 browser sandbox and response-policy boundaries are fail-closed under hostile inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-04-SUMMARY.md` when done.
</output>
