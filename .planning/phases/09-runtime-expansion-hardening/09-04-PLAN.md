---
phase: 09-runtime-expansion-hardening
plan: "04"
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - runtime/security_headers.ts
  - main.ts
  - routes/_app.tsx
  - routes/api/runtime.ts
  - components/NappletFrame.tsx
  - islands/NappletShell.tsx
  - runtime/connections.ts
  - runtime/intent.ts
  - runtime/media_contract.ts
  - runtime/media_reducer.ts
  - runtime/media_sessions.ts
  - runtime/portal_runtime.ts
  - runtime/transport.ts
  - tests/adversarial_browser_lifecycle_test.ts
autonomous: true
requirements: [QLT-02, QLT-03]
must_haves:
  truths:
    - "Iframe source/origin, sandbox, CSP, Permissions-Policy, intent tickets, media ownership, and reconnect generations fail closed under hostile browser input."
    - "Replacement, late completion, duplicate correlation, and shutdown cannot revive stale browser authority."
    - "Every mandatory browser/lifecycle adversarial row is green before lifecycle and browser acceptance waves begin."
  artifacts:
    - path: tests/adversarial_browser_lifecycle_test.ts
      provides: "Focused sandbox, header, intent, media, reconnect, and generation evidence"
  key_links:
    - from: components/NappletFrame.tsx
      to: routes/api/runtime.ts
      via: "exact WindowProxy/source binding followed by authenticated generation-bound transport"
---

<objective>
Close browser sandbox, header, intent, media, reconnect, and generation adversarial rows per D-05 through D-08.

Purpose: Prevent hostile frames and stale tabs from bypassing browser or process-owned lifecycle authority.
Output: Focused browser-boundary tests, closed response policy, and remediation in fourteen declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@components/NappletFrame.tsx
@routes/api/runtime.ts
@runtime/portal_runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a forged iframe source through sandbox and runtime denial</name>
  <files>runtime/security_headers.ts, main.ts, routes/_app.tsx, routes/api/runtime.ts, components/NappletFrame.tsx, runtime/portal_runtime.ts, tests/adversarial_browser_lifecycle_test.ts</files>
  <behavior>
    - Foreign origin/source and stale window/generation messages cannot dispatch or receive data.
    - Built responses enforce closed CSP and Permissions-Policy while the iframe retains sandbox=allow-scripts without same-origin authority.
  </behavior>
  <action>Trace one forged browser message through exact event.source ownership, origin rules, authenticated socket/window binding, and current generation checks. Add one response-header owner, retain the locked iframe sandbox, and assert zero dispatch/delivery plus sanitized errors per D-05 through D-08.</action>
  <verify><automated>deno test -A tests/adversarial_browser_lifecycle_test.ts --filter "browser boundary tracer" && deno task build</automated></verify>
  <done>The built shell and tracer prove forged frames cannot gain runtime authority or weaken browser policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust intent, media, reconnect, replacement, and shutdown rows</name>
  <files>routes/api/runtime.ts, components/NappletFrame.tsx, islands/NappletShell.tsx, runtime/connections.ts, runtime/intent.ts, runtime/media_contract.ts, runtime/media_reducer.ts, runtime/media_sessions.ts, runtime/portal_runtime.ts, runtime/transport.ts, tests/adversarial_browser_lifecycle_test.ts</files>
  <behavior>
    - Intent tickets are bounded, exact-target, single-use, generation-bound, and inert after popup closure, replacement, or expiry.
    - Media revoke/stop precedes grant; stale owners, tabs, results, and generations cannot mutate or resume playback.
    - Duplicate correlations, grace expiry, account change, replacement, late completion, and shutdown terminalize deterministically.
  </behavior>
  <action>Expand the closed hostile table across intent, media, reconnect, replacement, and shutdown. Assert process authority and ordered effects before client presentation; preserve source-bound sandbox parity and stream-oriented partial truth. Remediate all rows only in declared owners. Completion requires zero mandatory red rows; an externally owned failure is an execution blocker with exact evidence.</action>
  <verify><automated>deno test -A tests/adversarial_browser_lifecycle_test.ts tests/intent_runtime_test.ts tests/intent_navigation_test.tsx tests/media_lifecycle_test.ts tests/media_transport_test.ts tests/connection_controller_test.ts && deno task build</automated></verify>
  <done>All mandatory browser, intent, media, reconnect, replacement, and shutdown rows pass with no stale authority or sensitive output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| iframe/tab→shell/runtime | Browser messages and lifecycle observations are untrusted and reorderable. |
| stale async completion→current owner | Late work must not cross generation or account transitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-07 | Spoofing | iframe source/runtime window | critical | mitigate | Exact source, origin, account, identity, window, and generation binding. |
| T-09-08 | Elevation of privilege | intent/media ownership | high | mitigate | Single-use tickets and revoke-before-grant generation reducers. |
| T-09-09 | Tampering | reconnect/replacement | high | mitigate | Current-generation rechecks and deterministic terminal cleanup. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused adversarial suites and production build pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 and relevant QLT-03 browser/lifecycle boundaries are fail-closed under hostile inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-04-SUMMARY.md` when done.
</output>
