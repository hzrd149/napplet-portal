---
phase: 09-runtime-expansion-hardening
plan: "03"
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - runtime/artifacts.ts
  - runtime/catalog.ts
  - runtime/common.ts
  - runtime/signer_service.ts
  - runtime/storage.ts
  - runtime/storage_store.ts
  - runtime/nap_contract_registry.ts
  - runtime/nap_dispatcher.ts
  - runtime/portal_runtime.ts
  - tests/adversarial_authority_state_test.ts
autonomous: true
requirements: [QLT-02, QLT-03]
must_haves:
  truths:
    - "Capabilities, catalog launches, signing, common actions, and storage effects use exact server-derived account, identity, manifest, instance, and generation authority."
    - "Foreign, stale, malformed, over-quota, or cross-scope requests produce zero effects and sanitized errors."
    - "Every mandatory authority/state adversarial row is green before downstream work begins."
  artifacts:
    - path: tests/adversarial_authority_state_test.ts
      provides: "Focused capability, catalog, signer, common, and storage isolation evidence"
  key_links:
    - from: runtime/nap_dispatcher.ts
      to: runtime/portal_runtime.ts
      via: "server-derived exact window capability context"
---

<objective>
Close identity, capability, catalog, signer, common, and storage adversarial rows per D-05 through D-08.

Purpose: Prove a napplet cannot invent authority or cross account, artifact, instance, scope, or signer boundaries.
Output: Focused hostile-input suite and bounded remediation in ten declared owner files.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-runtime-expansion-hardening/09-01-SUMMARY.md
@.planning/phases/09-runtime-expansion-hardening/09-RESEARCH.md
@runtime/nap_contract_registry.ts
@runtime/nap_dispatcher.ts
@runtime/portal_runtime.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace forged capability and identity to zero signer or storage effect</name>
  <files>runtime/signer_service.ts, runtime/storage.ts, runtime/nap_contract_registry.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_authority_state_test.ts</files>
  <behavior>
    - A declared action without the exact verified grant is rejected before signer or storage service invocation.
    - Foreign account, identity, instance, scope, and stale generation variants remain effect-free and sanitized.
  </behavior>
  <action>Trace one request from canonical decoder through registry grant, current window authority, dispatcher, and effect owner. Derive all authority out of band, re-check generation immediately before asynchronous effects, and assert no signer/storage call occurs for every hostile mutation per D-05 through D-08.</action>
  <verify><automated>deno test -A tests/adversarial_authority_state_test.ts --filter "authority tracer"</automated></verify>
  <done>The tracer proves forged or stale client fields cannot create signer or storage effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaust catalog, capability, common, signer, and storage isolation rows</name>
  <files>runtime/artifacts.ts, runtime/catalog.ts, runtime/common.ts, runtime/signer_service.ts, runtime/storage.ts, runtime/storage_store.ts, runtime/nap_contract_registry.ts, runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, tests/adversarial_authority_state_test.ts</files>
  <behavior>
    - Only current exact accepted catalog artifacts confer launch authority; display staleness never preserves authority.
    - Common and signer operations cannot use nsec/client event authority or cross active accounts.
    - Shared/instance storage remains isolated, quota-bounded, deterministic, and unchanged after rejected or failed mutations.
  </behavior>
  <action>Use closed property/table cases for malformed grants, invented actions, foreign artifacts/accounts/instances, stale manifests/generations, signer separation, invalid scopes, quota edges, concurrent mutations, and failed persistence. Remediate every row only in the declared owners while preserving stream-first common data behavior. The task completes only at zero mandatory red rows; an externally owned failure is reported as an execution blocker with exact evidence.</action>
  <verify><automated>deno test -A tests/adversarial_authority_state_test.ts tests/catalog_test.ts tests/catalog_runtime_test.ts tests/signer_service_test.ts tests/storage_test.ts tests/common_test.ts</automated></verify>
  <done>All mandatory identity, catalog, capability, signer, common, and storage rows pass with exact isolation and zero denied effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| napplet claims→process authority | Client-declared identity and grants are untrusted. |
| account/artifact/scope→durable or signing effects | Effects must remain exact-owner isolated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-04 | Elevation of privilege | capability/dispatcher | critical | mitigate | Server-derived exact grants and current generation recheck before effects. |
| T-09-05 | Spoofing | catalog/signer | critical | mitigate | Exact accepted artifact and active-account signer binding. |
| T-09-06 | Information disclosure | storage/common | high | mitigate | Namespace isolation and sanitized projections/errors. |
| T-09-SC | Tampering | dependencies | high | mitigate | No package installs; preserve the lockfile. |
</threat_model>

<verification>
Focused adversarial and existing catalog/signer/storage/common suites pass with zero mandatory red rows.
</verification>

<success_criteria>
QLT-02 and relevant QLT-03 authority/state boundaries are fail-closed under hostile inputs.
</success_criteria>

<output>
Create `.planning/phases/09-runtime-expansion-hardening/09-03-SUMMARY.md` when done.
</output>
