---
phase: 03-mobile-shell-resilience
plan: "03"
type: execute
wave: 3
depends_on: [03-02]
files_modified: [shell/theme.ts, components/ThemeControls.tsx, components/AccountSheet.tsx, routes/_app.tsx, tests/theme_test.ts]
autonomous: true
requirements: [SHL-01]
must_haves:
  truths:
    - "System is the default; explicit System, Light, and Dark preferences persist per browser independently of sign-in and apply immediately (D-33, D-35)."
    - "A validated preference resolves before body paint, System follows OS changes live, and unavailable storage safely falls back to System (D-34, D-35)."
    - "One global browser `theme-color` follows the resolved shell theme and no theme preference crosses the napplet boundary (D-36, D-37)."
  artifacts: [shell/theme.ts, components/ThemeControls.tsx, tests/theme_test.ts]
  key_links:
    - "head pre-paint bootstrap -> validated browser preference -> html data-theme/color-scheme/theme-color"
    - "Account ThemeControls -> shared theme helper -> persistence and live System listener"
---
<objective>Establish the narrow first-paint theme contract implementing D-33, D-34, D-35, D-36, and D-37. Purpose: Resolve browser-local theme correctly before hydration without coupling visual migration or napplet APIs to the preference mechanism. Output: Pure theme helpers, pre-paint bootstrap, explicit controls, and focused tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/03-mobile-shell-resilience/03-CONTEXT.md
@.planning/phases/03-mobile-shell-resilience/03-RESEARCH.md
@.planning/phases/03-mobile-shell-resilience/COVERAGE.md
@.planning/phases/03-mobile-shell-resilience/03-02-SUMMARY.md
<interfaces>
`routes/_app.tsx` owns universal document head/body markup and therefore the one pre-paint bootstrap and global theme-color element.
`AccountSheet` is the shared interactive account/settings surface created by Plan 02; it consumes the new presentational `ThemeControls`.
`shell/theme.ts` exports `ThemePreference`, `ResolvedTheme`, closed-enum validation, resolution, DOM application, and shared storage/meta constants.
</interfaces></context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Resolve and apply the browser-local theme before first paint</name>
  <read_first>routes/_app.tsx, components/AccountSheet.tsx, assets/styles.css, tests/shell_resilience_test.tsx, .planning/phases/03-mobile-shell-resilience/03-CONTEXT.md, .planning/phases/03-mobile-shell-resilience/03-RESEARCH.md</read_first>
  <files>shell/theme.ts, routes/_app.tsx, tests/theme_test.ts</files>
  <behavior>- Missing, inaccessible, or invalid storage resolves to System without breaking render.
- System resolves from `prefers-color-scheme`; Light and Dark resolve directly.
- The head bootstrap runs before body markup and sets one fixed `data-theme`, `color-scheme`, and theme-color value.
- Applying theme never emits a runtime or iframe message.</behavior>
  <action>Start RED with pure resolution and document-bootstrap tests, then create `shell/theme.ts` around one storage key accepting only `system|light|dark`. Wrap persistence access in guarded helpers and apply only fixed `data-theme`, `style.colorScheme`, and predefined theme-color values. Emit the smallest matching script from `_app.tsx` before body paint per D-33 and D-34, retaining System-compatible SSR defaults. Keep the script stable for nonce/hash CSP deployment and do not broaden CSP with an unsafe-inline directive. Own exactly one global `meta[name=theme-color]` per D-36 and prove the module has no runtime/napplet transport dependency per D-37.</action>
  <verify><automated>deno test -A tests/theme_test.ts</automated></verify>
  <acceptance_criteria>Tests prove closed-enum validation, storage exceptions, System/Light/Dark resolution, pre-body execution order, fixed DOM assignments, one global theme-color element, and absence of iframe/runtime theme messages.</acceptance_criteria>
  <done>The initial document paint deterministically reflects the safe browser-local theme preference.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Persist explicit choices and follow System changes live</name>
  <read_first>shell/theme.ts, components/AccountSheet.tsx, islands/NappletShell.tsx, tests/theme_test.ts, tests/shell_resilience_test.tsx, .planning/phases/03-mobile-shell-resilience/03-CONTEXT.md</read_first>
  <files>components/ThemeControls.tsx, components/AccountSheet.tsx, shell/theme.ts, tests/theme_test.ts</files>
  <behavior>- An accessible control exposes exactly System, Light, and Dark and reflects the stored choice.
- Explicit selection applies immediately, persists independently of account state, and updates theme-color.
- A media-query listener exists only in System mode and is removed when preference changes or control unmounts.
- Theme changes are discrete with no animated color interpolation.</behavior>
  <action>Add presentational `ThemeControls` to Account settings and connect it only to the helpers from Task 1. Use an accessible radio/segmented-control contract for the three explicit choices, immediately apply and persist selection per D-33 and D-35, and register `matchMedia("(prefers-color-scheme: dark)")` change handling only while System is active. Clean up the prior listener on selection changes and unmount. Keep the controls independent of sign-in/signer state and do not migrate broad route/component colors in this plan; Plan 04 consumes the stable `data-theme` contract.</action>
  <verify><automated>deno test -A tests/theme_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Tests cover all choices, persistence, immediate DOM/meta updates, System live changes, listener replacement/cleanup, inaccessible storage, account-independent availability, and no animated color-transition contract.</acceptance_criteria>
  <done>Users can reliably select or follow System theme through one reusable browser-local control.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser persistence/OS preference → document theme | Untrusted stored strings and environment signals alter shell presentation. |
| inline head bootstrap → CSP/document | First-party script executes before hydration and must not weaken script policy. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09 | Tampering | stored theme preference | medium | mitigate | Accept only the closed enum and apply fixed attributes/colors, never stored markup or CSS text. |
| T-03-10 | Tampering | pre-paint bootstrap/CSP | high | mitigate | Keep a stable tiny first-party script compatible with nonce/hash and prohibit broad unsafe-inline policy changes. |
| T-03-12 | Information Disclosure | browser preference | low | accept | Theme stays in same-origin browser persistence and is never sent to backend or napplet frames. |
</threat_model>
## Artifacts this phase produces
- `ThemePreference`/`ResolvedTheme` validation, resolution, persistence, and DOM application helpers
- One first-paint bootstrap and global theme-color owner
- Accessible `ThemeControls` with live System observation
<verification>`deno test -A tests/theme_test.ts` and `deno task check` pass.</verification>
<success_criteria>The browser-local SHL-01 infrastructure is proven independently before any broad shell-surface migration.</success_criteria>
<output>Create `.planning/phases/03-mobile-shell-resilience/03-03-SUMMARY.md` when done.</output>
