---
phase: 01-one-day-napplet-runtime-mvp
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [runtime/account_store.ts, runtime/accounts.ts, tests/account_store_test.ts, tests/accounts_test.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, NAP-02, QUAL-02]
must_haves:
  truths:
    - "D-01/D-02: Nostr Connect is primary; bunker and Not recommended nsec paths work without browser key exposure"
    - "D-04/D-05: every account and complete signer state plus active ID persist in a sensitive host-protected snapshot"
    - "D-06/D-07/D-08: newest login is globally active; offline restore remains active; sign-out removes signing authority but not public reads"
  artifacts:
    - { path: runtime/account_store.ts, provides: versioned sensitive account snapshot adapter }
    - { path: runtime/accounts.ts, provides: process-wide account/sign-in/identity service }
  key_links:
    - { from: runtime/accounts.ts, to: runtime/account_store.ts, via: serialized mutation queue }
---

<objective>Deliver the server-owned account vertical slice and browser-safe identity projection, preserving D-01–D-08 exactly.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-one-day-napplet-runtime-mvp/01-CONTEXT.md
@.planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md
@runtime/transport.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Persist and restore the complete Applesauce account snapshot</name>
  <files>runtime/account_store.ts, tests/account_store_test.ts</files>
  <read_first>runtime/transport.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:293; .planning/phases/01-one-day-napplet-runtime-mvp/01-CONTEXT.md</read_first>
  <behavior>- Snapshot is `{version, activeAccountId, accounts}`. - Writes serialize, use a sibling temporary file then rename, and request restrictive permissions. - Corrupt/unknown records fail visibly without logging content. - Serialized signer material never enters transport objects.</behavior>
  <action>Implement D-04/D-05 using Applesauce serialization after registering Nostr Connect and private-key account types. Persist full signer state and active selection; document/store it as sensitive at-rest data. Keep the adapter server-only and do not add an ORM or SQL schema. Carry the costly reversibility record because changing this representation later requires sensitive-record migration.</action>
  <reversibility rating="costly">Changing the persisted account representation later requires migrating sensitive signer records.</reversibility>
  <verify><automated>deno test -A tests/account_store_test.ts</automated></verify>
  <acceptance_criteria>Round-trip preserves all accounts, NIP-46 client material, nsec account material, and active ID; file content never appears in errors/logs.</acceptance_criteria>
  <done>Account snapshot read/write seam is atomic, serialized, and sensitive.</done>
</task>
<task type="auto" tdd="true">
  <name>Run all sign-in paths and broadcast one global identity</name>
  <files>runtime/accounts.ts, tests/accounts_test.ts</files>
  <read_first>runtime/account_store.ts; ../hyprgate-gui/apps/shell/src/lib/auth/auth-actions.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-UI-SPEC.md</read_first>
  <behavior>- Nostr Connect creates a QR/copy URI; bunker and nsec complete server-side. - Newest success becomes active and persists. - Restored unavailable NIP-46 remains active/offline and retries. - Sign-out broadcasts unavailable and signing calls fail while public reads continue. - Browser projection contains only public identity/status.</behavior>
  <action>Implement D-01–D-08 with process-wide `AccountManager`, signer classes, active-state observable, safe public projection, and commands matching runtime transport. Nostr Connect leads; bunker and nsec remain normal secondary choices, with nsec never placed in URL/log/browser storage. Broadcast changes to all connections. AUTH-05 is satisfied by documenting read-only account mode as explicitly deferred, not by implementing it.</action>
  <verify><automated>deno test -A tests/accounts_test.ts tests/account_store_test.ts</automated></verify>
  <acceptance_criteria>All three sign-in paths, active switching, offline restore, sign-out, persistence, and cross-connection identity broadcast pass without secret transport.</acceptance_criteria>
  <done>One backend account authority serves every subordinate tab/window.</done>
</task>
</tasks>

## Artifacts this phase produces
- `runtime/account_store.ts`: `AccountSnapshot`, `AccountStore`.
- `runtime/accounts.ts`: `PortalAccounts`, public `IdentitySnapshot`, sign-in/sign-out commands.
- Account persistence and lifecycle tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| signer/account store→browser | Secret signer state must become public identity projection only |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Information disclosure | account snapshot | high | mitigate | Server-only types, restrictive permissions, redacted errors/logs, structural no-secret transport tests |
| T-01-05 | Elevation of privilege | sign operations | high | mitigate | Active backend account is sole signer authority; unavailable/sign-out rejects signing |
| T-01-06 | Tampering | snapshot write | medium | mitigate | Serialized queue and temp-file atomic rename |
| T-01-SC | Tampering | npm installs | high | mitigate | Dependency install already gated by Plan 01 |
</threat_model>
<verification>`deno task check && deno test -A tests/account_store_test.ts tests/accounts_test.ts`.</verification>
<success_criteria>Accounts survive restart, identity is global, and no signer secret crosses the browser boundary.</success_criteria>
<output>Create `.planning/phases/01-one-day-napplet-runtime-mvp/01-02-SUMMARY.md`.</output>
