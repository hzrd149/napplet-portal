---
phase: 01-one-day-napplet-runtime-mvp
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified: [runtime/artifacts.ts, runtime/connections.ts, routes/api/runtime.ts, tests/artifact_resolver_test.ts, tests/websocket_session_test.ts]
autonomous: true
requirements: [MVP-02, MVP-04, MVP-05, STREAM-07, NAP-01, NAP-04, QUAL-02]
must_haves:
  truths:
    - "D-16/D-17/D-18/D-19: only a verified configured artifact can launch; cache/version and retry semantics are explicit"
    - "D-31/D-32/D-33/D-34: one socket per tab owns reconnectable window/subscription namespaces over shared process state"
    - "D-20/D-21: verified identity is bound before exact opaque srcdoc handshake"
  artifacts:
    - { path: runtime/artifacts.ts, provides: NIP-5A/5D resolution and verified cache adapter }
    - { path: runtime/connections.ts, provides: connection/window/grace ownership registry }
    - { path: routes/api/runtime.ts, provides: Fresh WebSocket boundary }
  key_links:
    - { from: routes/api/runtime.ts, to: runtime/connections.ts, via: attach/receive/detach-with-grace }
---
<objective>Expand Plan 01's verified-artifact and owned-session tracer into the complete locked resolver, cache, and reconnect lifecycle.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@runtime/config.ts
@runtime/transport.ts
@../kehto/packages/nip/src/5d/index.ts
@.planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Resolve, verify, cache, and assemble the configured napplet</name>
  <files>runtime/artifacts.ts, tests/artifact_resolver_test.ts</files>
  <read_first>runtime/config.ts; tests/fixtures/supplied_napplet_contract.json; ../kehto/packages/nip/src/5d/index.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:210; .planning/phases/01-one-day-napplet-runtime-mvp/COVERAGE.md</read_first>
  <behavior>- Resolve signed manifest from configured relays and fetch from manifest hints plus configured Blossom servers. - Verify signature, aggregate and blobs before cache/write/browser bytes. - Fail closed for integrity or missing required capability. - Hold resolved version until restart or explicit retry. - Missing coordinate returns configured-empty state.</behavior>
  <action>Expand Plan 01's production resolver path to implement D-16–D-19, D-22, D-42–D-45 from the actual supplied-napplet fixture with `resolveNapplet`, an injected in-memory `NappletArtifactCache`, and the empirically verified `srcdoc` asset strategy. Only SHELL/IDENTITY/RELAY/OUTBOX can be granted per the phase COVERAGE.md. Local endpoints are ordinary sources. Do not author a napplet or synthetic fixture. Carry D-16's costly identity contract and D-47's full Phase 1 scope without a one-day deadline.</action>
  <reversibility rating="costly">Verified dTag/aggregate identity keys the session and later capability policy.</reversibility>
  <verify><automated>deno test -A tests/artifact_resolver_test.ts</automated></verify>
  <acceptance_criteria>Valid supplied fixtures resolve; altered signature/blob/aggregate and missing capabilities yield named failures with no executable HTML.</acceptance_criteria>
  <done>Only verified compatible content reaches the srcdoc seam.</done>
</task>
<task type="auto" tdd="true">
  <name>Own each tab/window through a reconnectable Fresh WebSocket</name>
  <files>runtime/connections.ts, routes/api/runtime.ts, tests/websocket_session_test.ts</files>
  <read_first>runtime/transport.ts; main.ts; utils.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:41; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:103</read_first>
  <behavior>- Server creates connection/window namespaces. - Reconnect token reattaches within grace and re-registers active logical subscriptions with original subIds. - Expiry destroys only that connection's windows. - Foreign ownership, malformed/oversized messages, and recognized timeouts are handled safely.</behavior>
  <action>Implement D-23 and D-31–D-35. Use one duplex socket per tab; model disconnect as detached state with configurable grace, idempotent cleanup, and ownership deletion before unsubscribe. The process-wide runtime/account remains primary; tabs are subordinate connection/window instances (`no-change`). Default unauthenticated exposure stays loopback-only per D-35; broader bind must be explicit.</action>
  <reversibility rating="costly">Adding multi-user portal authentication later changes every session ownership boundary.</reversibility>
  <verify><automated>deno test -A tests/websocket_session_test.ts</automated></verify>
  <acceptance_criteria>Reconnect neither leaks nor duplicates subscriptions; cross-tab namespace claims fail; timeout retains original ID; grace expiry is connection-scoped.</acceptance_criteria>
  <done>Verified napplet windows have a live, owned backend transport.</done>
</task>
</tasks>

## Artifacts this phase produces
- `runtime/artifacts.ts`: `ArtifactAdapter`, verified resolution/cache types.
- `runtime/connections.ts`: connection/window/subscription registry.
- `routes/api/runtime.ts`: WebSocket endpoint.
- Artifact and connection integration tests.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| relay/Blossom→artifact | Network bytes are untrusted until signature/hash verification |
| WebSocket→runtime | Unauthenticated loopback client messages claim runtime operations |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Tampering | artifact resolver | high | mitigate | Verify manifest signature, aggregate and every blob; fail closed before srcdoc |
| T-01-08 | Spoofing | connection registry | high | mitigate | Server-issued IDs and connection/window ownership checks |
| T-01-09 | Information disclosure | server exposure | high | mitigate | Default `127.0.0.1`; explicit override for LAN/public exposure |
| T-01-10 | Denial of service | session lifecycle | medium | mitigate | Size/type validation, correlation timeout, idempotent grace cleanup |
| T-01-SC | Tampering | npm installs | high | mitigate | Dependency install already gated by Plan 01 |
</threat_model>
<verification>`deno task check && deno test -A tests/artifact_resolver_test.ts tests/websocket_session_test.ts`.</verification>
<success_criteria>A verified artifact owns a safe reconnectable tab/window session; unverifiable content never executes.</success_criteria>
<output>Create `.planning/phases/01-one-day-napplet-runtime-mvp/01-03-SUMMARY.md`.</output>
