---
phase: 01-one-day-napplet-runtime-mvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [deno.json, deno.lock, runtime/config.ts, runtime/transport.ts, tests/runtime_contract_test.ts, tests/config_test.ts]
autonomous: false
requirements: [MVP-03, MVP-04, MVP-05, STREAM-07, NAP-01, NAP-04, QUAL-03]
must_haves:
  truths:
    - "D-21/D-22: coherent package imports expose exact SHELL, IDENTITY, RELAY, and OUTBOX envelopes without mixing sibling versions"
    - "D-41/D-42/D-43/D-44/D-46: startup configuration is immutable, validated, loopback-safe, and summarized without secrets"
    - "Recognized failures retain correlation IDs; unknown types are silent (D-23)"
  artifacts:
    - { path: runtime/config.ts, provides: immutable validated startup configuration }
    - { path: runtime/transport.ts, provides: browser/backend discriminated transport codec }
    - { path: tests/runtime_contract_test.ts, provides: exact package-envelope and srcdoc seam contract tests }
  key_links:
    - { from: runtime/transport.ts, to: "@napplet/nap", via: imported canonical wire types }
---

## Phase Goal

**As a** trusted single operator, **I want to** sign in and run one verified sandboxed napplet backed by live Nostr streams, **so that** a mobile browser can stay lightweight while the Deno server owns runtime authority.

<objective>
Prove the dependency, envelope, supplied-artifact `srcdoc`, configuration, test, and local-run seams before broader implementation. This is the production-quality tracer skeleton for D-21, D-22, D-23, and D-41–D-46.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md
@.planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md
@../kehto/packages/runtime/src/relay-handler.ts
@../napplet-web/packages/nap/src/relay/types.ts
@../napplet-web/packages/nap/src/outbox/types.ts
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify pinned low-volume packages before installation</name>
  <what-built>A researched, exact package/version set with registry audit evidence; no packages are installed before approval.</what-built>
  <how-to-verify>Open each named npmjs.com package page, confirm the exact version, publisher/source repository, and absence of suspicious substitution, then compare against the Package Legitimacy Audit.</how-to-verify>
  <read_first>.planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md</read_first>
  <action>Verify registry ownership/source links and the exact pinned versions for applesauce-accounts/core/relay/signers, @kehto/runtime/shell/services/nip, @napplet/core/nap, and nostr-tools. Confirm no suspicious package substitution; this checkpoint is required by the Package Legitimacy Audit.</action>
  <verify><automated>deno eval 'const p=["applesauce-accounts@6.2.0","applesauce-core@6.2.0","applesauce-relay@6.2.1","applesauce-signers@6.2.2","@kehto/runtime@0.20.1","@kehto/shell@0.19.1","@kehto/services@0.18.1","@kehto/nip@0.5.1","@napplet/core@0.31.0","@napplet/nap@0.31.0","nostr-tools@2.24.1"]; if(p.length!==11) Deno.exit(1)'</automated><human-check>Confirm each package on npmjs.com and approve the pinned set.</human-check></verify>
  <acceptance_criteria>Human approval names the checked package set and versions.</acceptance_criteria>
  <done>Package legitimacy gate approved.</done>
  <resume-signal>Type "approved" or name any package/version that must be rejected.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Prove coherent imports, exact envelopes, supplied srcdoc seam, and local commands</name>
  <files>deno.json, deno.lock, runtime/config.ts, runtime/transport.ts, tests/runtime_contract_test.ts, tests/config_test.ts</files>
  <read_first>deno.json; ../kehto/RUNTIME-SPEC.md; ../kehto/packages/shell/README.md; ../napplet-web/packages/nap/src/relay/types.ts; ../napplet-web/packages/nap/src/outbox/types.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md:157</read_first>
  <behavior>- Import the coherent published versions without browser-global evaluation on the server. - Freeze exact ready/init, relay, identity, outbox, error, EOSE and closed shapes. - Assemble a supplied verified artifact's HTML/files through a typed srcdoc input seam without authoring an example napplet. - Parse environment once; normalize/dedupe valid endpoints, default bind to 127.0.0.1, and sanitize summary output.</behavior>
  <action>After approval, pin the researched coherent dependency line and add `test` plus focused check tasks. Implement immutable `RuntimeConfig` and validated transport unions. Tests must use canonical sibling types as read-only references and a caller-supplied artifact fixture seam; per D-21/D-22 do not recreate NAP shapes or add an example napplet. Implement D-23 correlated typed timeouts and silent unknowns. Implement D-41–D-46 including built-in fallbacks, ordinary local endpoints, warnings for rejected URLs, unbounded in-memory cache settings, and sanitized startup fields. Record `no-change`: the runtime/account is process-wide and tabs are subordinate namespaces per D-07/D-33/D-35.</action>
  <verify><automated>deno task check &amp;&amp; deno test -A tests/runtime_contract_test.ts tests/config_test.ts</automated></verify>
  <acceptance_criteria>All pinned imports resolve; canonical fixtures pass; no local sibling is modified; configuration rejects unsafe schemes and defaults to loopback; supplied srcdoc assembly is callable.</acceptance_criteria>
  <done>The compatibility tracer runs through the real local check/test path.</done>
</task>
</tasks>

## Artifacts this phase produces

- `runtime/config.ts`: `RuntimeConfig`, `loadRuntimeConfig`, endpoint normalization.
- `runtime/transport.ts`: correlated connection/window command and event codecs.
- `tests/runtime_contract_test.ts`, `tests/config_test.ts`: compatibility and configuration gates.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| environment→runtime | Untrusted endpoint and bind configuration enters authority-bearing server code |
| iframe/WebSocket→codec | Untrusted messages cross into backend dispatch |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | transport codec | high | mitigate | Server-issued ownership fields; reject caller-selected foreign connection/window IDs |
| T-01-02 | Information disclosure | config/logging | high | mitigate | Loopback default and allowlisted startup summary; no credentials or signer material |
| T-01-03 | Tampering | endpoint parsing | medium | mitigate | Scheme validation, normalization, dedupe, and explicit rejected-value warnings |
| T-01-SC | Tampering | npm installs | high | mitigate | Blocking human legitimacy checkpoint before installation |
</threat_model>

<verification>Run `deno task check && deno task test`; inspect dependency graph for one coherent package line.</verification>
<success_criteria>Wave 0 proves imports, exact contracts, supplied-artifact assembly, and local execution before feature expansion.</success_criteria>
<output>Create `.planning/phases/01-one-day-napplet-runtime-mvp/01-01-SUMMARY.md`.</output>
