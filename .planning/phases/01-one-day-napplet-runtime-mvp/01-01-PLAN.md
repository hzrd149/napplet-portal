---
phase: 01-one-day-napplet-runtime-mvp
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [deno.json, deno.lock, runtime/config.ts, runtime/transport.ts, runtime/accounts.ts, runtime/artifacts.ts, runtime/connections.ts, runtime/relay_adapter.ts, runtime/portal_runtime.ts, routes/index.tsx, routes/api/runtime.ts, islands/NappletShell.tsx, components/NappletFrame.tsx, tests/runtime_contract_test.ts, tests/config_test.ts, tests/tracer_end_to_end_test.ts, tests/fixtures/supplied_napplet_contract.json]
autonomous: false
requirements: [MVP-01, MVP-02, MVP-03, MVP-04, MVP-05, AUTH-01, AUTH-03, STREAM-01, STREAM-02, STREAM-04, STREAM-05, STREAM-07, NAP-01, NAP-02, NAP-03, NAP-04, QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "D-47: the first delivery checkpoint lets a user sign in, launch the actual supplied verified napplet, complete its Kehto handshake, and observe an initial event plus a later live update from a backend-proxied stream"
    - "D-16/D-20/D-21/D-22: the supplied artifact, not an authored substitute, runs in an opaque `sandbox=allow-scripts` iframe using its exact NAP 0.31 envelopes"
    - "D-23/D-24/D-31: the narrow tracer crosses a source-bound browser bridge, owned backend connection, and continuing stream with correlated messages"
    - "D-41/D-42/D-43/D-44/D-46: tracer configuration is immutable, validated, loopback-safe, and summarized without secrets"
  artifacts:
    - { path: runtime/config.ts, provides: immutable validated startup configuration }
    - { path: runtime/transport.ts, provides: browser/backend discriminated transport codec }
    - { path: tests/fixtures/supplied_napplet_contract.json, provides: evidence derived from the actual supplied artifact including srcdoc assets and exact 0.31 envelopes }
    - { path: tests/tracer_end_to_end_test.ts, provides: executable sign-in through later live-update checkpoint }
  key_links:
    - { from: runtime/transport.ts, to: "@napplet/nap", via: imported canonical wire types }
    - { from: islands/NappletShell.tsx, to: routes/api/runtime.ts, via: one source-bound WebSocket proxy }
    - { from: runtime/portal_runtime.ts, to: runtime/relay_adapter.ts, via: continuing initial-plus-live observable }
---

## Phase Goal

**As a** trusted single operator, **I want to** sign in and run one verified sandboxed napplet backed by live Nostr streams, **so that** a mobile browser can stay lightweight while the Deno server owns runtime authority.

<objective>
Deliver D-47's genuine first vertical checkpoint, targeted for one day: sign in through the minimal lead path, resolve and verify the actual supplied napplet, launch it in `sandbox="allow-scripts"`, complete its Kehto handshake, and deliver both an initial event and a later live update through the backend-proxied stream. The production-quality narrow implementations in this plan are expanded—not replaced—by Plans 02–06, and full Phase 1 completion has no one-day deadline.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md
@.planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md
@../kehto/packages/runtime/src/relay-handler.ts
@../napplet/packages/nap/src/relay/types.ts
@../napplet/packages/nap/src/outbox/types.ts
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify pinned low-volume packages before installation</name>
  <what-built>A researched, exact package/version set with registry audit evidence; no packages are installed before approval.</what-built>
  <how-to-verify>Open each named npmjs.com package page, confirm the exact version, publisher/source repository, and absence of suspicious substitution, then compare against the Package Legitimacy Audit.</how-to-verify>
  <read_first>.planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md</read_first>
  <action>Verify registry ownership/source links and the exact pinned versions for applesauce-accounts/core/relay/signers, @kehto/runtime/shell/services/nip, @napplet/core/nap, and nostr-tools. Confirm no suspicious package substitution; this checkpoint is required by the Package Legitimacy Audit.</action>
  <verify><automated>deno eval 'const p=["applesauce-accounts@6.2.0","applesauce-core@6.2.0","applesauce-relay@6.2.1","applesauce-signers@6.2.2","@kehto/runtime@0.20.1","@kehto/shell@0.19.1","@kehto/services@0.18.1","@kehto/nip@0.5.1","@napplet/core@0.31.0","@napplet/nap@0.31.0","nostr-tools@2.24.1"]; if(p.length!==11) Deno.exit(1)'</automated><human-check>Confirm each package on npmjs.com and approve the pinned set.</human-check></verify>
  <acceptance_criteria>Human approval names the checked package set and versions.</acceptance_criteria>
  <done>Package legitimacy gate approved.</done>
  <resume-signal>Type "approved" or name any package/version that must be rejected.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Supply the actual napplet coordinate and verified artifact</name>
  <what-built>The approved dependency set and a precise input contract for the real tracer artifact.</what-built>
  <how-to-verify>Provide the actual configured NIP-5A coordinate and resolvable artifact, or its verified artifact bundle plus identity/hash evidence. Confirm it is the intended test napplet and is not a project-authored example or synthetic substitute.</how-to-verify>
  <read_first>.planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md:477; .planning/phases/01-one-day-napplet-runtime-mvp/COVERAGE.md; ../napplet/packages/nap/src/relay/types.ts; ../napplet/packages/nap/src/outbox/types.ts</read_first>
  <action>Require and record the exact user-provided coordinate/artifact input before any artifact-derived fixture or vertical success claim. Do not accept an authored example, generated test napplet, invented envelope, or unverified bundle.</action>
  <verify><automated>deno eval 'const coordinate=Deno.env.get("NAPPLET_COORDINATE"); const bundle=Deno.env.get("NAPPLET_ARTIFACT_PATH"); if(!coordinate &amp;&amp; !bundle) Deno.exit(1)'</automated><human-check>Confirm the supplied coordinate/artifact is the user's intended napplet.</human-check></verify>
  <acceptance_criteria>The executor has an exact resolvable coordinate or verified artifact bundle for the user's intended napplet.</acceptance_criteria>
  <done>The real artifact input is available for executable derivation.</done>
  <resume-signal>Provide the coordinate/artifact and type "supplied", or report why it cannot be provided.</resume-signal>
</task>
<task type="tracer" tdd="true">
  <name>Derive the supplied contract and prove sign-in → verified iframe → updating backend stream</name>
  <files>deno.json, deno.lock, runtime/config.ts, runtime/transport.ts, runtime/accounts.ts, runtime/artifacts.ts, runtime/connections.ts, runtime/relay_adapter.ts, runtime/portal_runtime.ts, routes/index.tsx, routes/api/runtime.ts, islands/NappletShell.tsx, components/NappletFrame.tsx, tests/runtime_contract_test.ts, tests/config_test.ts, tests/tracer_end_to_end_test.ts, tests/fixtures/supplied_napplet_contract.json</files>
  <read_first>deno.json; ../kehto/RUNTIME-SPEC.md; ../kehto/packages/shell/README.md; ../napplet/packages/nap/src/relay/types.ts; ../napplet/packages/nap/src/outbox/types.ts; .planning/phases/01-one-day-napplet-runtime-mvp/01-RESEARCH.md:157; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:41; .planning/phases/01-one-day-napplet-runtime-mvp/01-PATTERNS.md:230</read_first>
  <behavior>- Resolve the user-provided artifact, derive `tests/fixtures/supplied_napplet_contract.json`, and record its identity/hash, `srcdoc` relative-asset behavior, handshake messages, and exact NAP 0.31 RELAY/OUTBOX envelopes. - A minimal lead sign-in activates a backend-owned account and exposes only its safe public identity to the shell. - The verified artifact launches in exactly `sandbox="allow-scripts"`, sends `shell.ready`, receives one `shell.init`, and crosses a source-bound bridge. - A backend-owned RELAY subscription sends one initial event and, without completing after initial delivery, a later live update to the same napplet session.</behavior>
  <action>Immediately after the supplied-artifact checkpoint, first write the failing fixture/contract and end-to-end tests, then derive the real fixture from that artifact and make the tracer pass. After the package approval, pin the coherent researched versions and add focused test tasks. Implement the narrowest production-quality path through immutable loopback-safe config, canonical package types, one minimal lead sign-in, verified artifact resolution, opaque `srcdoc` iframe, source-bound postMessage bridge, one owned WebSocket session, Kehto handshake, and one backend-proxied continuing RELAY stream. Use the actual artifact's recorded asset strategy and exact 0.31 envelopes; treat mismatches as blocking compatibility failures. Deliver an initial stored/seed event followed by a separately emitted live update through the same observable/subscription, without awaiting stream completeness. Per D-47 this executable user path is the first delivery checkpoint; Plans 02–06 expand the same modules to every remaining locked account, artifact, reconnect, UI, relay/outbox, and acceptance behavior. Do not create an example or synthetic napplet.</action>
  <verify><automated>deno task check &amp;&amp; deno test -A tests/runtime_contract_test.ts tests/config_test.ts tests/tracer_end_to_end_test.ts</automated></verify>
  <acceptance_criteria>The real fixture exists and passes exact contract checks; the test launches only the supplied verified artifact in `sandbox="allow-scripts"`; minimal sign-in reaches active public identity; the artifact completes the Kehto handshake; and the same backend stream delivers an initial event plus a later live update.</acceptance_criteria>
  <done>D-47's first vertical checkpoint works end to end with the actual supplied napplet and no substitute artifact.</done>
</task>
</tasks>

## Artifacts this phase produces

- `runtime/config.ts`: `RuntimeConfig`, `loadRuntimeConfig`, endpoint normalization.
- `runtime/transport.ts`: correlated connection/window command and event codecs.
- `tests/fixtures/supplied_napplet_contract.json`: artifact identity, srcdoc asset behavior, and exact NAP 0.31 wire evidence derived from the user-provided artifact.
- `runtime/accounts.ts`, `runtime/artifacts.ts`, `runtime/connections.ts`, `runtime/relay_adapter.ts`, `runtime/portal_runtime.ts`: narrow production path later expanded by Plans 02, 03, and 05.
- `routes/index.tsx`, `routes/api/runtime.ts`, `islands/NappletShell.tsx`, `components/NappletFrame.tsx`: minimal real shell/iframe/proxy path later expanded by Plan 04.
- `tests/runtime_contract_test.ts`, `tests/config_test.ts`, `tests/tracer_end_to_end_test.ts`: compatibility, configuration, supplied-fixture, and vertical checkpoint gates.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| environment→runtime | Untrusted endpoint and bind configuration enters authority-bearing server code |
| supplied artifact→iframe | User-provided bytes and recorded contract evidence enter an opaque-origin execution boundary |
| iframe/WebSocket→codec | Untrusted messages cross into backend dispatch |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | transport codec | high | mitigate | Server-issued ownership fields; reject caller-selected foreign connection/window IDs |
| T-01-02 | Information disclosure | config/logging | high | mitigate | Loopback default and allowlisted startup summary; no credentials or signer material |
| T-01-03 | Tampering | endpoint parsing | medium | mitigate | Scheme validation, normalization, dedupe, and explicit rejected-value warnings |
| T-01-04 | Tampering | supplied artifact/fixture | high | mitigate | Verify artifact identity/hash, derive the fixture from those bytes, and fail on contract mismatch before iframe launch |
| T-01-SC | Tampering | npm installs | high | mitigate | Blocking human legitimacy checkpoint before installation |
</threat_model>

<verification>Run `deno task check && deno test -A tests/runtime_contract_test.ts tests/config_test.ts tests/tracer_end_to_end_test.ts`; inspect the derived fixture and test trace for sign-in, verified iframe, handshake, initial event, and later live update.</verification>
<success_criteria>Wave 0 cannot complete until the user supplies the actual artifact, executable derivation writes and validates its fixture, and the genuine end-to-end vertical tracer passes.</success_criteria>
<output>Create `.planning/phases/01-one-day-napplet-runtime-mvp/01-01-SUMMARY.md`.</output>
