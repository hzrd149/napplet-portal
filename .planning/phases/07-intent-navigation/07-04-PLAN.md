---
phase: 07-intent-navigation
plan: 04
type: execute
wave: 4
depends_on: [07-02, 07-03]
files_modified:
  - main.ts
  - routes/api/runtime.ts
  - routes/intent/reserved.tsx
  - islands/IntentReservation.tsx
  - routes/index.tsx
  - tests/intent_production_test.ts
  - tests/runtime_reconnect_smoke_test.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "Production constructs one process-owned IntentService from the same CatalogService and exposes exact INTENT traffic only on authenticated runtime windows."
    - "A production build/start WebSocket path proves handlers, invocation, private ticket claim, and correlated settlement without dev-server upgrade assumptions."
    - "Reconnect, replacement, and teardown cannot replay navigation, leak tickets, or preserve stale handler authority."
  artifacts:
    - path: main.ts
      provides: "Single process-owned CatalogService/IntentService composition"
    - path: routes/api/runtime.ts
      provides: "Production WebSocket INTENT projection, dispatch, navigation ack, and ticket claim routing"
    - path: tests/intent_production_test.ts
      provides: "Production-server end-to-end intent tracer"
    - path: islands/IntentReservation.tsx
      provides: "CSP-compatible opener-sever-first reservation and ticket-claim bootstrap"
  key_links:
    - from: main.ts
      to: runtime/portal_runtime.ts
      via: "shared CatalogService and IntentService lifecycle"
    - from: routes/api/runtime.ts
      to: islands/NappletShell.tsx
      via: "owned WebSocket runtime event/command/ack correlation"
    - from: routes/intent/reserved.tsx
      to: runtime/intent.ts
      via: "opener-sever-first target bootstrap followed by exact reservationId/ticket claim over authenticated runtime"
    - from: tests/intent_production_test.ts
      to: "deno task build && deno task start"
      via: "validated loopback PORTAL_PORT production transport"
---

<objective>
Wire and prove the complete Phase 7 capability in the production Fresh runtime.

Purpose: Close the last integration risks across process ownership, WebSocket routing, browser correlation, reconnect, and same-origin ticket entry.
Output: Production composition/route wiring and focused production/reconnect verification.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-intent-navigation/07-02-SUMMARY.md
@.planning/phases/07-intent-navigation/07-03-SUMMARY.md
@.planning/phases/07-intent-navigation/07-CONTEXT.md
@main.ts
@routes/api/runtime.ts
@routes/index.tsx
@tests/runtime_reconnect_smoke_test.ts

<interfaces>
Consume the completed IntentService, runtime hub, strict transport messages, and shell controller from plans 07-01 through 07-03.
Production WebSocket upgrades are exercised only by `deno task build && deno task start`, using validated loopback `PORTAL_PORT`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace pinned INTENT over the production WebSocket and same-origin ticket route</name>
  <files>main.ts, routes/api/runtime.ts, routes/index.tsx, tests/intent_production_test.ts</files>
  <behavior>
    - Production uses one IntentService derived from the process-owned CatalogService and active-account/runtime lifecycle.
    - An authenticated window queries handlers, invokes one verified accepted handler, receives a constrained shell command, acknowledges it, claims its private ticket once after target readiness, and receives one canonical result.
    - Foreign owners, unknown correlations, replayed/expired claims, and non-current targets are rejected without payload disclosure.
    - Production and fixture tracers carry identical frozen archetype declarations from exact signed tags into VerifiedCatalogArtifact and IntentService.
    - The reservation route severs opener before transport/ticket claim and uses CSP-compatible external/static bootstrap with the existing opaque-origin napplet iframe sandbox unchanged.
  </behavior>
  <action>Construct one process-owned `IntentService` from the same CatalogService produced through `createProductionCatalogResolver` and `createPortalRuntime`; assert that frozen signed-tag declarations reach VerifiedCatalogArtifact, registry availability, and invocation without re-decoding. Extend `/api/runtime` with the exact reserve/authorized/ack/claim codecs and owner/generation checks. Trace source-bound message, synchronous reservation, runtime.forward authorization, opener-driven commit, one claim, and canonical settlement under build/start on loopback.</action>
  <verify>
    <automated>deno test -A tests/intent_production_test.ts --filter "production intent tracer|signed archetype|reservation opener"</automated>
  </verify>
  <done>The real production transport proves exact pinned discovery/invocation, verified navigation, private claim, and correlated handled settlement end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve the opener-sever-first reservation entry</name>
  <files>routes/intent/reserved.tsx, islands/IntentReservation.tsx, tests/intent_production_test.ts</files>
  <behavior>
    - The fixed route accepts only a bounded reservationId/ticket bootstrap, and URL/SSR/log output never contains payload, handler destination, or caller-controlled markup.
    - The hydrated bootstrap executes `window.opener = null` before opening runtime transport or sending the exact ticket claim.
    - CSP remains compatible without inline script relaxation, while launched napplet iframes retain exact `sandbox="allow-scripts"` behavior.
  </behavior>
  <action>Add the fixed same-origin reservation route and a Fresh island bootstrap so generated application script remains compatible with the existing CSP. Validate fragment/bootstrap values, sever opener as the island's first browser-side operation, then authenticate the target runtime and atomically claim the server-held ticket using the exact codec. Render only inert status UI before claim; reject foreign origin, mismatched reservation/generation/target window, replay, and expiry. Do not relax CSP or iframe sandbox policy.</action>
  <verify>
    <automated>deno test -A tests/intent_production_test.ts --filter "reservation opener|reservation CSP"</automated>
  </verify>
  <done>The same-origin target severs opener before sensitive work, claims once through authenticated runtime, and cannot weaken CSP/sandbox or expose payload in navigation state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove generation churn, teardown, and reconnect integration</name>
  <files>routes/api/runtime.ts, tests/intent_production_test.ts, tests/runtime_reconnect_smoke_test.ts</files>
  <behavior>
    - Catalog replacement/uninstall revokes future invocation immediately and fails unresolved launch/ticket work once.
    - Same-generation reconnect preserves unresolved work only within existing grace and never replays committed navigation.
    - Account change, grace expiry, socket replacement, window closure, and runtime teardown clear registrations, reservations, correlations, and tickets deterministically.
    - intent.changed preserves last-good partial display on transient sync failure while current authority remains generation-checked.
  </behavior>
  <action>Connect catalog/account/reconnect lifecycle notifications to runtime INTENT projection and cleanup. Ensure each socket/window subscription is removed exactly once, committed commands are never resent after resume, and all unresolved correlations/tickets fail or expire under the existing grace policy. Extend production and reconnect smoke tests across catalog churn, stale caller/target windows, connection replacement, last-good availability, exact current authority, and process teardown. Preserve existing catalog/resource/upload route behavior while adding the INTENT branch.</action>
  <verify>
    <automated>deno test -A tests/intent_production_test.ts tests/runtime_reconnect_smoke_test.ts tests/intent_runtime_test.ts</automated>
  </verify>
  <done>Production lifecycle tests prove no stale authority, duplicate navigation, payload replay, or leaked pending work across replacement, reconnect, closure, or teardown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser WebSocket → production runtime | Network frames enter authenticated runtime-window dispatch. |
| same-origin ticket entry → backend store | URL-visible opaque token requests sensitive single-use payload. |
| reconnect generation → pending intent state | A resumed transport may reconnect to stable ownership but must not replay terminal work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-12 | Spoofing | `/api/runtime` | high | mitigate | Reuse server-established connection/window ownership before strict INTENT dispatch. |
| T-07-13 | Information disclosure | ticket entry/claim | high | mitigate | URL holds only opaque expiring token; exact account/target readiness and atomic claim gate payload. |
| T-07-14 | Tampering | reconnect/catalog generation | high | mitigate | Recheck account/catalog/manifest generation before commit/claim and revoke on churn. |
| T-07-15 | Repudiation | production correlation | medium | mitigate | Opaque backend correlation with idempotent ack/result/cleanup tests; sanitized logs exclude payload. |
| T-07-SC | Tampering | package supply chain | high | mitigate | No installs; production remains on audited locked pins. |
</threat_model>

<verification>
Run `deno test -A tests/intent_contract_test.ts tests/intent_registry_test.ts tests/intent_runtime_test.ts tests/intent_navigation_test.tsx tests/intent_production_test.ts tests/runtime_reconnect_smoke_test.ts`, then `deno task check`, `deno task build`, and the focused production smoke.
</verification>

<success_criteria>
- INT-01, INT-02, and INT-03 operate through the real production runtime and exact pinned contracts.
- Process, connection, account, catalog, window, surface, correlation, and ticket lifecycles remain consistently bound.
- The implementation adds no public envelope outside pinned 0.31.0 and no arbitrary navigation path.
</success_criteria>

## Source Coverage Audit

| Source | Items covered here | Status |
|--------|--------------------|--------|
| GOAL | Complete trusted discovery/invocation through shell navigation | COVERED |
| REQ | INT-01, INT-02, INT-03 production integration | COVERED |
| RESEARCH | production wiring; exact transport; ticket claim; build/start testing; lifecycle/reconnect | COVERED |
| CONTEXT | complete end-to-end integration of every locked decision | COVERED |

Across plans 07-01 through 07-04 every GOAL, REQ, RESEARCH, and CONTEXT item is covered. Deferred ideas remain excluded; no item is missing and no phase split is required.

<output>
Create `.planning/phases/07-intent-navigation/07-04-SUMMARY.md` when done.
</output>
