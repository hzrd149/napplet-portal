---
phase: 07-intent-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/catalog.ts
  - runtime/intent.ts
  - tests/intent_contract_test.ts
  - tests/intent_registry_test.ts
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "INT-01: A napplet sees handlers only from the active account's current accepted, integrity-verified catalog artifacts."
    - "INT-02: Handler choice is deterministic and malformed, superseded, unresolved, or uninstalled declarations never retain invocation authority."
    - "Transient catalog sync errors may retain last-good availability display, but never stale authority."
  artifacts:
    - path: runtime/intent.ts
      provides: "Strict pinned-contract codecs, generation-bound IntentService registry, deterministic selection, and canonical results"
    - path: runtime/catalog.ts
      provides: "Verified artifacts enriched with canonical archetype declarations"
    - path: tests/intent_registry_test.ts
      provides: "Registry churn, competing-handler, and stale-authority coverage"
  key_links:
    - from: runtime/catalog.ts
      to: runtime/intent.ts
      via: "CatalogService current accepted ready projection and exact verified identity"
    - from: runtime/intent.ts
      to: "@napplet/core@0.31.0 and @napplet/nap/intent@0.31.0"
      via: "production type-only imports for public request/result/availability shapes"
---

<objective>
Create the trusted, generation-bound intent registry and exact pinned-contract domain behavior.

Purpose: INT-01 and the authority half of INT-02 require one backend source of truth derived from CatalogService, not a second manifest query path.
Output: A strict three-field archetype decoder, verified catalog enrichment, deterministic handler registry, and focused contract/registry tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-intent-navigation/07-CONTEXT.md
@.planning/phases/07-intent-navigation/07-RESEARCH.md
@runtime/catalog.ts
@runtime/artifacts.ts
@runtime/connections.ts
@tests/catalog_test.ts

<interfaces>
From runtime/catalog.ts:
`VerifiedCatalogArtifact` carries manifestEventId/title/version/capabilities and launch `{ dTag, aggregateHash, srcdoc }`.
`CatalogService.subscribe(listener)`, `project()`, and `launch(catalogEventId, coordinate, manifestEventId)` are the existing authority seams.

Pinned public contracts:
Use `IntentRequest`, `IntentAvailability`, `IntentCandidate`, and `IntentResult` from `@napplet/core`; use only the six `intent.invoke|available|handlers` request/result envelopes plus `intent.changed` from `@napplet/nap/intent`. No sibling production imports.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one accepted archetype from verified manifest to available handler</name>
  <files>runtime/catalog.ts, runtime/intent.ts, tests/intent_contract_test.ts</files>
  <behavior>
    - A verified artifact with exactly `["archetype", "note", "napplet:note/open"]` becomes one immutable `note/open` declaration and one available candidate.
    - Tags with extra fields, invalid or numbered slugs, query/fragment/blank convention segments, or slug mismatch are omitted without invalidating verified launch bytes.
    - Duplicate identical declarations collapse; distinct valid actions/conventions remain stable.
  </behavior>
  <action>Add a first-party strict archetype tag decoder at the verified artifact boundary. Accept only exact three-string tags, slug `^[a-z0-9][a-z0-9-]*$`, queryless `napplet:<same-slug>/<action>`, bounded components, and immutable output; derive action from the final convention path segment. Extend `VerifiedCatalogArtifact` and ready catalog enrichment with declarations, without exposing srcdoc or weakening the existing exact accepted-event verification. Create `IntentService` over CatalogService notifications and demonstrate one current ready artifact through `handlers()`/`available(archetype)` using the pinned 0.31.0 public shapes. Production imports remain pinned npm aliases; `../napplet` remains reference-only. Do not add `intent.deliver`, URI invocation, candidate contracts/eventKinds, or trailing kind fields.</action>
  <verify>
    <automated>deno test -A tests/intent_contract_test.ts</automated>
  </verify>
  <done>One exact accepted and verified declaration reaches canonical availability, malformed declarations are absent, and catalog launch integrity behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the generation-bound registry and deterministic policy</name>
  <files>runtime/intent.ts, tests/intent_registry_test.ts</files>
  <behavior>
    - Registry authority keys include account pubkey, catalog event ID, manifest event ID, dTag, aggregate hash, and archetype.
    - Candidates sort by dTag then accepted manifest event ID; only the first is default.
    - Explicit handler means an installed candidate dTag and succeeds only for one current policy-permitted match; `choose` returns denied.
    - Uninstall/replacement revokes authority immediately and invalidates unresolved work, while stale/error display may retain last-good availability.
    - Convention must be an exact candidate convention and its derived action must match the request action; omitted action defaults to `open`.
  </behavior>
  <action>Complete the process-owned `IntentService` projection and selection API. Separate last-good projected availability from current-generation authority, rebuild from CatalogService's active account/current catalog truth, and ignore pending/unavailable artifacts. Use the research-locked lexicographic dTag then accepted-manifest-event-ID order. Validate normalized archetype/action/convention and explicit handler semantics; expose canonical `IntentResult` helpers with `ok:false`, `handled:false`, and only sanitized stable `unavailable`, `denied`, or `failed` reasons. Add generation tokens/guards for later launch settlement and deterministic subscription notification for `intent.changed`; do not query relays or artifacts independently.</action>
  <verify>
    <automated>deno test -A tests/intent_registry_test.ts tests/catalog_test.ts</automated>
  </verify>
  <done>Registry churn, competing handlers, default/explicit selection, stale display versus revoked authority, and convention/action mismatch are deterministic and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed manifest → registry | Untrusted tags become handler declarations only after exact artifact verification and strict decoding. |
| stale display → current authority | Last-good presentation must not grant invocation after catalog replacement or uninstall. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | archetype decoder | high | mitigate | Exact three-field shape, bounded grammar, immutable normalized declarations, and malformed-tag tests. |
| T-07-02 | Elevation of privilege | IntentService registry | high | mitigate | Bind authority to active account and exact catalog/manifest/dTag/hash generation; revoke on replacement/uninstall. |
| T-07-03 | Information disclosure | availability projection | medium | mitigate | Project only pinned candidate fields; never srcdoc, payloads, account internals, or relay data. |
| T-07-SC | Tampering | package supply chain | high | mitigate | No installs; preserve audited pinned `@napplet/core` and `@napplet/nap` 0.31.0 imports and lockfile. |
</threat_model>

<verification>
Run `deno test -A tests/intent_contract_test.ts tests/intent_registry_test.ts tests/catalog_test.ts` and `deno task check`.
</verification>

<success_criteria>
- INT-01 availability derives solely from current accepted, ready, verified CatalogService entries.
- Strict archetype decoding matches the canonical 0.31.0 producer and rejects every noncanonical shape.
- Deterministic selection and generation revocation are executable contracts for later transport/navigation plans.
</success_criteria>

## Source Coverage Audit

| Source | Items covered here | Status |
|--------|--------------------|--------|
| GOAL | Trusted handler discovery and backend selection | COVERED |
| REQ | INT-01; INT-02 registry/result foundations | COVERED |
| RESEARCH | exact pinned envelopes/types; strict three-field codec; generation registry; deterministic ordering; stale-display separation | COVERED |
| CONTEXT | all Handler discovery decisions; deterministic policy portions of Invocation and policy | COVERED |

Deferred marketplaces, remote/uninstalled handlers, user routing rules, and arbitrary cross-origin navigation are excluded intentionally.

<output>
Create `.planning/phases/07-intent-navigation/07-01-SUMMARY.md` when done.
</output>
