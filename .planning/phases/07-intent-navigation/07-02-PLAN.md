---
phase: 07-intent-navigation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - runtime/intent.ts
  - runtime/transport.ts
  - runtime/portal_runtime.ts
  - tests/intent_runtime_test.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "INT-02: A source-bound live caller receives exactly one canonical correlated result after handler launch and shell acknowledgement."
    - "INT-03: Backend policy alone authorizes reuse, new-tab, or stack and issues only constrained navigation commands."
    - "Intent payload is bounded strict JSON and crosses to an exact verified target only through a private single-use ticket."
  artifacts:
    - path: runtime/transport.ts
      provides: "Exact-key INTENT, navigation acknowledgement, and private ticket claim codecs"
    - path: runtime/intent.ts
      provides: "Invocation state machine and bounded single-use LaunchTicketStore"
    - path: runtime/portal_runtime.ts
      provides: "Per-window intent dispatch and lifecycle cleanup"
    - path: tests/intent_runtime_test.ts
      provides: "Invocation, ticket, stale-session, outcome, and reconnect tests"
  key_links:
    - from: runtime/portal_runtime.ts
      to: runtime/intent.ts
      via: "authenticated connection/window owner and IntentService invocation"
    - from: runtime/intent.ts
      to: runtime/catalog.ts
      via: "CatalogService.launch using exact current catalog/coordinate/manifest identity"
    - from: runtime/intent.ts
      to: runtime/transport.ts
      via: "opaque correlated navigation command/ack and ticket claim"
---

<objective>
Build the authenticated backend invocation, launch-ticket, and settlement seam.

Purpose: Convert canonical NAP-INTENT requests into policy-authorized, generation-safe shell commands without leaking payloads or artifact authority.
Output: Strict transport codecs, private tickets, lifecycle-safe invocation dispatch, and backend tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-intent-navigation/07-01-SUMMARY.md
@.planning/phases/07-intent-navigation/07-CONTEXT.md
@.planning/phases/07-intent-navigation/07-RESEARCH.md
@runtime/transport.ts
@runtime/portal_runtime.ts
@runtime/connections.ts
@tests/runtime_contract_test.ts
@tests/websocket_session_test.ts

<interfaces>
Consume the `IntentService` generation/selection/result interfaces produced by 07-01.
Existing `RuntimeServiceHub.openWindow(windowId, send, connectionId?)` binds services to a runtime window; `CatalogService.launch(catalogEventId, coordinate, manifestEventId)` is the exact artifact gate.
Existing WebSocket text frames are capped at 256,000 characters and runtime.forward ownership comes from server-known connectionId/windowId.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one authenticated invocation through verification, ticket, acknowledgement, and result</name>
  <files>runtime/intent.ts, runtime/transport.ts, runtime/portal_runtime.ts, tests/intent_runtime_test.ts</files>
  <behavior>
    - A live source-bound caller invokes one current handler, exact catalog launch verification succeeds, shell acknowledges navigation, target claims once after readiness, and caller receives one handled result with its original correlation ID.
    - The target receives payload only from a private claimed ticket, never an invented public NAP message.
    - Malformed top-level envelopes use envelope error; valid requests use canonical IntentResult outcomes.
  </behavior>
  <action>Define exact-key decoders for the three pinned incoming envelopes and their precise required fields, plus internal shell navigation acknowledgements and ticket claims. Build the invocation sequence in `IntentService`: validate envelope/payload, live caller owner/source, archetype/action/convention/current generation, handler and mode policy; reserve an opaque backend correlation; call exact `CatalogService.launch`; recheck account/generation; emit one constrained command containing shell IDs and verified launch material; wait for acknowledgement; then settle the caller once. Add a private in-memory `LaunchTicketStore` bound to account, caller owner, exact target identity, convention, payload, correlation, and short expiry; consume atomically once only after target artifact/session readiness. There is no `intent.deliver`; no caller URL, sender, window name, account, or target identity is trusted.</action>
  <verify>
    <automated>deno test -A tests/intent_runtime_test.ts --filter "authenticated invocation"</automated>
  </verify>
  <done>The one end-to-end backend path verifies exact authority, commits only after shell ack, delivers by single-use ticket, and returns one pinned result envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close validation, failure, and reconnect lifecycle paths</name>
  <files>runtime/intent.ts, runtime/transport.ts, runtime/portal_runtime.ts, tests/intent_runtime_test.ts</files>
  <behavior>
    - Serialized request.payload is at most 64 KiB, finite strict JSON, acyclic, depth/key bounded, own plain data; the full frame remains capped at 256 KiB.
    - unavailable, denied, and failed map to stable sanitized IntentResult errors; malformed processing alone uses envelope error.
    - Replacement/uninstall, stale caller/target, denied mode, failed verification, closed/blocked surface ack, timeout, account change, and grace expiry settle once and revoke tickets.
    - Reconnect under the same valid connection/window generation does not replay committed navigation; unresolved work survives only existing grace and then fails once.
    - available/handlers queries and generation changes emit only exact pinned result/change envelopes.
  </behavior>
  <action>Harden strict payload validation with the resolved 64 KiB serialized bound, plain own JSON data, finite numbers, and conservative depth/key limits; retain the 256 KiB frame ceiling. Implement idempotent terminal settlement, bounded pending invocation/ticket registries, timeout and teardown cleanup, and stable sanitized reasons. Wire `RuntimeServiceHub` window lifecycle to `IntentService` availability queries, invoke dispatch, `intent.changed`, shell acknowledgement, and ticket claim without duplicating account/catalog state. Use existing connection/window ownership and reconnect grace semantics; never replay a navigation already committed. Test stale generations, competing handlers, explicit/choose policy, every canonical failure, single-use/expiry, cleanup, and reconnect.</action>
  <verify>
    <automated>deno test -A tests/intent_runtime_test.ts tests/runtime_contract_test.ts tests/websocket_session_test.ts</automated>
  </verify>
  <done>All valid and invalid invocation paths terminate once, bounded registries recover capacity, tickets cannot replay or cross identities, and reconnect semantics are deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox caller → runtime | Untrusted intent frames cross authenticated connection/window ownership. |
| backend → shell | Only policy-authorized opaque navigation commands cross to browser controls. |
| ticket store → target session | Sensitive payload is released once to one exact verified account/handler session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Spoofing | intent dispatcher | high | mitigate | Exact event source plus connectionId/windowId/account/current verified identity validation. |
| T-07-05 | Denial of service | payload and pending registries | high | mitigate | 64 KiB payload, 256 KiB frame, shape/depth/key limits, bounded registries and timeouts. |
| T-07-06 | Information disclosure | LaunchTicketStore | high | mitigate | Opaque short-lived account/caller/target-bound single-use tickets claimed only after readiness. |
| T-07-07 | Repudiation | invocation settlement | medium | mitigate | Backend correlations and idempotent terminal states across ack, timeout, teardown, and reconnect. |
| T-07-SC | Tampering | package supply chain | high | mitigate | No package installs; reuse locked audited package pins. |
</threat_model>

<verification>
Run `deno test -A tests/intent_runtime_test.ts tests/runtime_contract_test.ts tests/websocket_session_test.ts` and `deno task check`.
</verification>

<success_criteria>
- INT-02 canonical results cover success, unavailable, denied, malformed, and failed lifecycle paths.
- Navigation cannot precede exact launch verification and generation recheck.
- Payload delivery is private, bounded, single-use, identity-bound, expiring, and absent from public NAP envelopes.
</success_criteria>

## Source Coverage Audit

| Source | Items covered here | Status |
|--------|--------------------|--------|
| GOAL | Trusted invocation backend and shell-controlled command seam | COVERED |
| REQ | INT-01 queries/change; INT-02 full invocation; INT-03 backend policy | COVERED |
| RESEARCH | exact envelopes/results; 64 KiB payload; validation order; single-use tickets; acknowledgement; reconnect | COVERED |
| CONTEXT | all Invocation/policy decisions and backend portions of Failure/lifecycle and Shell navigation | COVERED |

<output>
Create `.planning/phases/07-intent-navigation/07-02-SUMMARY.md` when done.
</output>
