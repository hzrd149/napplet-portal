---
phase: 02-backend-runtime-expansion
plan: "07"
type: execute
wave: 5
depends_on: [02-06]
files_modified: [runtime/contract_report.ts, tests/runtime_contract_test.ts, tests/contract_drift_test.ts, tests/fixtures/supplied_napplet_contract.json, tests/end_to_end_test.ts, .planning/phases/02-backend-runtime-expansion/02-CONTRACT-DRIFT.json, README.md]
autonomous: true
requirements: [V2-07, V2-08]
must_haves:
  truths:
    - "Pinned npm packages are the executable contract authority; sibling sources are read only by non-production drift tooling (D-22)."
    - "Blocking tests cover compile-time types, serialized fixtures, handshake, streams, EOSE, close, correlation, typed errors, and the supplied-napplet smoke path (D-24)."
    - "Sibling mismatch, unreadability, or absence produces a structured report and never fails check, release, or runtime startup solely because of drift (D-23, D-25)."
  artifacts: [runtime/contract_report.ts, tests/contract_drift_test.ts, .planning/phases/02-backend-runtime-expansion/02-CONTRACT-DRIFT.json]
  key_links: ["pinned package types/fixtures -> adapters -> lifecycle tests; sibling source -> diagnostic report only"]
  prohibitions:
    - statement: "A moving sibling checkout must not silently become production runtime authority or a release gate."
      status: unverified
      flagged: true
---
<objective>Close Phase 2 with authoritative package contract coverage and non-blocking sibling drift evidence. Purpose: Prove Kehto/Napplet compatibility without making local repositories executable dependencies. Output: Layered contract suite, structured drift report, smoke test, and documentation.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-RESEARCH.md
@.planning/phases/02-backend-runtime-expansion/COVERAGE.md
@.planning/phases/02-backend-runtime-expansion/02-06-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Prove pinned Kehto/Napplet dispatch through the supplied napplet smoke path</name>
  <read_first>tests/runtime_contract_test.ts, tests/end_to_end_test.ts, tests/fixtures/supplied_napplet_contract.json, deno.json, ../kehto/RUNTIME-SPEC.md, ../kehto/packages/runtime/src/relay-handler.ts, ../napplet/packages/nap/src/relay/types.ts, ../napplet/packages/nap/src/outbox/types.ts</read_first>
  <files>tests/runtime_contract_test.ts, tests/fixtures/supplied_napplet_contract.json, tests/end_to_end_test.ts</files>
  <behavior>- Pinned package types compile against portal adapters.
- Serialized fixtures round-trip success/error/correlation and stream lifecycle frames.
- Supplied napplet completes handshake, initial/live events, EOSE, close, and typed error through backend dispatch.</behavior>
  <action>Expand blocking authoritative tests for `@kehto/runtime`, `@napplet/core@0.31.0`, and `@napplet/nap@0.31.0` across handshake, RELAY/OUTBOX streams, EOSE, close, correlation, ownership, provenance, and typed errors per D-22 and D-24. Retain the real supplied-napplet end-to-end smoke. Resolve V2-07 unclassified and V2-08 concurrency probe predicates through two logical owners, isolated close, duplicate correlation rejection, and deterministic terminal frames.</action>
  <verify><automated>deno test -A tests/runtime_contract_test.ts tests/end_to_end_test.ts</automated></verify>
  <acceptance_criteria>All named lifecycle behaviors have a blocking case against pinned imports and serialized fixtures; supplied napplet smoke traverses browser boundary, backend dispatcher, stream events, and close.</acceptance_criteria>
  <done>The executable integration is proven entirely from pinned production contracts.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Emit non-blocking structured sibling drift evidence</name>
  <read_first>deno.json, runtime/contract_report.ts, tests/runtime_contract_test.ts, ../kehto/RUNTIME-SPEC.md, ../napplet/packages/core/src/types/nostr.ts, .planning/phases/02-backend-runtime-expansion/02-CONTEXT.md</read_first>
  <files>runtime/contract_report.ts, tests/contract_drift_test.ts, .planning/phases/02-backend-runtime-expansion/02-CONTRACT-DRIFT.json, README.md</files>
  <behavior>- Report names affected contract, exact pinned version, sibling revision/availability, and adapter coverage.
- Mismatch, parse failure, missing file, or absent sibling is recorded and exits successfully.
- Production runtime has no import of report or sibling paths.</behavior>
  <action>Create diagnostic-only `generateContractDriftReport` and `ContractDriftReport` schema. Compare pinned serialized/type surface evidence with reference-only sibling files, record revision/availability and per-adapter coverage, and write `02-CONTRACT-DRIFT.json` per D-23 and D-25. Ensure the normal test/check path asserts report structure but never fails solely on drift; document how to regenerate and interpret it. Do not add sibling path/workspace imports to production.</action>
  <verify><automated>deno test -A tests/contract_drift_test.ts tests/runtime_contract_test.ts && deno task check && deno task test</automated></verify>
  <acceptance_criteria>Drift tests exit zero for matching, mismatching, missing, and unreadable sibling fixtures while producing valid structured status; repository search shows sibling imports only in diagnostic test/report inputs; full suite passes.</acceptance_criteria>
  <done>Contract drift is visible, structured, and non-blocking while pinned adapters remain release authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| sibling checkout → drift tool | Mutable local source is untrusted diagnostic input. |
| wire fixture → dispatcher | Serialized cross-frame messages enter backend adapters. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-17 | Tampering | sibling drift input | medium | mitigate | Read-only parsing, stable error records, no production import, no release authority. |
| T-02-18 | Spoofing | runtime wire fixtures | high | mitigate | Source/correlation/owner validation and pinned typed decoders. |
| T-02-19 | Information Disclosure | drift report | low | accept | Report contains public versions, revisions, contract names, and coverage only; no paths/secrets/payloads. |
</threat_model>
## Probe assumptions
- V2-07 unclassified coverage is defined by complete named dispatch lifecycle behavior.
- V2-08 concurrency coverage is defined by deterministic parallel owner/correlation/drift generation cases.
## Artifacts this phase produces
- `ContractDriftReport`, `ContractDriftEntry`, `generateContractDriftReport`
- `runtime/contract_report.ts`, `tests/contract_drift_test.ts`, `.planning/phases/02-backend-runtime-expansion/02-CONTRACT-DRIFT.json`

## Multi-Source Coverage Audit
| Source | Items | Coverage |
|---|---|---|
| GOAL | Durable backend-owned Applesauce account/event/runtime foundations, local event/blob caches, sibling contract integration | Plans 01–07 |
| REQ | V2-01 through V2-10 | Every ID appears in plan frontmatter and executable tasks |
| RESEARCH | settings store, event runtime/loaders, relay policy/cache, Blossom cache, NIP-78 catalog, contract report, security controls | Plans 01–07 |
| CONTEXT | D-01 through D-25 | Cited in objectives, actions, truths, and acceptance criteria across Plans 01–07 |
| UI | 25 covered and 3 backstop considerations | Covered truths and structured backstops in Plans 05–06 |
| SPEC-less edges | 16 probe rows | Each surfaced under Probe assumptions and resolved into task predicates; no row dismissed |
| SPEC-less prohibitions | Recalled values/safety boundaries | Descriptor-less flagged-unverified entries in each relevant plan |

Excluded without gap: Phase 3 broader NAP domains/approval persistence/production hardening and UI-SPEC out-of-scope guardrails.
<verification>`deno task check`, `deno task test`, and API coverage matrix validation pass.</verification>
<success_criteria>Pinned contracts are fully exercised, supplied-napplet E2E remains green, and sibling drift always yields a non-blocking structured report.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-07-SUMMARY.md` when done.</output>
