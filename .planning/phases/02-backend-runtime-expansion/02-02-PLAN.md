---
phase: 02-backend-runtime-expansion
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified: [runtime/relay_policy.ts, runtime/relay_cache.ts, runtime/event_runtime.ts, runtime/relay_adapter.ts, runtime/outbox.ts, tests/relay_policy_test.ts, tests/relay_cache_test.ts]
autonomous: true
requirements: [V2-02, V2-04, V2-06]
must_haves:
  truths:
    - "A local relay request completes by EOSE or configured timeout before the live upstream subscription opens (D-02, D-04)."
    - "Valid upstream events reach the caller before asynchronous local cache acknowledgement, and cache failure only degrades health (D-03)."
    - "Blocked relays never reach connection, subscription, publish, or AUTH creation; AUTH requires per-relay opt-in and is separate from portal login (D-14, D-15)."
  artifacts: [runtime/relay_policy.ts, runtime/relay_cache.ts, tests/relay_policy_test.ts, tests/relay_cache_test.ts]
  key_links:
    - "settings$/NIP-65 state -> RelayPolicy -> RelayPool operations"
    - "local Relay.request -> concat -> upstream subscription -> EventStore/cache queue"
  prohibitions:
    - statement: "Relay AUTH must not silently become automatic signer authorization for every challenge."
      status: unverified
      flagged: true
    - statement: "Optional cache health must not silently become a prerequisite for user streams."
      status: unverified
      flagged: true
---
<objective>Deliver local-first event caching and policy-safe NIP-65/NIP-42 routing. Purpose: Make durable caching optional and non-blocking while preserving backend authority. Output: RelayPolicy, RelayCache, integrations, and focused tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-RESEARCH.md
@.planning/phases/02-backend-runtime-expansion/02-PATTERNS.md
@.planning/phases/02-backend-runtime-expansion/02-01-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Enforce blocked-relay precedence and opt-in AUTH</name>
  <read_first>runtime/outbox.ts, runtime/relay_adapter.ts, runtime/settings.ts, runtime/event_runtime.ts, ../kehto/packages/runtime/src/relay-handler.ts, .planning/phases/02-backend-runtime-expansion/02-CONTEXT.md</read_first>
  <files>runtime/relay_policy.ts, runtime/event_runtime.ts, runtime/outbox.ts, tests/relay_policy_test.ts</files>
  <behavior>- Normalize and deduplicate relay URLs before use.
- Derive direction-aware NIP-65 routes, then defaults/fallbacks, then remove blocked relays.
- Permit NIP-42 AUTH only for an exact canonical relay with explicit opt-in; blocked state wins.</behavior>
  <action>Create `RelayPolicy`, `resolveReadRelays`, `resolveWriteRelays`, `resolveAuthPermission`, and observable relay-map derivation. Apply policy before every `RelayPool.relay`, subscription, publish, and authenticate call per D-11, D-12, D-14, and D-15. Do not mirror NIP-65 into mutable portal state. Resolve V2-06 adjacency/empty/ordering predicates with canonical URL equality, empty eligible-set behavior, and deterministic precedence tests.</action>
  <verify><automated>deno test -A tests/relay_policy_test.ts</automated></verify>
  <acceptance_criteria>Tests prove blocked URLs produce zero pool calls across connect/subscribe/publish/AUTH; AUTH is false without exact opt-in; normalized duplicates collapse; NIP-65/default/fallback order is deterministic.</acceptance_criteria>
  <done>All relay operations pass through one policy boundary before connection creation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expand subscriptions into bounded local-first read-through streams</name>
  <read_first>runtime/relay_adapter.ts, runtime/event_runtime.ts, runtime/relay_policy.ts, runtime/settings.ts, tests/relay_stream_test.ts, ../kehto/packages/runtime/src/relay-result.ts</read_first>
  <files>runtime/relay_cache.ts, runtime/relay_adapter.ts, tests/relay_cache_test.ts</files>
  <behavior>- Local request completion precedes upstream subscribe.
- Timeout/error converts only the local branch to empty completion and records sanitized degraded health.
- Upstream delivery is synchronous relative to queued cache publish; provenance is inserted even for duplicates.
- Close remains isolated per owner and emits one terminal EOSE.</behavior>
  <action>Implement `RelayCache`, `relayCacheHealth$`, and an observed cache-write queue using `concat(localRequest$, upstream$)`, the Applesauce request timeout, `tap`, `catchError`, `finalize`, and owned teardown per D-02 through D-05. Extend `BackendRelayAdapter` without changing its correlated wire contract. The local relay is the only durable event backend per D-01.</action>
  <verify><automated>deno test -A tests/relay_cache_test.ts tests/relay_stream_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Tests prove EOSE/timeout/error ordering, upstream fallback, delivery before cache acknowledgement, observed write rejection, provenance, duplicate behavior, singular EOSE, and owner-isolated close.</acceptance_criteria>
  <done>Portal streams use an optional local relay as a bounded read-through cache without delaying correctness.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| settings/NIP-65 → RelayPool | Operator and network-derived endpoints may cause outbound connections. |
| relay challenge → signer | Untrusted relay requests connection-scoped AUTH. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Elevation of Privilege | RelayPolicy AUTH | high | mitigate | Exact canonical relay binding, explicit opt-in, blocked override, and no portal-login coupling. |
| T-02-04 | Denial of Service | RelayCache | high | mitigate | Bounded local request timeout and non-blocking observed writes. |
| T-02-05 | Information Disclosure | cache health | low | accept | Only canonical endpoint status and sanitized reason are exposed; no payloads or signer data. |
</threat_model>
## Probe assumptions
- V2-04 was unclassified; local absence, timeout, EOSE, failure, and live fallback are made explicit tests.
- V2-02 concurrency/idempotency coverage includes duplicate arrivals, concurrent owners, and close isolation.
## Artifacts this phase produces
- `RelayPolicy`, `resolveReadRelays`, `resolveWriteRelays`, `resolveAuthPermission`, `RelayCache`, `RelayCacheHealth`
- `runtime/relay_policy.ts`, `runtime/relay_cache.ts`, `tests/relay_policy_test.ts`, `tests/relay_cache_test.ts`
<verification>`deno test -A tests/relay_policy_test.ts tests/relay_cache_test.ts tests/relay_stream_test.ts` and `deno task check` pass.</verification>
<success_criteria>Local EOSE/timeout gates upstream opening; cache failure is non-fatal; no blocked relay is connected or authenticated.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-02-SUMMARY.md` when done.</output>
