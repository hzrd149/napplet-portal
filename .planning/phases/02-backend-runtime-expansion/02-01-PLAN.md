---
phase: 02-backend-runtime-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [deno.json, deno.lock, runtime/settings_store.ts, runtime/settings.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, tests/settings_test.ts, tests/tracer_end_to_end_test.ts]
autonomous: false
requirements: [V2-01, V2-02, V2-03]
must_haves:
  truths:
    - "A backend-persisted settings change emits through one process-owned settings$ source and is used by the next runtime operation without restart (D-10, D-13)."
    - "One tracer saves relay settings, observes the reactive snapshot, loads a real event through the process-wide Applesauce runtime, and tears down cleanly."
    - "The shared EventStore owns live dedupe, relay provenance, replaceable/addressable/delete semantics, and loader/model access (D-05)."
  artifacts:
    - runtime/settings_store.ts
    - runtime/settings.ts
    - runtime/event_runtime.ts
    - tests/tracer_end_to_end_test.ts
  key_links:
    - "PortalRuntime -> EventRuntime -> EventStore/RelayPool/loader"
    - "SettingsStore -> RuntimeSettingsService.settings$ -> runtime consumers"
  prohibitions:
    - statement: "Operator persistence must not silently become browser-owned routing authority."
      status: unverified
      flagged: true
    - statement: "Event durability must not silently become a second embedded event database (D-01)."
      status: unverified
      flagged: true
---

<objective>
**As a** portal operator, **I want to** persist runtime endpoints and exercise them through one durable backend runtime, **so that** later cache, policy, and catalog slices share a proven reactive foundation.

Purpose: Establish the production-quality Phase 2 tracer and satisfy D-01, D-05, D-10, and D-13 without duplicating backend state.
Output: Pinned loader dependency, atomic settings service, process-wide event runtime, and end-to-end tracer.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-RESEARCH.md
@.planning/phases/02-backend-runtime-expansion/02-PATTERNS.md
@.planning/phases/01-one-day-napplet-runtime-mvp/01-06-SUMMARY.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the suspicious Applesauce loader package</name>
  <read_first>deno.json, .planning/phases/02-backend-runtime-expansion/02-RESEARCH.md</read_first>
  <what-built>No install occurs before publisher/package legitimacy is confirmed.</what-built>
  <how-to-verify>Open npmjs.com/package/applesauce-loaders, confirm version 6.2.0 and that its publisher/repository lineage matches the already pinned Applesauce packages, then approve the exact `npm:applesauce-loaders@6.2.0` import.</how-to-verify>
  <acceptance_criteria>Human explicitly approves `applesauce-loaders@6.2.0`; otherwise execution stops without modifying dependency files.</acceptance_criteria>
  <resume-signal>Type "approved" or reject the package.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove persisted settings through a process-wide Applesauce event load</name>
  <read_first>runtime/account_store.ts, runtime/accounts.ts, runtime/portal_runtime.ts, runtime/relay_adapter.ts, runtime/config.ts, deno.json, tests/tracer_end_to_end_test.ts, .planning/phases/02-backend-runtime-expansion/02-CONTEXT.md</read_first>
  <files>deno.json, deno.lock, runtime/settings_store.ts, runtime/settings.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, tests/settings_test.ts, tests/tracer_end_to_end_test.ts</files>
  <behavior>
    - A versioned settings snapshot round-trips atomically and emits canonical values through `RuntimeSettingsService.settings$`.
    - `EventRuntime` owns one `EventStore`, `RelayPool`, Applesauce loader, and deterministic `destroy()` lifecycle.
    - The tracer changes settings, performs a real loader-backed event lookup through `PortalRuntime`, observes the new settings without restart, and tears down subscriptions.
  </behavior>
  <action>Add the approved exact `applesauce-loaders` pin. Implement `SettingsStore` with serialized temp-file/rename writes and defensive version parsing, `RuntimeSettingsService` with one `BehaviorSubject<RuntimeSettings>` plus synchronous `settings` getter, and `EventRuntime` as the injected owner of the shared `EventStore`, `RelayPool`, loader, and model seams. Extend `PortalRuntime` to consume this singleton. Defaults originate from `runtime/config.ts`; persistence remains backend-owned per D-10, applies to new work without forced teardown per D-13, and no embedded database is introduced per D-01. Resolve the V2-01 fallback probe's unclassified assumption by testing each named Applesauce role; resolve V2-02 empty/idempotency/concurrency predicates with empty-store behavior, duplicate insertion, serialized settings writes, and deterministic teardown.</action>
  <verify><automated>deno test -A tests/settings_test.ts tests/tracer_end_to_end_test.ts && deno task check</automated></verify>
  <acceptance_criteria>`deno.json` contains `npm:applesauce-loaders@6.2.0`; settings tests prove absence defaults, corrupt/version-invalid rejection, canonical round-trip, queued last-write result, and reactive emission; tracer test proves settings-to-loader end-to-end behavior and `destroy()` releases owned resources.</acceptance_criteria>
  <done>The backend has one persistent reactive configuration source and one process-owned Applesauce runtime proven through a real end-to-end lookup.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted JSON → runtime | Untrusted/corrupt local state enters process-owned routing configuration. |
| relay events → EventStore | Untrusted network events enter shared models. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | SettingsStore/EventStore | high | mitigate | Versioned field validation, atomic writes, and EventStore signature/semantic acceptance tests. |
| T-02-02 | Denial of Service | EventRuntime teardown | medium | mitigate | Owned subscriptions and idempotent `destroy()` verified by tracer. |
| T-02-SC | Tampering | npm installs | high | mitigate | Blocking legitimacy checkpoint before installing the SUS loader pin. |
</threat_model>

## Probe assumptions
- V2-01 was unclassified by the deterministic edge probe; the plan treats complete coverage of every named Applesauce role as the explicit acceptance boundary.
- V2-02 adjacency/empty/ordering/idempotency/concurrency predicates are resolved by EventStore semantics and deterministic tests, not by a second state engine.

## Artifacts this phase produces
- `SettingsStore`, `RuntimeSettings`, `RuntimeSettingsService`, `EventRuntime`, `createEventRuntime`
- `runtime/settings_store.ts`, `runtime/settings.ts`, `runtime/event_runtime.ts`, `tests/settings_test.ts`

<verification>`deno test -A tests/settings_test.ts tests/tracer_end_to_end_test.ts` and `deno task check` pass.</verification>
<success_criteria>Persisted settings reach a real Applesauce loader operation without restart; one shared EventStore/RelayPool owns semantics and teardown.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-01-SUMMARY.md` when done.</output>

