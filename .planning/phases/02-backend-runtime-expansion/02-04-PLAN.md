---
phase: 02-backend-runtime-expansion
plan: "04"
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [runtime/catalog.ts, runtime/portal_runtime.ts, runtime/transport.ts, runtime/artifacts.ts, tests/catalog_test.ts]
autonomous: true
requirements: [V2-01, V2-09, V2-10]
must_haves:
  truths:
    - "The authoritative installed catalog is one public signed replaceable kind-30078 event for the active account with entries limited to coordinate and accepted manifest ID (D-16, D-17)."
    - "Latest valid replacement wins; mutations serialize from the latest projection and never merge divergent edits (D-18)."
    - "Launch and browser-safe identity derive only from the accepted integrity-verified manifest; a newer manifest remains pending until approval (D-19)."
    - "Uninstall publishes a replacement catalog and does not claim immediate artifact deletion (D-21)."
  artifacts: [runtime/catalog.ts, tests/catalog_test.ts]
  key_links:
    - "PortalAccounts.identity$ -> CatalogService load/project/sign/publish"
    - "catalog acceptedManifestEventId -> PortalArtifactResolver -> runtime-attested launch identity"
  prohibitions:
    - statement: "The public catalog must not silently become storage for secrets or capability approvals."
      status: unverified
      flagged: true
    - statement: "Iframe-supplied metadata must not silently become runtime identity."
      status: unverified
      flagged: true
---
<objective>Deliver an account-owned synchronized installed catalog with accepted-manifest identity. Purpose: Replace the configured tile with a trustworthy mutable Nostr projection. Output: Catalog codec/service, runtime commands, transport projection, and tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-RESEARCH.md
@.planning/phases/02-backend-runtime-expansion/02-02-SUMMARY.md
@.planning/phases/02-backend-runtime-expansion/02-03-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Load, approve, and launch one accepted NIP-78 catalog entry</name>
  <read_first>runtime/accounts.ts, runtime/outbox.ts, runtime/artifacts.ts, runtime/portal_runtime.ts, runtime/transport.ts, components/HomeView.tsx, .planning/phases/02-backend-runtime-expansion/02-CONTEXT.md</read_first>
  <files>runtime/catalog.ts, runtime/portal_runtime.ts, runtime/transport.ts, runtime/artifacts.ts, tests/catalog_test.ts</files>
  <behavior>- Decode only valid kind 30078 events for the active pubkey and exact application `d` tag.
- Project the latest valid event and resolve each accepted manifest through verified artifacts.
- Serialize approve/uninstall commands from the latest projection, sign with the active signer, and publish typed settlement.
- Continue launching the accepted event ID until approval succeeds.</behavior>
  <action>Create `CatalogService`, `InstalledCatalog`, `InstalledNappletEntry`, `CatalogProjection`, `decodeCatalogEvent`, `approveManifestUpdate`, and `uninstallNapplet`. Use application identifier `org.napplet.portal:installed` and versioned public JSON containing only `coordinate` and `acceptedManifestEventId` per D-16 through D-21. Validate kind, author, exact d tag, signature/EventStore acceptance, NIP-5A coordinate, and 64-hex event ID. Derive display/version/capability comparison solely from verified manifests. Expose browser-safe projection and correlated commands through existing transport; serialize per active account and re-read current projection before signing. Resolve V2-09's unclassified probe with zero/one/many, malformed, concurrent replacement, signer-absent, partial-publish, update, and uninstall tests.</action>
  <verify><automated>deno test -A tests/catalog_test.ts tests/artifact_resolver_test.ts tests/runtime_contract_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Tests prove defensive decode, latest-valid replacement, active-account isolation, signed command templates, partial relay settlement, accepted-manifest pinning, iframe identity rejection, update approval, and uninstall without cache deletion.</acceptance_criteria>
  <done>One synchronized catalog entry can be loaded, approved, and launched end-to-end using runtime-attested identity.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public NIP-78 event → catalog | Untrusted relay content becomes account-visible application state. |
| browser command → signer | Browser requests a signed replacement event. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-09 | Spoofing | Catalog identity | high | mitigate | Active author, exact d tag, valid signature, accepted verified manifest, and no iframe identity input. |
| T-02-10 | Tampering | Catalog mutations | high | mitigate | Serialize per account, re-read latest projection, sign backend template, publish typed outcomes. |
| T-02-11 | Information Disclosure | public content | medium | mitigate | Codec permits coordinate and accepted public manifest ID only. |
</threat_model>
## Probe assumptions
- V2-09 and V2-10 were unclassified; catalog cardinality, invalid input, stale concurrent replacement, absent signer, and unverified identity are explicit acceptance cases.
## Artifacts this phase produces
- `CatalogService`, `InstalledCatalog`, `InstalledNappletEntry`, `CatalogProjection`, `decodeCatalogEvent`, `approveManifestUpdate`, `uninstallNapplet`
- `runtime/catalog.ts`, `tests/catalog_test.ts`
<verification>`deno test -A tests/catalog_test.ts tests/runtime_contract_test.ts` and `deno task check` pass.</verification>
<success_criteria>Catalog state is account-owned, replaceable, defensively projected, and launches only accepted verified manifests.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-04-SUMMARY.md` when done.</output>

