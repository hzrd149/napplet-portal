---
phase: 02-backend-runtime-expansion
plan: "05"
type: execute
wave: 3
depends_on: [02-02]
files_modified: [utils.ts, main.ts, routes/settings.tsx, components/ProfileView.tsx, islands/NappletShell.tsx, assets/styles.css, tests/settings_route_test.tsx, tests/shell_architecture_test.ts]
autonomous: true
requirements: [V2-03, V2-06]
must_haves:
  truths:
    - "Runtime settings renders every labeled field from the immediate persisted backend snapshot; absent optional endpoints remain editable, and Save alone becomes `Saving…` during submit."
    - "Invalid submissions preserve all values, keep valid fields usable, show field-associated errors and the specified summary, and atomically retain the prior active settings."
    - "Cache relay and Blossom health render independently as checking, healthy, or degraded and never disable form use or napplet launch."
    - "AUTH rows show canonical eligible relays, explicit `Allow NIP-42 AUTH`, blocked override, and the documented empty state."
    - statement: "At 320px, URL-list fields wrap or scroll internally without page-level horizontal overflow."
      verification: backstop
    - statement: "At 320px, AUTH URLs middle-truncate with the full accessible value and controls remain at least 44px."
      verification: backstop
  artifacts: [routes/settings.tsx, tests/settings_route_test.tsx]
  key_links: ["Profile Runtime settings action -> Fresh GET/POST /settings -> RuntimeSettingsService"]
  prohibitions:
    - statement: "Settings UI must not silently imply that saving restarts or reconnects existing work."
      status: unverified
      flagged: true
---
<objective>Expose backend-owned routing and cache configuration in the approved mobile shell. Purpose: Let operators change safe defaults without transferring runtime authority to the browser. Output: Fresh settings route, Profile navigation, health presentation, and route/UI tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/02-backend-runtime-expansion/02-UI-SPEC.md
@.planning/phases/02-backend-runtime-expansion/02-CONTEXT.md
@.planning/phases/02-backend-runtime-expansion/02-02-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Save canonical runtime settings from Profile through Fresh</name>
  <read_first>routes/signin.tsx, routes/api/signin/nsec.ts, components/ProfileView.tsx, islands/NappletShell.tsx, utils.ts, main.ts, assets/styles.css, .planning/phases/02-backend-runtime-expansion/02-UI-SPEC.md</read_first>
  <files>utils.ts, main.ts, routes/settings.tsx, components/ProfileView.tsx, islands/NappletShell.tsx, assets/styles.css, tests/settings_route_test.tsx, tests/shell_architecture_test.ts</files>
  <behavior>- GET renders canonical current values immediately.
- POST validates every URL collection and commits atomically through RuntimeSettingsService.
- Success says `Settings saved. New operations will use these values.`
- Failure preserves submitted values and previous active settings.</behavior>
  <action>Add backend settings and cache health to typed Fresh state. Create server-rendered GET/POST `/settings` with fieldsets `Relay routing`, `Relay authentication`, and `Blossom servers`; fields `Fallback and extra relays`, `Default indexer relays`, `Default lookup relays`, optional local relay cache, and `Blossom servers`; and per-relay `Allow NIP-42 AUTH`. Add `Runtime settings` action from Profile while retaining the existing two-destination nav and persistent inert iframe. Implement the exact copy, 16px/24px/40rem layout, responsive Save behavior, focus/error/status semantics, native controls, 44px targets, and colors/typography from UI-SPEC. Saving applies D-10 through D-15 and never claims forced reconnection.</action>
  <verify><automated>deno test -A tests/settings_route_test.tsx tests/shell_architecture_test.ts && deno task check</automated></verify>
  <acceptance_criteria>Route tests prove immediate snapshot render, valid canonical atomic save, invalid retention, blocked AUTH row behavior, and exact success/error copy; architecture tests prove browser Back returns to Profile and iframe identity/mount persists.</acceptance_criteria>
  <done>An operator can navigate from Profile, edit all scoped settings, and save them to the backend without restart.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser form → settings service | Untrusted URL and AUTH choices request backend outbound policy changes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-12 | Spoofing | settings POST | high | mitigate | Same-origin Fresh handler, typed parsing, canonical scheme/URL validation, and blocked precedence. |
| T-02-13 | Information Disclosure | route errors | medium | mitigate | Stable field messages without secrets, filesystem paths, raw exceptions, or transport diagnostics. |
</threat_model>
## Probe assumptions
- V2-03 was unclassified; empty, invalid, partial, concurrent-save, and canonicalized-value behavior is defined by route tests and UI-SPEC.
## Artifacts this phase produces
- Fresh route `GET/POST /settings`, `RuntimeSettingsPage`, `SettingsGroup`, `UrlListField`, `AuthRelayRow`, `CacheHealthSummary`
- `routes/settings.tsx`, `tests/settings_route_test.tsx`
<verification>`deno test -A tests/settings_route_test.tsx tests/shell_architecture_test.ts` and `deno task check` pass; backstop visual checks remain honest verifier `human_needed` if no explicit browser evidence exists.</verification>
<success_criteria>Settings UX matches all resolved UI considerations and persists policy-safe canonical backend state.</success_criteria>
<output>Create `.planning/phases/02-backend-runtime-expansion/02-05-SUMMARY.md` when done.</output>
