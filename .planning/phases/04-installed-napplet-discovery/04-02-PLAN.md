---
phase: 04-installed-napplet-discovery
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified: [runtime/transport.ts, runtime/portal_runtime.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/catalog_ui_test.tsx, tests/end_to_end_test.ts]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "Preview, approval, and launch are correlated, bounded runtime commands whose failures cannot hang the browser (D-01 through D-04, D-11, D-12)."
    - "Catalog pushes preserve last-good cards while independently reporting loading, ready, stale, or error (D-05 through D-07)."
    - "A successful installed-card launch alone supplies verified bytes to the existing single mounted iframe; stale failure stays on Home (D-11 through D-13)."
  artifacts: [runtime/transport.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/end_to_end_test.ts]
  key_links:
    - "strict command decoder -> RuntimeServiceHub -> CatalogService domain operation -> correlated result"
    - "CatalogService listener -> runtime.catalog status/projection push -> shell last-good state"
    - "catalog.launch success -> bridge identity reset -> existing NappletFrame srcdoc lifecycle"
---
<objective>Carry catalog authority safely across the existing production WebSocket, implementing D-01 through D-07 and D-11 through D-13. Purpose: ensure the browser can request review, approval, and launch without acquiring authority or losing useful streamed state. Output: strict codecs, runtime dispatch, correlation cleanup, and production-path tests.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/04-installed-napplet-discovery/04-01-SUMMARY.md
@.planning/phases/04-installed-napplet-discovery/04-CONTEXT.md
@runtime/transport.ts
@runtime/portal_runtime.ts
@routes/api/runtime.ts
@islands/NappletShell.tsx
<interfaces>Consume the Plan 01 `CatalogService.previewInstall`, generation-bound approval, status/projection subscription, and exact launch operation. Preserve the existing same-origin `/api/runtime`, 256KB frame ceiling, connection/window ownership, `runtime.catalog` push, controller lifecycle, `bridge.reset()`, and one mounted `NappletFrame`.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Preview, approve, and launch one installed naddr over the production runtime seam</name>
  <read_first>runtime/transport.ts, runtime/portal_runtime.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/end_to_end_test.ts, tests/catalog_ui_test.tsx, .planning/phases/04-installed-napplet-discovery/04-CONTEXT.md</read_first>
  <files>runtime/transport.ts, runtime/portal_runtime.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/end_to_end_test.ts</files>
  <behavior>- Strict bounded commands carry correlation ID and only server-required selectors; unknown keys/shapes are rejected.
- Preview result carries server-derived review facts; approval carries source catalog ID; launch carries the exact triple.
- Success launches one verified artifact through the mounted frame; stale launch returns concise typed error, refreshes catalog, and remains Home.
- Socket close, timeout, replacement, and component teardown settle and delete every pending command.</behavior>
  <action>Start with transport and end-to-end expectations, then define exact-key codecs and server result unions for install preview, install approval, uninstall, and installed launch. Dispatch through `RuntimeServiceHub` to Plan 01 methods without duplicating catalog logic. In `NappletShell`, replace the unbounded boolean command map with typed correlated promises that have a deterministic timeout and are rejected/cleared on socket close, controller replacement, and unmount. Replace direct projected-srcdoc card opening with the launch request; only a successful result resets the iframe bridge, assigns returned verified identity/bytes, and navigates. On stale/unavailable failure request/accept the fresh projection, show retryable Home copy, and retain the current frame lifecycle per D-01 through D-04 and D-11 through D-13.</action>
  <verify><automated>deno test -A tests/end_to_end_test.ts tests/catalog_ui_test.tsx</automated></verify>
  <acceptance_criteria>Tests prove exact codec rejection, correlation, cleanup on every terminal transport path, server-only review facts, stale approval/launch errors, no pre-success iframe assignment, one successful mounted-frame launch, and no raw projected srcdoc launch path.</acceptance_criteria>
  <done>A real runtime connection completes the trusted install-to-launch happy path and fails stale requests closed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Push partial catalog data and synchronization status independently</name>
  <read_first>routes/api/runtime.ts, runtime/portal_runtime.ts, runtime/catalog.ts, islands/NappletShell.tsx, tests/catalog_ui_test.tsx, .planning/phases/04-installed-napplet-discovery/04-RESEARCH.md</read_first>
  <files>runtime/portal_runtime.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/catalog_ui_test.tsx</files>
  <behavior>- Initial loading can precede data; pending/ready/unavailable enrichments push incrementally.
- Refresh error changes status without replacing the last usable projection with empty data.
- Catalog replacement invalidates open review identity and late pushes cannot restore an older generation.
- Account switch resets authority to the new account while ordinary reconnect preserves last-good display.</behavior>
  <action>Expose Plan 01 projection/status emissions through the runtime bridge and make `sendCatalog` encode status separately from optional data. Remove the route's fabricated empty projection on catch. In the island, accept only structurally valid monotonic/current projections, preserve last-good cards during loading/stale/error, reset only for an authoritative account change, and invalidate review state whenever catalog event identity changes per D-04 through D-07. Keep backend projection as the sole catalog authority and browser state as display-only.</action>
  <verify><automated>deno test -A tests/catalog_ui_test.tsx tests/end_to_end_test.ts</automated></verify>
  <acceptance_criteria>Tests cover initial loading, empty, populated partial, enrichment pushes, stale/error with retained cards, recovery, review invalidation, account switch, reconnect, and rejection of older late projection.</acceptance_criteria>
  <done>WebSocket synchronization never hides known installed entries and never creates a second browser catalog authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser JSON -> same-origin runtime command decoder | Untrusted wire data requests catalog mutation or executable launch. |
| backend projection/result -> browser shell | Network messages can alter visible catalog and iframe bytes. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-06 | Tampering / Elevation of Privilege | runtime catalog commands | high | mitigate | Exact-key bounded decoders plus backend active-account and generation revalidation. |
| T-04-07 | Tampering | runtime launch result | high | mitigate | Correlate result to pending exact request and assign iframe only from successful server result. |
| T-04-08 | Denial of Service | pending command registry | high | mitigate | Bound frame size and command count; timeout and clear on close/replacement/unmount. |
| T-04-09 | Information Disclosure | error/result payloads | medium | mitigate | Return stable codes and public review facts only; omit signer material, relay internals, and executable bytes on failure. |
</threat_model>
## Artifacts this plan produces
- Exact correlated install-preview, install-approval, uninstall, and launch wire contracts
- Independent catalog status/projection pushes with last-good retention
- Production runtime boundary tests
<verification>`deno test -A tests/catalog_ui_test.tsx tests/end_to_end_test.ts && deno task check` passes.</verification>
<success_criteria>CAT-01 through CAT-03 work through the actual runtime transport, and every high-severity boundary threat is mitigated by executable tests.</success_criteria>
<output>Create `.planning/phases/04-installed-napplet-discovery/04-02-SUMMARY.md` when done.</output>
