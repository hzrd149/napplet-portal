---
phase: 04-installed-napplet-discovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [main.ts, runtime/catalog.ts, runtime/artifacts.ts, runtime/relay_policy.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, tests/catalog_test.ts, tests/relay_policy_test.ts, tests/artifact_resolver_test.ts, tests/catalog_runtime_test.ts]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "An accepted catalog entry appears immediately as pending and remains visible as ready or unavailable without waiting for every artifact (D-05, D-06, D-07)."
    - "A valid named-manifest naddr resolves through policy-approved hint plus configured read relays into immutable review facts; malformed input changes no state (D-01, D-02)."
    - "Approval is bound to the reviewed catalog generation, verifies the artifact, and advances only after every required relay accepts; failures remain retryable (D-03, D-04)."
    - "Launch re-reads current account catalog truth and releases executable bytes only for an exact catalog/coordinate/accepted-manifest triple (D-11, D-12, D-13)."
  artifacts: [runtime/catalog.ts, runtime/relay_policy.ts, main.ts, tests/catalog_test.ts, tests/catalog_runtime_test.ts]
  key_links:
    - "active-account EventStore replacement -> raw projection -> generation-guarded artifact enrichment"
    - "naddr decode -> RelayPolicy.read-approved sources -> PortalArtifactResolver -> immutable install preview"
    - "launch selectors -> latest decoded catalog -> exact accepted artifact resolver"
    - "main.ts account/settings streams -> RelayPolicy/EventRuntime production sources -> CatalogService load/status/account-change signals"
---
<objective>Establish the backend authority for installed catalog projection, install review/approval, and launch, implementing D-01 through D-07 and D-11 through D-13. Purpose: make accepted catalog truth visible independently of artifact availability while closing stale install and launch races. Output: tested catalog-domain methods and exact artifact selection.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-installed-napplet-discovery/04-CONTEXT.md
@.planning/phases/04-installed-napplet-discovery/04-RESEARCH.md
@.planning/milestones/v1.0-phases/02-backend-runtime-expansion/02-04-SUMMARY.md
@.planning/milestones/v1.0-phases/02-backend-runtime-expansion/02-05-SUMMARY.md
<interfaces>Preserve `decodeCatalogEvent(event, activePubkey)`, `CatalogService.load/subscribe`, serialized mutation publishing, and `PortalArtifactResolver` integrity checks. Evolve `CatalogProjectionEntry` from always-ready executable content to a stable accepted entry with `resolution: pending|ready|unavailable`, optional verified metadata, and no ordinary projected `srcdoc`. Add `RelayPolicy.previewReads(hints, configuredReads, limit)` as the sole preview relay selector: canonical ws/wss hints and configured reads are independently filtered against blocked policy, combined hints-first, stably deduplicated, and capped; no usable hint falls back to the same capped configured set. Resolve enrichment with exactly four workers and an `(activePubkey,catalogEventId,coordinate,acceptedManifestEventId)` in-flight key.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Stream one accepted entry from pending truth to verified readiness</name>
  <read_first>runtime/catalog.ts, runtime/artifacts.ts, runtime/relay_policy.ts, runtime/event_runtime.ts, tests/catalog_test.ts, tests/artifact_resolver_test.ts, .planning/phases/04-installed-napplet-discovery/04-CONTEXT.md</read_first>
  <files>runtime/catalog.ts, tests/catalog_test.ts</files>
  <behavior>- Raw accepted membership emits before artifact resolution completes per D-05 through D-07.
- A failed artifact retains the coordinate and accepted manifest ID with unavailable state; a later successful retry enriches the same entry.
- At most four exact-key resolutions run concurrently, duplicate requests share in-flight work, and old account/catalog completions are discarded.
- Refresh failure preserves the last usable projection and reports status separately.</behavior>
  <action>Start with deferred-promise tests, then split decoded catalog membership from artifact enrichment in `CatalogService`. Emit stable coordinate-keyed pending entries synchronously, schedule a four-worker backend enrichment queue with exact-key in-flight deduplication, publish ready/unavailable changes through the existing listener, and generation-check active pubkey, catalog event ID, coordinate, and manifest ID before applying completion. Retain the last usable projection across transient resolution/refresh failure and expose status independently. Keep accepted entries even when verification fails, but keep executable bytes absent until ready per D-05, D-06, and D-07. Do not introduce a browser catalog store or wait for all entries.</action>
  <verify><automated>deno test -A tests/catalog_test.ts</automated></verify>
  <acceptance_criteria>Tests deterministically prove immediate raw emission, pending-to-ready and pending-to-unavailable transitions, retained entry count, recovery, four-job bound, in-flight deduplication, late-result rejection, account switch isolation, and last-good preservation.</acceptance_criteria>
  <done>One accepted entry is useful immediately and safely enriches without blocking or reanimating stale truth.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add stale-bound install preview, approval, and exact launch authority</name>
  <read_first>runtime/catalog.ts, runtime/artifacts.ts, runtime/relay_policy.ts, runtime/event_runtime.ts, tests/catalog_test.ts, tests/relay_policy_test.ts, tests/artifact_resolver_test.ts, .planning/phases/04-installed-napplet-discovery/04-RESEARCH.md</read_first>
  <files>runtime/catalog.ts, runtime/artifacts.ts, runtime/relay_policy.ts, tests/catalog_test.ts, tests/relay_policy_test.ts, tests/artifact_resolver_test.ts</files>
  <behavior>- Only a bounded valid kind-35129 naddr normalizes to the canonical coordinate per D-01.
- Preview returns publisher, coordinate, manifest ID, title, version, aggregate hash, capabilities, and source catalog ID only after integrity verification per D-02.
- Approval rejects a changed source catalog, never advances optimistically, and retains retry context on verify/publish failure per D-03/D-04.
- Launch rejects stale catalog, missing/replaced/unresolved entry, or manifest mismatch and resolves only the accepted exact event per D-11 through D-13.</behavior>
  <action>Add strict bounded naddr preview input using pinned `nostr-tools/nip19.decodeNostrURI`; accept only kind 35129 and normalize canonical coordinate. Add `RelayPolicy.previewReads(hints, configuredReads, limit)` which canonicalizes only ws/wss endpoints, removes blocked endpoints, combines permitted hints before configured allowed reads, stably deduplicates, and caps the result at eight; when hints are absent or all rejected, return up to eight configured permitted reads. Return immutable server-derived review facts plus `sourceCatalogEventId`; never accept facts back from the browser. Extend serialized approval to compare that generation before verification/sign/publish and preserve complete required-relay settlement semantics. Add a launch method that re-reads the active account's latest valid catalog, compares the triple, resolves the exact accepted event through the integrity boundary, and returns a retryable typed failure otherwise. Remove `srcdoc` from ordinary catalog projection while preserving aggregate hash as review-only data per D-01 through D-04 and D-11 through D-13.</action>
  <verify><automated>deno test -A tests/catalog_test.ts tests/relay_policy_test.ts tests/artifact_resolver_test.ts</automated></verify>
  <acceptance_criteria>Focused tests cover malformed/wrong-kind/oversized naddr, non-ws hint rejection, canonical dedupe, blocked removal, hints-plus-configured stable ordering, eight-relay cap, no-hint/all-blocked fallback, exact review fields, changed-generation rejection, all-relay settlement, recoverable failure, every stale-launch matrix case, newer-unapproved manifest retention, and exact verified bytes on success.</acceptance_criteria>
  <done>Backend catalog authority can preview, approve, and launch without trusting stale browser state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Construct the arbitrary-coordinate catalog runtime in production</name>
  <read_first>main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, runtime/artifacts.ts, runtime/relay_policy.ts, runtime/catalog.ts, tests/end_to_end_test.ts, .planning/phases/04-installed-napplet-discovery/04-RESEARCH.md</read_first>
  <files>main.ts, runtime/event_runtime.ts, runtime/portal_runtime.ts, runtime/artifacts.ts, tests/catalog_runtime_test.ts</files>
  <behavior>- Production preview resolves an arbitrary valid kind-35129 coordinate from the selected hint/configured relay set rather than the configured fixture.
- Exact manifest event ID is loaded and verified against coordinate publisher/identifier before artifact resolution.
- Active-account change cancels the old NIP-78 subscription, clears old authority, starts the new policy-approved subscription, and signals loading.
- Relay event, EOSE, error, reconnect, and settings relay changes produce explicit CatalogService load/status signals without replacing last-good data.
- A non-fixture production-path test previews and launches a second coordinate through injected relay/Blossom sources.</behavior>
  <action>Replace the fixture-only `resolveCatalogArtifact` composition with a production resolver that accepts coordinate, exact manifest event ID, and selected relays; load the exact event through `EventRuntime`, require kind/pubkey/d-tag to match the normalized coordinate and ID, then pass its declared Blossom data through `PortalArtifactResolver` before returning review or executable output. In `main.ts`, construct one live `RelayPolicy` from reactive settings, inject its `previewReads` selector and configured reads into `CatalogService`, and wire active-account plus settings changes to generation cancellation/reload. Subscribe the current account's NIP-78 filter through the policy-approved configured read set and map relay next/EOSE/error plus reconnect/settings/account changes to explicit CatalogService loading/ready/stale/error/account-reset signals. Ensure replacement subscriptions are torn down before new ones start and arbitrary naddr hints never mutate global settings. Add `tests/catalog_runtime_test.ts` using injected relay and fetch sources to prove a non-fixture coordinate previews, approves, synchronizes, and launches exact verified bytes through the same construction seams.</action>
  <verify><automated>deno test -A tests/catalog_runtime_test.ts tests/catalog_test.ts tests/relay_policy_test.ts tests/artifact_resolver_test.ts && deno task check</automated></verify>
  <acceptance_criteria>The production composition no longer compares catalog requests to `fixture.coordinate`; tests prove arbitrary-coordinate exact-event loading, policy-selected relay use, manifest coordinate/ID rejection, Blossom integrity verification, account/settings subscription replacement, EOSE/error/stale signaling, last-good preservation, and complete non-fixture preview-to-launch behavior.</acceptance_criteria>
  <done>`main.ts` constructs a process-owned catalog runtime that works for arbitrary approved napplets and reacts correctly to real account, relay, and synchronization lifecycle signals.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser naddr/selectors -> CatalogService | Untrusted strings select relay reads, catalog mutations, and executable artifacts. |
| relay/EventStore -> catalog projection | Signed network events and asynchronous artifact results can replace visible authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering / Elevation of Privilege | install approval TOCTOU | high | mitigate | Bind preview and approval to source catalog ID, re-read active-account truth inside serialized mutation, and test changed-generation rejection. |
| T-04-02 | Spoofing / Tampering | installed launch selectors | high | mitigate | Triple-key latest-catalog comparison plus exact accepted artifact verification before returning bytes. |
| T-04-03 | Server-Side Request Forgery | naddr relay hints | high | mitigate | Bound input, accept canonical ws/wss only, and apply existing direction/block policy before relay access. |
| T-04-04 | Denial of Service | artifact enrichment | high | mitigate | Four-job queue, exact-key in-flight deduplication, generation discard, and deterministic concurrency tests. |
| T-04-05 | Information Disclosure | catalog projection | medium | mitigate | Project public review metadata only; omit executable srcdoc and signer/account internals. |
</threat_model>
## Artifacts this plan produces
- Partial-state `CatalogProjectionEntry` and generation-guarded enrichment lifecycle
- Immutable install preview, stale-bound approval, and exact accepted-manifest launch methods
- Backend catalog authority regression suite and non-fixture production construction test
<verification>`deno test -A tests/catalog_test.ts tests/relay_policy_test.ts tests/artifact_resolver_test.ts tests/catalog_runtime_test.ts` and `deno task check` pass.</verification>
<success_criteria>CAT-01, CAT-02, and CAT-03 have a tested backend authority seam honoring every cited locked decision and blocking all high-severity threats.</success_criteria>
<output>Create `.planning/phases/04-installed-napplet-discovery/04-01-SUMMARY.md` when done.</output>
