---
phase: 05-resource-and-blossom-transfer
plan: "04"
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified: [runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, runtime/connections.ts, routes/api/runtime.ts, islands/NappletShell.tsx, main.ts, tests/nap_dispatcher_test.ts, tests/end_to_end_test.ts]
autonomous: true
requirements: [RES-01, RES-02, RES-03, UPL-01, UPL-02, UPL-03]
must_haves:
  truths:
    - "RESOURCE cancel, UPLOAD cancellation, window expiry, and runtime shutdown abort owned work; transient socket detach during reconnect grace does not."
    - "A verified napplet can use every closed RESOURCE/UPLOAD action through the correlated runtime seam under per-window quotas."
    - "Upload status is scoped to connection/window/verified napplet/account and pushes only to its owner."
  artifacts: [runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, runtime/connections.ts, routes/api/runtime.ts, islands/NappletShell.tsx, main.ts, tests/nap_dispatcher_test.ts, tests/end_to_end_test.ts]
  key_links:
    - "ConnectionRegistry.destroyWindow -> dispatcher abortWindow -> resource/upload AbortControllers"
    - "runtime settings + active signer -> process-owned services -> owned dispatcher -> binary/canonical iframe result"
---
<objective>Wire the complete RESOURCE/UPLOAD capability into process and connection lifecycle. Purpose: turn the isolated services into a safe usable NAP surface with deterministic cancellation, quotas, status ownership, and shutdown. Output: process-owned dispatcher integration and end-to-end coverage.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/05-resource-and-blossom-transfer/05-02-SUMMARY.md
@.planning/phases/05-resource-and-blossom-transfer/05-03-SUMMARY.md
@runtime/portal_runtime.ts
@runtime/connections.ts
@routes/api/runtime.ts
@main.ts
<interfaces>Construct one `NapDispatcher` with resource service, transfer service, settings getter, active account/signer, and `send(owner,message,bytes?)`. Its owner key includes connectionId, windowId, verified napplet identity, and active account. `ConnectionRegistry` already invokes `destroyWindow(windowId)` only after grace expiry; preserve that ordering and use it as the cancellation boundary.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Cancel one live RESOURCE request exactly when its window expires</name>
  <files>runtime/nap_dispatcher.ts, runtime/connections.ts, runtime/portal_runtime.ts, tests/nap_dispatcher_test.ts</files>
  <behavior>- Explicit `resource.cancel` aborts the matching owner/request and produces no response.
- Socket detach preserves work during reconnect grace; reattachment keeps the owner; grace expiry aborts once and drops late completion.
- Duplicate active IDs and a third byte-bearing operation return quota-exceeded without starting work.
- Service destroy aborts every operation and clears retained upload state.</behavior>
  <action>Create `NapDispatcher` with an owned in-flight registry keyed by the complete authority tuple and request ID. Combine explicit controller, service shutdown, and per-operation deadline signals; register before work, delete in `finally`, ignore unknown/late cancellation, and suppress late results after owner invalidation. Extend `RuntimeServiceHub` window cleanup and `ConnectionRegistry.destroyWindow` composition so expiry, explicit window removal, and process destruction call dispatcher cleanup, while ordinary WebSocket close only detaches and starts grace. Enforce two byte-bearing operations per window and duplicate-ID rejection before reading bytes or opening the network.</action>
  <verify><automated>deno test -A tests/nap_dispatcher_test.ts tests/websocket_session_test.ts</automated></verify>
  <done>Cancellation and quota tests prove exact ownership, grace preservation, expiry cleanup, shutdown cleanup, and no late delivery.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire every RESOURCE/UPLOAD action through production and verify closed outcomes</name>
  <files>runtime/nap_dispatcher.ts, runtime/portal_runtime.ts, routes/api/runtime.ts, islands/NappletShell.tsx, main.ts, tests/nap_dispatcher_test.ts, tests/end_to_end_test.ts</files>
  <behavior>- info, bytes, bytesMany, cancel, upload, status, and status.changed retain pinned envelope names and correlation behavior.
- Foreign or expired upload IDs return generic unavailable results and never reveal another owner's status or URLs.
- Settings changes affect subsequent operations without restart; in-flight operations retain their validated snapshot.
- Existing RELAY, OUTBOX, catalog, reconnect, and verified launch behavior remains passing.</behavior>
  <action>Construct the resource and Blossom transfer services once in `main.ts` from `runtimeSettings.settings$`, exact local-cache discovery, and `signerAccounts.signEvent`; inject them into the existing process runtime rather than creating per-route instances. Extend the route and island binary/control dispatch to all pinned actions, including ordered binary `bytesMany`, upload input, correlated results, status queries, and owner-only uncorrelated `upload.status.changed` pushes. Snapshot validated settings at operation start. Scope retained upload status by verified frame identity and current account as well as connection/window; sign-out/window expiry invalidates authority. Preserve the existing 32-entry browser command registry behavior for control correlations and keep binary byte ceilings separate from its JSON character ceiling.</action>
  <verify><automated>deno test -A tests/nap_dispatcher_test.ts tests/end_to_end_test.ts tests/runtime_contract_test.ts tests/websocket_session_test.ts tests/catalog_ui_test.tsx</automated></verify>
  <done>All six Phase 5 requirements pass production-shaped end-to-end, ownership, cancellation, settlement, and regression tests.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| verified window -> process services | Capability calls cross from untrusted napplet execution into backend authority. |
| connection lifecycle -> operation lifetime | Reconnect, expiry, sign-out, and shutdown change who may receive or inspect results. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-12 | Elevation of Privilege / Information Disclosure | upload status ownership | high | mitigate | Scope by connection, window, verified identity, and account; generic foreign/expired response tests. |
| T-05-13 | Denial of Service | in-flight registry | high | mitigate | Duplicate rejection, two-operation owner cap, deadlines, finally cleanup, expiry/shutdown abort. |
| T-05-14 | Tampering | stale completion after authority change | high | mitigate | Generation/owner validity check immediately before delivery and sign-out/window cleanup invalidation. |
| T-05-15 | Repudiation | cancellation/status transitions | low | accept | Canonical terminal status and sanitized internal outcome records provide sufficient Phase 5 evidence without persistent audit storage. |
## Source Coverage Audit
| Source | Items | Coverage |
|---|---|---|
| GOAL | bounded RESOURCE and backend-owned Blossom uploads | 05-01 through 05-04 |
| REQ | RES-01, RES-02, RES-03, UPL-01, UPL-02, UPL-03 | all listed in plan frontmatter and task done states |
| RESEARCH | binary Blob transport, closed codecs, every-hop SSRF, incremental bounds, cache optionality, cancellation, canonical upload settlement, exact authorized SDK pin, and explicit 0.31.0 optional-local representation mismatch | 05-01, 05-02, 05-03, 05-04; Phase 9 compatibility marker is created by 05-03 |
| CONTEXT | every locked Resource policy, URL safety, Blossom settlement, and runtime lifecycle decision | 05-01 through 05-04; deferred arbitrary allowlists, browser-direct uploads, and generalized caching excluded |
</threat_model>
<verification>`deno task test` and `deno task check` pass; production build/start WebSocket smoke remains green.</verification>
<success_criteria>RESOURCE and UPLOAD are complete backend-owned capabilities with binary fidelity, closed contracts, every-hop network policy, all-required upload settlement, optional local caching, and deterministic lifecycle cleanup.</success_criteria>
<output>Create `.planning/phases/05-resource-and-blossom-transfer/05-04-SUMMARY.md` when done.</output>
