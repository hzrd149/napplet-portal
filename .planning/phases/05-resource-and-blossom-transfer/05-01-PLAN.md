---
phase: 05-resource-and-blossom-transfer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [runtime/binary_transport.ts, runtime/transport.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/binary_transport_test.ts]
autonomous: true
requirements: [RES-01, RES-02, UPL-01]
must_haves:
  truths:
    - "A sandboxed napplet can inspect closed RESOURCE and UPLOAD capability envelopes without browser-owned policy."
    - "One bounded HTTPS resource crosses the existing WebSocket as binary and reaches the iframe as the canonical Blob result."
    - "Binary frames cannot escape their connection, window, request, type, or byte-length binding."
  artifacts: [runtime/binary_transport.ts, runtime/transport.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/binary_transport_test.ts]
  key_links:
    - "canonical iframe RESOURCE/UPLOAD envelope -> binary WebSocket codec -> owned backend command"
    - "backend binary result -> correlated browser frame -> canonical iframe Blob result"
---
<objective>Prove the binary, closed-contract RESOURCE/UPLOAD seam end to end. Purpose: remove the architectural risk that JSON serialization destroys Blob and ArrayBuffer values before broader policy and transfer behavior is built. Output: production binary framing, strict control codecs, canonical info responses, and one bounded HTTPS RESOURCE tracer.</objective>
<execution_context>@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-resource-and-blossom-transfer/05-CONTEXT.md
@.planning/phases/05-resource-and-blossom-transfer/05-RESEARCH.md
@.planning/phases/04-installed-napplet-discovery/04-03-SUMMARY.md
@runtime/transport.ts
@routes/api/runtime.ts
@islands/NappletShell.tsx
<interfaces>Preserve `decodeClientMessage(raw, owner)` for ordinary JSON NAP traffic and the existing connection/window ownership checks. Add a binary frame header containing a fixed magic/version, frame kind, bounded UTF-8 correlation ID, declared payload length, and owner binding supplied by the authenticated socket rather than trusted from bytes. At iframe edges retain pinned `resource.info`, `resource.bytes`, `upload.info`, and `upload.upload` shapes with real Blob/ArrayBuffer values.</interfaces></context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Carry one canonical RESOURCE Blob through the full runtime seam</name>
  <files>runtime/binary_transport.ts, runtime/transport.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/binary_transport_test.ts</files>
  <behavior>- A canonical `resource.bytes` iframe request sends bounded metadata plus a binary payload-free command and receives one canonical `resource.bytes.result` containing a Blob.
- Fragmented or concatenated binary frames decode deterministically; wrong magic/version/kind, oversized metadata/payload, mismatched length, duplicate active ID, foreign owner, and late result fail closed.
- JSON forwarding remains compatible for existing RELAY, OUTBOX, and catalog traffic.</behavior>
  <action>Write the failing production-shaped codec test first. Add `runtime/binary_transport.ts` with encode/decode functions that never accept owner identifiers from untrusted frame bytes and never base64-expand content. Extend `runtime/transport.ts` with exact closed RESOURCE/UPLOAD control-envelope decoding from pinned 0.31.0 types. In `NappletShell`, intercept only canonical RESOURCE/UPLOAD messages from the already registered verified iframe source; preserve structured-clone Blob/ArrayBuffer values, encode bounded binary frames, and reconstruct canonical Blob results before `postMessage`. In the WebSocket route, distinguish text from ArrayBuffer frames without coercing binary to string, enforce separate metadata and 5 MiB payload ceilings, and implement a deterministic injected HTTPS fetch tracer with incremental reading and a 10-second deadline. Do not let the island choose destinations, limits, MIME policy, server lists, or signer behavior.</action>
  <verify><automated>deno test -A tests/binary_transport_test.ts tests/runtime_contract_test.ts tests/iframe_bridge_test.ts</automated></verify>
  <done>One real binary RESOURCE happy path works from canonical iframe request to backend and back, while malformed/foreign frames and JSON regressions are rejected by tests.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Advertise closed RESOURCE and UPLOAD policy snapshots</name>
  <files>runtime/transport.ts, routes/api/runtime.ts, islands/NappletShell.tsx, tests/binary_transport_test.ts</files>
  <behavior>- `resource.info` advertises `https` and `blossom`, maxBytes 5242880, and maxUrls 8 using only pinned fields.
- `upload.info` advertises the blossom rail, maxBytes 5242880, the closed MIME allowlist, and canonical return fields.
- Unknown keys, malformed IDs, unsupported actions, and non-canonical byte-bearing shapes are denied without network work.</behavior>
  <action>Define one immutable backend policy snapshot with 5 MiB, eight URLs, two active byte-bearing operations per window, three redirects, 2,048 URL characters, 10-second resource and 30-second upload deadlines, and a conservative passive MIME allowlist for PNG, JPEG, GIF, WebP, AVIF, plain text, JSON, and PDF. Generate pinned info envelopes from that same snapshot so advertised and enforced limits cannot drift. Keep HTTP reserved for the exact configured loopback cache origin and do not advertise it. Map decoder failures to canonical generic errors without internal URLs, resolved addresses, or exception text.</action>
  <verify><automated>deno test -A tests/binary_transport_test.ts tests/runtime_contract_test.ts</automated></verify>
  <done>RES-01 and UPL-01 inspection surfaces are canonical, closed, backend-derived, and mechanically tied to enforcement constants.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| sandboxed iframe -> island -> WebSocket | Untrusted envelopes and bytes cross two serialization boundaries. |
| WebSocket -> backend fetch | A verified window asks the process to spend network and memory resources. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering / Elevation of Privilege | binary frame ownership | high | mitigate | Bind decoded frames to authenticated socket owner, strict version/kind/length codecs, and foreign-owner tests. |
| T-05-02 | Denial of Service | binary buffering | high | mitigate | Enforce 5 MiB payload and small metadata ceilings before allocation plus two active operations per window. |
| T-05-03 | Information Disclosure | canonical error projection | medium | mitigate | Stable public codes/messages only; never serialize destinations, DNS answers, auth, or raw exceptions. |
| T-05-SC | Tampering | dependency imports | high | mitigate | Add no package-manager dependency in this plan; retain exact installed pins and lockfile. |
</threat_model>
<verification>`deno test -A tests/binary_transport_test.ts tests/runtime_contract_test.ts tests/iframe_bridge_test.ts` and `deno task check` pass.</verification>
<success_criteria>The phase architecture is proven through one production-quality binary HTTPS resource path and canonical RESOURCE/UPLOAD info envelopes.</success_criteria>
<output>Create `.planning/phases/05-resource-and-blossom-transfer/05-01-SUMMARY.md` when done.</output>

